# Codex 依頼書 — 2026-04-18（第2便）

**依頼者**: よしひろさん（Claude Code 監査役 経由でドラフト）
**対象リポ**: wordpressyoshilover
**前便**: `docs/handoff/codex_requests/2026-04-18.md` / `codex_responses/2026-04-18.md`
**本便の方針**: T-007 パーサーバグの根本修正 → 疑陽性を洗い直し → 受け入れ試験再開の前提を整える

## 背景

前便の調査で以下が判明:

- T-007: `_extract_score()` が raw text 全体から score を拾い、UUID 断片 `4725-97a1` を `25-97` と誤認していた。venue fallback も月間日程の切り出し誤りで誤値を拾う。
- 影響範囲: 直接表面化は 62518。同種 score 誤抽出を 62527 / 62540 に確認。T-002 の6件（62044/61981/61886/61802/61770/61598）は venue fallback 疑陽性の可能性が高い。
- T-001 の原因は `src/yoshilover-exclude-cat.php:16-44` の `pre_get_posts` が REST を除外していないこと（修正は後回し可）。
- T-003 は `src/draft_inventory_from_logs.py` で解決（現在 draft = 77件）。

詳細は `docs/handoff/codex_responses/2026-04-18.md` と `docs/handoff/tickets/OPEN.md#T-007` を参照。

## 依頼タスク（優先順）

### 1. 【T-007 🔴 最優先・実装】fact_check パーサーの根本修正

**修正対象**: `src/acceptance_fact_check.py`

**Codex 自身が前便で提示した修正方針をそのまま実装してください**:

1. `_source_reference_facts()`（`:311-328`）から score/venue の raw text 抽出経路を削除。
   `_extract_score()` を raw page text 全体に適用する現仕様を禁止し、
   structured source（例: game reference API の score 構造体）のみ許可する。
2. `_fetch_npb_schedule_snapshot()`（`:356-375`）を、date block の文字数切り出しから
   HTML 構造（対象日の日程行 DOM）ベースの抽出へ書き換える。
3. `_check_game_facts()`（`:518-570`）で expected 値の supply origin に応じて evidence URL を分岐:
   - `reference` 由来 → game reference URL
   - `source_facts` 由来 → source URL
4. regression test を追加:
   - 62518 ケース（UUID 断片 `4725-97a1` から `25-97` が抽出されないこと）
   - NPB 月間日程の切り出しズレで別試合 venue を拾わないこと

### 2. 【T-007 影響範囲】修正後の再判定

修正実装が通ったら、以下の post_id を `acceptance_fact_check` で再判定し、
結果を **`docs/fix_logs/{date}_t007_post_fix_recheck.md`** に Markdown で出力:

- score 誤抽出疑い: `62518`, `62527`, `62540`
- venue fallback 疑陽性（T-002）: `62044`, `61981`, `61886`, `61802`, `61770`, `61598`

各 post_id について、**修正前/修正後** の `result`（green/yellow/red）と findings の差分が
分かる形式にしてください。

### 3. 【T-002 副次】再判定を受けたチケット更新

上記 #2 の結果から、T-002（公開中6件）の各 post_id が以下どれに該当するか整理:

- **A. 修正後 green**: 疑陽性確定 → T-002 の該当行に「green確認済」を付記
- **B. 修正後も RED**: 真の事実誤記 → 引き続き要対応として残す
- **C. yellow**: 判定不安定 → 個別確認必要

整理結果は `docs/handoff/codex_responses/2026-04-18_02.md` に含める。

## 制約

- **deploy / env変更 / Cloud Run 反映は本便では一切行わない**
- T-001 の WP プラグイン修正は本便では行わない（受け入れ試験の前提にはならないため）
- T-008（62527 title 自動修正実行）は T-007 再判定の結果次第なので本便では行わない
- 現状の `.codex` / `data/` 未追跡ファイルは触らない
- テストは 362 passed（または追加分のみ増加）を維持

## 完了報告

1. `docs/handoff/codex_responses/2026-04-18_02.md` に結果をまとめる
   - T-007 実装の diff サマリ
   - 修正前/修正後の fact_check 再判定結果（9件）
   - T-002 の3分類（A/B/C）
2. `docs/fix_logs/{date}_t007_post_fix_recheck.md` を生成
3. commit & push 後、よしひろさん に
   「T-007 修正完了。再判定の結果まとめを確認してほしい」と伝達

## 参考

- 本件 tickets: `docs/handoff/tickets/OPEN.md#T-007`, `#T-002`
- 前便 response: `docs/handoff/codex_responses/2026-04-18.md`
- 全体設計: `AGENTS.md`
