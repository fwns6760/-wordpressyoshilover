# Codex 依頼書 — 2026-04-18（第3便・deploy）

**依頼者**: よしひろさん（Claude Code 監査役 経由でドラフト）
**対象リポ**: wordpressyoshilover
**前便**: `codex_responses/2026-04-18_02.md`（T-007 修正完了、366 passed）
**本便の目的**: T-007 修正 + auto_fix/notifier 拡張 + draft_inventory を Cloud Run に反映

**前提**: よしひろさんの明示的な deploy 承認を得た後に実行してください。
未承認状態ではこの依頼の 1〜3 を実行してはいけません。

## 反映対象の commit

- `ba97edc` fix: harden fact check game reference parsing（T-007本体）
- `957f458` feat: add acceptance auto-fix dry run workflow
- `10fa214` T-003 draft_inventory_from_logs + WP/Codex調査結果
- `cb9c7e0` docs: update fact check scheduler cadence
- ほか master HEAD までの docs 群（上記2便分）

## 変更しないもの（重要）

- **env 変数は一切変更しない**
  - `RUN_DRAFT_ONLY=1` 維持
  - `ENABLE_PUBLISH_FOR_*=0` 全て維持
  - `GMAIL_APP_PASSWORD_SECRET_NAME` 変更なし
- **Scheduler ジョブのスケジュール変更なし**（7/12/17/22 JST 維持）
- **Secret Manager のバージョン変更なし**

つまり、**コードのみ**を新しい revision に差し替える。

## 依頼タスク

### 1. Cloud Run へ deploy

- サービス: `yoshilover-fetcher`（asia-northeast1、baseballsite プロジェクト）
- 現 revision: `yoshilover-fetcher-00130-nxg`
- 新 revision: `00131-*` 以降
- 手順は `docs/phase_c_runbook.md` のデプロイ節に従う
- deploy コマンド実行ログは `docs/handoff/codex_responses/2026-04-18_03.md` に貼る

### 2. smoke test

deploy 完了後、以下を実行:

1. **Scheduler の手動トリガー**（1回だけ）
   - `rss-fetcher-scheduled` を手動 run
   - Cloud Logging で新 revision が起動したことを確認
2. **fact_check_email_sent が新 revision で出ること**（次の cron 発火 or 手動 run で）
3. **rss_fetcher_run_summary / flow_summary / featured image / ERROR**
   - ERROR レベル 0 を確認
   - `draft_only=true` である（publish が解放されていない）ことを確認

### 3. 新パーサーの動作確認

- 次の Scheduler 発火（17:00 or 22:00 JST）でメール受信
- メール本文の 🔴 / 🟡 / ✅ / 自動修正候補 / 差し戻し推奨 / 手動確認必要 を点検
- 特に `score=25-97` のような明らかな異常値が**出ていない**こと

### 4. 問題発生時の rollback

- 新 revision で ERROR 急増 or 異常判定が出た場合、
  `yoshilover-fetcher-00130-nxg` に traffic 100% 戻し
- rollback 手順は `docs/phase_c_runbook.md` 参照
- 戻したら本依頼書の結果欄に「rollback 実施」と記録

## 本便でやらないこと

- env 変更（`RUN_DRAFT_ONLY` / `ENABLE_PUBLISH_FOR_*`）
- T-002（61981 opponent 阪神→楽天 の記事修正）
- T-008（62527 の title auto_fix 本番実行）
- T-010（61770/61598 の source_reference_missing 対応）
- T-001（WP `yoshilover-exclude-cat.php` の pre_get_posts 修正）

## 完了報告

1. `docs/handoff/codex_responses/2026-04-18_03.md` に:
   - deploy コマンド実行ログ
   - 新 revision 名
   - smoke test 結果（ERROR 0 確認、draft_only=true 確認）
   - 次 Scheduler 発火メールの結果（後追い）
2. commit & push
3. よしひろさん に「deploy 完了。次の Scheduler メールを見てほしい」と伝達

## 参考

- Phase C runbook: `docs/phase_c_runbook.md`
- Operation logs: `docs/operation_logs.md`
- 監査チケット: `docs/handoff/tickets/OPEN.md#T-011`
