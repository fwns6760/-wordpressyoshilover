# Codex 全量依頼書 — 2026-04-18（第4便・クロージング）

**依頼者**: よしひろさん（Claude Code 監査役 経由でドラフト）
**対象リポ**: wordpressyoshilover
**前便**: `codex_responses/2026-04-18_02.md`（T-007 修正完了、366 passed、commit `ba97edc`）
**本便の目的**: 未解決チケット T-001 / T-002 / T-008 / T-011 を本便で全量クロージング

## 背景（現状の総括）

- **残チケット**: T-011（deploy）/ T-001（WP plugin）/ T-002（61981 opponent）/ T-008（62527 title）/ T-010（yellow 2件）
- **既解決**: T-003（draft_inventory 実装）/ T-007（fact_check パーサー修正）/ T-002 Aクラス3件（疑陽性確定）
- **Cloud Run**: 旧パーサーのまま（revision `yoshilover-fetcher-00130-nxg`）。本便で最新 `ba97edc` へ反映する
- **本番 env**: `RUN_DRAFT_ONLY=1` / `ENABLE_PUBLISH_FOR_*=0` → 本便では**一切変更しない**

## 依頼の全体像

以下を **Step 1 → Step 6 の順に** 実行してください。各 Step の成功確認が取れてから次へ進む。
途中で想定外のエラー（ERROR レベル）・テスト失敗・smoke test 異常が出たら、
**その時点で停止** して `codex_responses/2026-04-18_04.md` に状態を記録し、よしひろさんに一報。

---

### Step 1. 【T-011】Cloud Run へ最新 master を deploy

**対象 commit**: master HEAD（本便着手時の最新）
**現行 revision**: `yoshilover-fetcher-00130-nxg`

**作業**:
1. `docs/phase_c_runbook.md` の deploy 節に従って Cloud Run `yoshilover-fetcher` を更新
2. env 変数は現状維持（`RUN_DRAFT_ONLY=1` / `ENABLE_PUBLISH_FOR_*=0`）
3. 新 revision の番号（`00131-*` 以降）を控える

**smoke test**:
- Scheduler `rss-fetcher-scheduled` を1回だけ手動 trigger
- Cloud Logging で新 revision が起動したことを確認
- ERROR レベル 0 を確認
- `draft_only=true` を確認（publish が解放されていないこと）

**失敗時**: 旧 revision `00130-nxg` に traffic 100% 戻し → ここで全 Step 中止

---

### Step 2. 【T-001】WP プラグイン `yoshilover-exclude-cat.php` の REST ガード追加

**対象**: `src/yoshilover-exclude-cat.php:16-44` の `pre_get_posts` フック

**修正**:
フック関数の先頭に以下を追加:
```php
if ( defined('REST_REQUEST') && REST_REQUEST ) {
    return;
}
```

**反映**:
- WP 側へ差し替え（FTP / xserver ファイルマネージャ / Git 連携のいずれか運用中の手段で）
- 反映後、以下3本が意図通り動くことを確認:
  - `curl .../posts?status=draft&per_page=5&context=edit` → draft のみ返る
  - `curl .../posts?status=publish&per_page=5&context=edit` → publish のみ返る
  - 既存フロント一覧（テーマ側）の除外カテゴリ動作が保たれている

**確認コマンドの結果**: `codex_responses/2026-04-18_04.md` に載せる

---

### Step 3. 【T-002】p=61981 の opponent 阪神→楽天 を修正

**対象**: `post_id=61981`、postgame、公開中
**evidence**: `https://www.nikkansports.com/baseball/news/202604140001354.html`

**作業**:
1. 対象記事の title と本文で「阪神」を「楽天」に**文脈上適切な箇所のみ**置換
   - 「阪神タイガース」「阪神が」「対阪神」など opponent を指す表現を変更
   - チーム戦績文・引用内の阪神（無関係な言及）は残す
2. `acceptance_auto_fix` の whitelist に opponent が入っていないなら、
   手動で `WPClient` 経由の `update_post` を使う
3. 修正後、`acceptance_fact_check --post-id 61981` で green になることを確認
4. `status=publish` のまま（draft 戻ししない）

**注意**:
- 文中の画像 alt / キャプションも確認
- タイトルは既存を尊重して必要最小限の書き換え
- 書き換えた diff を `codex_responses/2026-04-18_04.md` に掲載

---

### Step 4. 【T-008】p=62527 の title DeNA→ヤクルト を auto_fix で修正

**対象**: `post_id=62527`、postgame、公開中
**evidence**: `https://www.nikkansports.com/baseball/news/202604170001523.html`

**作業**:
1. `python3 -m src.acceptance_auto_fix --post-id 62527` を dry-run で再確認（提案内容が `DeNA` → `ヤクルト` の title 1箇所置換であること）
2. 問題なければ本番モードで実行（`--apply` 等のフラグは実装に従う）
3. 適用後 `acceptance_fact_check --post-id 62527` で green を確認
4. `status=publish` のまま

---

### Step 5. Step 3 / 4 の確認 + T-010 の方針決定

**再 fact_check**:
- p=61981 と p=62527 が green になったこと
- 影響外の近傍記事（直近10件程度）が引き続き green / 変化なし であること

**T-010 の扱い**:
- 61770 / 61598 の yellow（`source_reference_missing`）は**本便では放置**
- `codex_responses/2026-04-18_04.md` に「T-010 は放置、別途よしひろさん判断を待つ」と記載するのみ
- 記事改変は行わない

---

### Step 6. 完了報告

`docs/handoff/codex_responses/2026-04-18_04.md` を新規作成し、以下を記載:

1. **Step 1 deploy**: 新 revision 名 / smoke test 結果 / ERROR 数
2. **Step 2 WP plugin**: `REST_REQUEST` ガード diff / 3本の curl 確認結果
3. **Step 3 61981**: 記事 diff / fact_check 再判定結果
4. **Step 4 62527**: auto_fix 実行ログ / fact_check 再判定結果
5. **Step 5**: 近傍記事の fact_check 結果 / T-010 放置の記載
6. **テスト**: `python3 -m unittest discover -s tests` の結果（366以上）
7. **チケット closing 提案**:
   - T-011 RESOLVED
   - T-001 RESOLVED
   - T-002 RESOLVED
   - T-008 RESOLVED
   - T-010 OPEN 継続

commit & push 後、よしひろさんに **1行だけ** 報告:
> 「全量クロージング完了。監査確認お願いします」

---

## 絶対にやらないこと（再掲）

- **env 変更**: `RUN_DRAFT_ONLY` / `ENABLE_PUBLISH_FOR_*` の変更
- **Secret Manager バージョン変更**
- **Scheduler スケジュール変更**
- **Phase C 公開解放**（`ENABLE_PUBLISH_FOR_POSTGAME=1` など）
- **T-010 の記事改変**（yellow のまま残す）

## 例外処理

- Step 1 deploy で異常 → 即 rollback + 全 Step 中止 + 報告
- Step 2〜4 のいずれかで想定外のテスト失敗 / 記事改変ミス → 該当 Step を rollback（WP 記事は前版に戻す）+ 中止 + 報告
- 途中ステップで `ENABLE_PUBLISH_FOR_*` や `RUN_DRAFT_ONLY` を触りたくなった場合 → **必ず** よしひろさんに確認してから（事故4記事の再発防止）

## 参考

- Phase C runbook: `docs/phase_c_runbook.md`
- Operation logs: `docs/operation_logs.md`
- 失敗パターン: `docs/handoff/06_failure_patterns.md`（特に 2026-04-17 事故）
- 未解決チケット: `docs/handoff/tickets/OPEN.md`
- 全体設計: `AGENTS.md`
