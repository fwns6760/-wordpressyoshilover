# Codex 依頼書 — 2026-04-18（第6便・T-008 残件クローズ）

**依頼者**: よしひろさん
**前便**: `codex_responses/2026-04-18_05.md`（Step A 完了 / Step B 停止）
**本便の目的**: `p=62527` の body 側残件だけ対応し T-008 をクローズ

## 背景

第5便で:
- `61981` → green 確認 → T-002 RESOLVED 提案済み
- `62527` → title は `DeNA → ヤクルト` 済みだが、本文に `DeNA` が複数残り red 継続

残存 `DeNA` の切り分け（よしひろさん判断済み）:
- **summary ブロックの `相手=DeNA`** → 誤り。`ヤクルト` に直す
- **本文中の `4日DeNA戦と並んで今季最多8得点`（複数行）** → **過去試合への言及なので触らない**

Codex が body 全置換を避けて停止した判断は正しい。本便ではその切り分けを明示する。

---

## 依頼内容

p=62527 の残件だけ対応してください。

### 目的

- summary 欄の `相手=DeNA` は誤りなので `ヤクルト` に修正
- 本文中の `4日DeNA戦と並んで今季最多8得点` は過去試合への言及なので変更しない

### 要件

1. live post `62527` を取得して、content 内の **summary ブロックだけを対象にする**
2. `相手` 欄の値 `DeNA` → `ヤクルト` を **1 箇所だけ** 置換
3. 本文中の `4日DeNA戦` は **一切変更しない**
4. 更新後に下記を実行:
   ```bash
   python3 -m src.acceptance_fact_check --post-id 62527 --json
   ```
5. green になれば T-008 クローズ提案
6. 結果を `docs/handoff/codex_responses/2026-04-18_06.md` に記録して commit & push

### 異常時

- `4日DeNA戦` に触れそうなら中止して報告
- green にならなければ中止して残存 findings を報告

### 禁止

- title はもう触らない（前便で修正済み）
- 全文置換しない
- env変更しない
- deploy しない

### 成功判定

- fact_check `result=green` かつ `findings=[]`
- 本文中の `4日DeNA戦` が 5 箇所すべて保持されている（diff で確認）

### 所要時間

- 想定 10〜15 分

---

## 次の動き（よしひろさん側）

Codex から `codex_responses/2026-04-18_06.md` 報告が返ってきたら:
- green 確認 → T-008 RESOLVED 提案を承認
- T-002 と合わせて RESOLVED.md へ移動（Claude Code 側で更新）
- 残 OPEN は T-001（WP plugin、Xserver 情報待ち）と T-010（yellow 放置）のみ

## 参考

- 前便レポート: `docs/handoff/codex_responses/2026-04-18_05.md`
- 未解決チケット: `docs/handoff/tickets/OPEN.md`
- 失敗パターン: `docs/handoff/06_failure_patterns.md`
