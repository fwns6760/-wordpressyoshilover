# Codex 依頼書 — 2026-04-18（第18便・Phase C postgame/lineup 解放）

**依頼者**: よしひろさん（Claude Code 経由・明示承認済み）
**前便**: `codex_responses/2026-04-18_17.md`（T-001 REST ガード コード修正完了）
**本便の目的**: `postgame` / `lineup` の publish を解放し、Phase C 本格稼働に入る

## 背景

- 今日の全量 fact_check 監査で **publish red 0件** を確認済み（新 parser, revision `00135-rpc`）
- T-007 / T-012 修正で venue/score 誤抽出・歴史参照 false positive は解消済み
- yellow 残は `source_reference_missing` A=5件と `subject:needs_manual_review` 4件のみで、どちらも systemic ではない（WONTFIX 寄せ保留可）
- 受け入れ試験の代わりとしての品質ゲートは実質クリア
- CLAUDE.md に記載の事故事例（2026-04-17 に `RUN_DRAFT_ONLY=0` で4記事誤公開）を踏まえ、本便は明示承認ルート + rollback 手順を内蔵する

## 本便スコープ

- Cloud Run `yoshilover-fetcher` の env 変更 3 つのみ:
  - `RUN_DRAFT_ONLY=1` → `0`
  - `ENABLE_PUBLISH_FOR_POSTGAME=0` → `1`
  - `ENABLE_PUBLISH_FOR_LINEUP=0` → `1`
- 他の env / コード / Secret Manager / Scheduler には一切触らない
- 特に `AUTO_TWEET_ENABLED=0` は維持（X 投稿は Phase C publish が数日安定してから別便で）

## 現状（依頼書作成時点、Claude Code が確認済み）

- revision: `yoshilover-fetcher-00135-rpc`
- 関連 env:
  - `RUN_DRAFT_ONLY=1`
  - `ENABLE_PUBLISH_FOR_POSTGAME=0`
  - `ENABLE_PUBLISH_FOR_LINEUP=0`
  - 他 `ENABLE_PUBLISH_FOR_*=0`（全カテゴリ待機）
  - `AUTO_TWEET_ENABLED=0`
  - `PUBLISH_REQUIRE_IMAGE=1`
- Scheduler `fact-check-morning-report`: `0 * * * *` Asia/Tokyo ENABLED

## 依頼内容

### Step 0: 現状記録（rollback 用）

```bash
gcloud run services describe yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --format='value(status.latestReadyRevisionName)'

gcloud run services describe yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --format='value(spec.template.spec.containers[0].env)' \
  > /tmp/yoshilover_env_before_phase_c.txt
```

- 期待: revision が `00135-rpc` と一致、env 全量が tmp に保存
- NG: revision が `00135-rpc` と不一致 → **中止してよしひろさんに報告**（想定外の deploy が発生している可能性）

### Step 1: env 変更

```bash
gcloud run services update yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --update-env-vars ENABLE_PUBLISH_FOR_POSTGAME=1,ENABLE_PUBLISH_FOR_LINEUP=1,RUN_DRAFT_ONLY=0
```

- 期待: 新 revision 作成、traffic 100% が新 revision に切替
- NG: deploy 失敗 → **rollback 不要**（env 変更が反映されていない）、エラー内容を response doc に記載して中止

### Step 2: smoke test（既存 script 利用）

```bash
NEW_REVISION=$(gcloud run services describe yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --format='value(status.latestReadyRevisionName)')
echo "new_revision=$NEW_REVISION"
bash scripts/cloud_run_smoke_test.sh "$NEW_REVISION"
```

- 期待（以下 4 点すべて OK）:
  - revision ready
  - traffic 100%
  - `RUN_DRAFT_ONLY=0`
  - `ENABLE_PUBLISH_FOR_POSTGAME=1` / `ENABLE_PUBLISH_FOR_LINEUP=1`
  - `AUTO_TWEET_ENABLED=0` 維持
  - `PUBLISH_REQUIRE_IMAGE=1` 維持
- NG: いずれか不一致 → **Step 5 rollback**

### Step 3: 手動 trigger で初回観察

```bash
gcloud scheduler jobs run fact-check-morning-report \
  --location asia-northeast1 --project baseballsite
```

直後のログ確認:

```bash
sleep 30
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND (jsonPayload.event="fact_check_email_sent" OR jsonPayload.event="fact_check_email_skipped" OR jsonPayload.event="rss_fetcher_flow_summary" OR severity>="ERROR")' \
  --project baseballsite --freshness=5m --limit 20 --format=json \
  | jq '.[].jsonPayload | {event, reason, red, yellow, green, checked_posts, subtype_counts}'
```

- 期待: `ERROR` 0件、fact_check 系が正常に動く
- NG: ERROR 出現 or 想定外挙動 → **Step 5 rollback**

### Step 4: publish 解放後の初回 `/run` 観察（任意）

次の自然発火 or 手動 `/run` で、`postgame` / `lineup` 記事が publish されるかを観察:

```bash
# 直近 15 分の rss_fetcher_run_summary をチェック
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND jsonPayload.event="rss_fetcher_run_summary"' \
  --project baseballsite --freshness=15m --limit 5 --format=json \
  | jq '.[].jsonPayload | {event, drafts_created, publishes_created, created_subtype_counts, draft_only, error_count}'
```

- 期待: `draft_only=false`、`publishes_created` が 0 以上、`created_subtype_counts` が postgame/lineup を含み得る
- 解放直後に publish 対象の記事がなければ `publishes_created=0` のこともある（記事生成されるタイミング次第）→ その場合は「次回発火まで待ち」として response doc に明記
- NG: publish 対象以外のカテゴリが publish された → **Step 5 rollback**

### Step 5: rollback 手順（異常時のみ）

```bash
gcloud run services update yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --update-env-vars ENABLE_PUBLISH_FOR_POSTGAME=0,ENABLE_PUBLISH_FOR_LINEUP=0,RUN_DRAFT_ONLY=1
```

- 期待: `RUN_DRAFT_ONLY=1` 復旧、publish 停止
- smoke test を再実行して確認
- response doc に rollback 実施と原因推定を記載

### Step 6: response doc 作成

`docs/handoff/codex_responses/2026-04-18_18.md` に以下を記録:

- Step 0: 記録した revision / env
- Step 1: env 変更のコマンド出力、新 revision 名
- Step 2: smoke test 結果
- Step 3: 手動 trigger 後のログ抜粋（ERROR 有無、fact_check 結果）
- Step 4: `rss_fetcher_run_summary` 抜粋
- Step 5: rollback を実施したか否か
- Cloud Run image digest、成功 / 失敗判定

## 期待される出力

**成功パターン**（Step 1〜4 完走）:

```text
new_revision=yoshilover-fetcher-00136-xxx
OK: Cloud Run revision is serving, RUN_DRAFT_ONLY=0, ENABLE_PUBLISH_FOR_POSTGAME=1, ENABLE_PUBLISH_FOR_LINEUP=1, AUTO_TWEET_ENABLED=0, PUBLISH_REQUIRE_IMAGE=1.
```

**失敗 + rollback 成功**:

```text
rolled_back=true
revision_after_rollback=yoshilover-fetcher-00137-yyy
cause=<短い原因>
```

## 成功判定

- Step 0〜4 がすべて NG なく完走
- 新 revision で `RUN_DRAFT_ONLY=0` / `ENABLE_PUBLISH_FOR_POSTGAME=1` / `_LINEUP=1` が確認できる
- `AUTO_TWEET_ENABLED=0` / `PUBLISH_REQUIRE_IMAGE=1` が維持されている
- Step 3 の手動 trigger で ERROR が出ない

## 失敗時

- Step 1〜2 で失敗 → Step 5 rollback、原因を response doc に記載、commit/push して中止
- Step 3〜4 で異常 → Step 5 rollback、ログ抜粋を response doc に記載、commit/push して中止
- 何があっても「中断して報告」の一貫姿勢。よしひろさんに判断を戻す

## 禁止

- `AUTO_TWEET_ENABLED` / `ENABLE_X_POST_FOR_*` への変更（X 投稿は別便）
- `ENABLE_PUBLISH_FOR_*` の `postgame` / `lineup` 以外の解放
- `PUBLISH_REQUIRE_IMAGE` の変更
- Secret Manager / Scheduler の変更
- コード変更 / WP 側の変更 / Xserver 側の操作
- env 変更を `--set-env-vars` で実施する（全量上書きになるので必ず `--update-env-vars`）

## 次の動き（よしひろさん側）

- 成功: 次の自然発火で publish が実際に起きるかを観察（fact_check メール本文の 24h 運用サマリで可視化される）。24〜48 時間問題なければ manager/notice の解放を検討
- 失敗 + rollback: 原因ログを Claude Code に渡す → 次便設計

## 所要時間

- 想定 10〜15 分（env 変更 1 分 + smoke test 2 分 + 手動 trigger + ログ確認 + response doc）

## 成果物

- 新 Cloud Run revision（`yoshilover-fetcher-00136-???`）
- `docs/handoff/codex_responses/2026-04-18_18.md`:
  - 旧 revision / 旧 env 記録
  - env 変更コマンド出力
  - smoke test 結果
  - 手動 trigger 後の観察ログ
  - rollback 実施有無
  - 最終 revision と image digest

## 参考

- 旧事故事例（CLAUDE.md）: 2026-04-17 に `RUN_DRAFT_ONLY=0` で4記事誤公開 → 本便は `ENABLE_PUBLISH_FOR_POSTGAME/LINEUP=1` 併設で解放対象を限定、かつ rollback 手順内蔵
- 現状 revision: `yoshilover-fetcher-00135-rpc`
- Scheduler: `fact-check-morning-report` `0 * * * *` Asia/Tokyo
