# Codex 依頼書 — 2026-04-18（第19便・T-019 post_id=62584 アイキャッチ欠損 調査）

**依頼者**: よしひろさん（Claude Code 経由）
**前便**: `codex_responses/2026-04-18_18.md`（Phase C postgame/lineup 解放、予定）
**本便の目的**: post_id=62584 の featured_media が空である原因を切り分け、systemic かどうかを判定

## 背景

- よしひろさんが WP で 62584 を確認、アイキャッチなし
- 62584 は現在 draft（非公開）、`PUBLISH_REQUIRE_IMAGE=1` は publish 時のみ効くので draft は画像なしでも作られる
- 今日のコード修正群は画像処理に一切触れていないのでデグレ可能性は低い
  - T-007: `acceptance_fact_check.py` の venue/score 抽出
  - T-010: `draft_audit.py` の `extract_source_links()` 拡張
  - T-012: `acceptance_fact_check.py` の team 抽出 + historical game ref
  - T-016: `fact_check_notifier.py` のメール配送 metadata
  - T-018: `fact_check_notifier.py` の hourly 化 + skip + サマリ
  - T-001: `yoshilover-exclude-cat.php` の REST ガード
- 真の原因候補: 生成時の画像取得失敗 / ソース側に画像なし / attach 失敗 など

## 本便スコープ

- **調査のみ**。コード修正・deploy・env 変更・WP 側操作は一切しない
- 原因特定 → 改善方針提案 → 修正は次便以降で別途判断

## 依頼内容

### Step 0: 62584 の現状確認

```bash
# 認証: .env の WP_USER / WP_APP_PASSWORD を python-dotenv 経由で読む（shell source は禁止、PW に特殊文字が混じる可能性）
python3 -c "
from dotenv import load_dotenv; load_dotenv()
import os, base64, json, urllib.request
user = os.environ['WP_USER']; pw = os.environ['WP_APP_PASSWORD']
url = os.environ['WP_URL'].rstrip('/') + '/wp-json/wp/v2/posts/62584?context=edit'
auth = base64.b64encode(f'{user}:{pw}'.encode()).decode()
req = urllib.request.Request(url, headers={'Authorization': f'Basic {auth}'})
with urllib.request.urlopen(req) as r: d = json.load(r)
keep = ['id','status','date','date_gmt','modified','type','slug','link','featured_media','categories','tags','meta']
out = {k: d.get(k) for k in keep}
out['title'] = d.get('title',{}).get('rendered','')
print(json.dumps(out, ensure_ascii=False, indent=2))
"
```

- 期待: `featured_media=0`、status / date / categories / subtype メタが判明
- NG: python-dotenv 不在 → `python3 -m pip install python-dotenv --user` で導入 or stdlib で自作 parse（shell `source` は禁止）

### Step 1: 生成時の Cloud Run ログ抽出

```bash
# 62584 に言及するログを幅広に（48h 窓）
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND (textPayload=~"62584" OR jsonPayload.post_id=62584 OR jsonPayload.created_post_id=62584)' \
  --project baseballsite --freshness=48h --limit 50 --order=asc --format=json \
  | jq '.[] | {timestamp, severity, event: .jsonPayload.event, payload: .jsonPayload}'
```

- 期待: 62584 の article_created / rss_fetcher_flow_summary / wp_create_post / set_featured_image 等の足跡
- NG: 48h 窓で 0 件 → 窓を 7d に拡大し再試行

### Step 2: 画像取得関連の warn/error 抽出

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND (severity>="WARNING") AND (textPayload=~"(image|featured|attach|thumbnail)" OR jsonPayload.event=~"(image|featured|attach|thumbnail)")' \
  --project baseballsite --freshness=48h --limit 50 --order=asc --format=json \
  | jq '.[] | {timestamp, severity, event: .jsonPayload.event, message: .textPayload}'
```

- 期待: 62584 生成時刻の前後で image/featured 系の warn/error があるか
- NG: 0 件なら Step 3 で再判定

### Step 3: 同時期の他記事サンプリング（systemic 判定）

```bash
# 62584 の date_gmt 前後 6h 窓で、同じ subtype の draft を 10 件サンプリング
python3 -c "
from dotenv import load_dotenv; load_dotenv()
import os, base64, json, urllib.request
user = os.environ['WP_USER']; pw = os.environ['WP_APP_PASSWORD']
base = os.environ['WP_URL'].rstrip('/')
# 62584 の周辺 draft を取得（T-001 で draft filter 壊れてるので single GET でループ or list API で workaround）
# ここでは rss_fetcher ログから created_post_id を 20件拾うのが確実
" 
# もしくは:
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND jsonPayload.event="rss_fetcher_flow_summary"' \
  --project baseballsite --freshness=48h --limit 20 --order=desc --format=json \
  | jq '.[] | .jsonPayload | {timestamp: .timestamp, drafts_created: .drafts_created, created_subtype_counts: .created_subtype_counts, created_post_ids: .created_post_ids}'
```

サンプリングした近隣 draft 10件程度について、Step 0 と同じ WP REST `GET` で `featured_media` を取得し、**画像あり/なしの比率** を算出:

- 期待: 画像あり率が高い（例: 8/10）なら **62584 は単発（systemic ではない）**
- 期待: 画像なし率が高い（例: 7/10）なら **systemic（image パイプラインの故障）**
- 判定を response doc に明記

### Step 4: 画像処理ロジックの位置特定

```bash
# 画像設定に関するコードを把握（修正しない、読み取りのみ）
git grep -n "featured_media\|set_featured\|thumbnail_id\|og:image\|attach" src/ | head -50
```

- 期待: 画像設定ロジックのエントリポイントを特定
- response doc に「画像設定のエントリ関数」「失敗時の挙動」を記載

### Step 5: 過去 commit で画像処理に触った履歴

```bash
git log --all --oneline --since="14 days ago" -- src/ \
  | xargs -I{} sh -c 'git show --stat {} | head -20' 2>/dev/null \
  | grep -iE "(image|featured|thumbnail|og_image|og:image|attach)" \
  | head -30
```

- 期待: 直近 14日で画像処理に touch した commit の有無
- もしあれば、その commit で画像パイプラインが変わった可能性がある旨を response doc に記載

### Step 6: 結論と改善方針提案

response doc に以下を記載:

1. **事実サマリ**: 62584 の現状（status, date, featured_media, subtype, source_url など）
2. **systemic 判定**: 近隣 draft のサンプリング結果（画像あり/なし比率）
3. **原因仮説**: Step 1〜5 から絞り込んだ最有力仮説（1〜2個）
4. **改善方針**（修正は本便ではやらない、次便判断用）:
   - (a) OGP 画像取得失敗時の再試行 / fallback
   - (b) ソースに画像がないケースの統一プレースホルダ
   - (c) attach エラーをログ化して運用可視化
   - (d) 他
5. **次便への引き継ぎ**: 修正が必要なら optional 改善の優先度、不要なら WONTFIX 判断材料

### Step 7: response doc 作成 + commit/push

- `docs/handoff/codex_responses/2026-04-18_19.md` を作成
- commit & push（コード変更なしのドキュメントのみ）
- 認証: `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover"`

## 期待される出力

**成功パターン**:

```text
62584 featured_media=0
近隣 draft N 件中、画像あり M 件 / 画像なし K 件
最有力仮説: <1 行サマリ>
改善方針: (a) / (b) / (c) / WONTFIX のいずれか推奨
```

## 成功判定

- Step 0〜6 をすべて実施し、response doc に事実と仮説が記載されている
- systemic/単発の判定が明示されている
- 改善方針が 1 つ以上提案されている（修正実装はしない）

## 失敗時

- WP REST が 401/403 で取れない → `.env` を python-dotenv で読む、shell source は禁止、認証不可なら Cloud Run ログだけで判定して報告
- Cloud Run ログに 62584 が見当たらない → 窓を 7 日に拡大、それでも 0 件なら「生成時ログ未検出」として報告
- 途中で中止: 中断理由を response doc に記載、commit/push して報告

## 禁止

- コード修正（本便は調査のみ）
- deploy / env 変更
- WP 側の記事修正
- `.env` を shell で `source` する（PW に特殊文字があるので python 経由で読む）
- Secret Manager / Scheduler / Xserver 操作
- T-019 以外のスコープ拡大

## 次の動き（よしひろさん側）

- 成功: response doc を Claude Code が読み、systemic なら修正の第20便起票、単発なら WONTFIX で T-019 RESOLVED 判断
- 失敗: 中断理由に応じて再設計

## 所要時間

- 想定 15〜25 分（WP REST 1回 + Cloud Run ログ 3クエリ + サンプリング + コード読み + response doc）

## 成果物

- `docs/handoff/codex_responses/2026-04-18_19.md`:
  - 62584 事実サマリ
  - 近隣 draft サンプリング表
  - 画像処理ロジックの入口特定
  - 原因仮説（最有力 1〜2）
  - systemic/単発の判定
  - 改善方針提案（修正は次便）

## 参考

- T-019 チケット: `docs/handoff/tickets/OPEN.md`
- 最近のコード修正群は画像処理に触れていない（デグレ可能性低）
- `PUBLISH_REQUIRE_IMAGE=1` は publish 時のみ効く（draft は画像なしで作れる仕様）
- ローカル `.env` 取り扱い: shell `source` は PW に特殊文字が混じっていると shell エラー + PW 一部漏洩リスクあり。**必ず python-dotenv か stdlib parse**
