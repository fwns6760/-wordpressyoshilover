# Codex 依頼書 — 2026-04-18（第24便・CLAUDE.md の git 役割移管を commit/push）

**依頼者**: よしひろさん（Claude Code 経由）
**前便**: `codex_responses/2026-04-18_23.md`（T-022 scheduler gap 修正、`giants-weekend-eve = 0,30 18-22 * * 0,6`）
**本便の目的**: **CLAUDE.md に反映した「git 操作は Codex 管轄」のルール変更を commit/push する。** それだけ。

## 背景

- 2026-04-18 夜、よしひろさんから「Git も Codex の役割」と明示指示
- Claude Code が CLAUDE.md を編集済み（`git status` で `M CLAUDE.md`）
- ルール移管後は Claude Code 自身では commit/push しないため、**初回から Codex に委譲**

## 変更内容（Claude Code が編集済み）

`CLAUDE.md` に以下を反映:

1. **「開発」の定義**に `git add` / `git commit` / `git push` を追加（`docs/` 対象でも Codex 管轄）
2. **判断基準の例**に git 操作の OK/NG を列挙（log/diff/show は閲覧 OK、add/commit/push は開発 NG）
3. **Claude Code がやらないこと**に「リポジトリへの反映操作全般」を追加
4. **Claude Code の作業範囲**から `git add/commit/push` を削除、Codex 管轄と明記
5. **セッション終了前の手順** Step 4 を「Codex 依頼書で依頼」に変更

## 本便スコープ

- `CLAUDE.md` の diff 確認
- `git add CLAUDE.md`
- commit（下記 message 厳守）
- push（`GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover"`）

## 禁止

- `CLAUDE.md` の追加編集（Claude Code の文面をそのまま commit）
- `.codex` / `data/` など未追跡ディレクトリの staging（`git add -A` / `git add .` 禁止）
- `src/` / `tests/` / scheduler / env / Secret 変更
- `--no-verify` など hook スキップ
- force push / amend

---

## Step 0: diff 確認

### コマンド例

```bash
cd /home/fwns6/code/wordpressyoshilover
git status --short
git diff CLAUDE.md
```

### 期待出力

- `git status --short` に `M CLAUDE.md` が出る
- `git diff` に上記「変更内容」1〜5 の差分が出る

### 成功判定

- 変更が CLAUDE.md のみ
- `.codex` / `data/` は untracked のまま（本便では触らない）

### 失敗時

- `CLAUDE.md` 以外に modified が出ていたら **中止** し response doc に記録
- diff が期待と違っていたら **中止** し Claude Code に差し戻し

### 所要時間

- 2 分

---

## Step 1: staging（CLAUDE.md のみ）

### コマンド例

```bash
git add CLAUDE.md
git status --short
```

### 期待出力

- `M  CLAUDE.md`（staged）のみ
- `.codex` / `data/` は `??` のまま

### 成功判定

- staged は CLAUDE.md 1 ファイルだけ

### 失敗時

- 他ファイルが staged された場合 `git restore --staged <file>` で解除

### 所要時間

- 1 分

---

## Step 2: commit

### コマンド例

```bash
git commit -m "docs(claude-md): git 操作を Codex 管轄に移管"
```

### 期待出力

- commit hash が 1 つ増える
- `1 file changed` の sub line

### 成功判定

- `git log --oneline -1` で今回の commit が top

### 失敗時

- pre-commit hook 失敗 → hook の原因を修正して再 commit（`--no-verify` 禁止）
- いかなる場合も `--amend` しない。新規 commit で対応

### 所要時間

- 2 分

---

## Step 3: push

### コマンド例

```bash
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover" git push origin master
```

### 期待出力

- `To github.com:...` とともに push 成功

### 成功判定

- `git status` が `Your branch is up to date with 'origin/master'`

### 失敗時

- `rejected (non-fast-forward)` → `git pull --rebase origin master` → conflict 無ければ再 push、conflict 出たら中止して response doc に記録
- 認証失敗 → SSH key path 確認、Claude Code に差し戻し

### 所要時間

- 2 分

---

## Step 4: response doc 作成

### 作成先

`docs/handoff/codex_responses/2026-04-18_24.md`

### 記載事項

- Step 0 diff 抜粋（1〜5 の各項目が入っていること）
- commit hash
- push の成功ログ 1 行
- `.codex` / `data/` は未追跡のまま手を付けていないこと
- 異常なし / あり の一言結論

### 所要時間

- 3 分

---

## Step 5: response doc 自体も commit/push

### コマンド例

```bash
git add docs/handoff/codex_responses/2026-04-18_24.md
git commit -m "docs(handoff): add codex response 24"
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover" git push origin master
```

### 期待出力

- 2 つ目の commit が origin/master に反映

### 成功判定

- `git log --oneline -2` で今回 2 commit が並ぶ

### 失敗時

- push rejected → `git pull --rebase` → 再 push

### 所要時間

- 2 分

---

## 便全体の所要時間見積

- 合計 12 分（diff 2 + add 1 + commit 2 + push 2 + response 3 + 自 commit 2）

## 便全体の成功判定

- origin/master が 2 commit 先行
  1. `docs(claude-md): git 操作を Codex 管轄に移管`
  2. `docs(handoff): add codex response 24`
- CLAUDE.md が GitHub 上で更新されている
- `.codex` / `data/` は未追跡のまま（本便で手を付けていない）

## よしひろさん / Claude Code 側の次の動き

- **成功時**:
  - Claude Code が response doc 監査して `session_logs/` に追記
  - 以降、Claude Code は `docs/` 編集のみ行い、commit/push は常に Codex 依頼書で渡す
- **失敗時**:
  - push rejected なら第25便で pull --rebase → 再 push を依頼
  - CLAUDE.md 差分が想定と違えば Claude Code が再編集 → 再依頼
