# Codex 依頼書 — 2026-04-18（第26便・残差分の一括 commit）

## 目的

ローカル未コミットの docs 変更を一括 commit / push。

## 対象ファイル

- `CLAUDE.md`（modified）
- `docs/handoff/codex_requests/2026-04-18_24.md`（untracked）
- `docs/handoff/codex_requests/2026-04-18_25.md`（untracked）
- `docs/handoff/codex_requests/2026-04-18_26.md`（本ファイル）
- `docs/handoff/tickets/OPEN.md`（modified）
- `docs/handoff/tickets/RESOLVED.md`（modified）

## やること

- 上記 6 ファイルをファイル名指定で `git add`
- commit message: `docs(handoff): close T-022/T-023, add codex request 24/25/26 and CLAUDE.md rules`
- push: `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover" git push origin master`
- その後 `docs/handoff/codex_responses/2026-04-18_26.md` を作成し、Step 0 の `git status` 相当、commit hash、push 結果、残 untracked を事実のみ記載
- response も `git add` / commit (`docs(handoff): add codex response 26`) / push

## NG

- `.codex` / `data/` の staging（`git add -A` / `git add .` 禁止）
- CLAUDE.md / OPEN.md / RESOLVED.md の内容変更（Claude Code の編集をそのまま commit）
- `src/` / scheduler / env / Secret 変更
- `--no-verify` / `--amend`
- push rejected 時に force push（`git pull --rebase origin master` → 再 push）
