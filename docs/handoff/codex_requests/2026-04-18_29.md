# Codex 依頼書 — 2026-04-18（第29便・cleanup commit + 次改善の前提調査）

## 目的

1. ローカル残差分（request 28.md / 29.md / OPEN.md）の commit
2. 次の薄さ改善候補（統計・直近試合・対戦成績の本文注入）の実装前提として、既存コードで使っている外部データ源 / 内部蓄積を grep ベースで列挙する

## 対象

- `docs/handoff/codex_requests/2026-04-18_28.md`（untracked）
- `docs/handoff/codex_requests/2026-04-18_29.md`（本ファイル、untracked）
- `docs/handoff/tickets/OPEN.md`（modified、T-021 を deploy 済状態に更新）
- `src/` 全体（grep 対象）

## やること（cleanup 部）

- 上記 3 ファイルをファイル名指定で `git add` → commit → push
- commit message: `docs(handoff): bundle request 28/29 and T-021 deploy status`
- push: `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover" git push origin master`

## やること（調査部・数値と事実のみ）

`src/` を grep し、以下を **単純列挙**（判断・提案は書かない）:

1. **選手成績データ関連** の function / 変数 / import
   - keyword: `batting_average`, `era`, `stats`, `season_stats`, `player_stats`, `打率`, `防御率`, `打点`, `成績`
2. **直近試合 / 試合結果 / schedule 関連**
   - keyword: `recent_games`, `last_games`, `schedule`, `game_log`, `game_result`, `最近`, `直近`
3. **対戦成績 / head-to-head 関連**
   - keyword: `matchup`, `head_to_head`, `vs_pitcher`, `対戦成績`, `相性`
4. **外部 HTTP 通信先 URL 一覧**
   - `httpx.get(` / `requests.get(` / `fetch(` / `f"https://...` / `f'https://...'` を grep し、URL だけ列挙
5. **内部蓄積（過去記事や history）を参照している経路**
   - `wp/v2/posts` 呼び出し箇所（第27便の related posts 実装経路も含む）
   - `history` / `past_posts` / `archived` 系
6. **既存 prompt に渡す input のうち "数値・時系列" 系**
   - `_build_*_strict_prompt` の引数で、source facts 以外に数値 / 前後試合 / 成績が渡されているか

各項目、**ファイル名 + 行番号 + 該当 identifier** を最大 10 件まで列挙（10 件超は「他 N 件」と件数だけ）。

## 成果物

`docs/handoff/codex_responses/2026-04-18_29.md` に:
- cleanup commit hash / push 結果 / 残 untracked
- 調査 1〜6 の列挙（事実のみ）

その後 response doc を同じく `git add` → commit（`docs(handoff): add codex response 29`） → push。

## NG

- `.codex` / `data/` の staging（`git add -A` / `git add .` 禁止）
- 調査での解釈・推奨・修正案・次便候補（Claude Code の判断範囲）
- `src/` のコード変更（本便は commit + grep のみ）
- env / Secret / scheduler / deploy 変更
- `--no-verify` / `--amend`
