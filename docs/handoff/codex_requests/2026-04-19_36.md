# Codex 依頼書 — 2026-04-19（第36便・Phase 1 前提調査）

**依頼者**: よしひろさん（Claude Code 経由）  
**種別**: 調査便  
**前提台帳**: `docs/handoff/master_backlog.md`（2026-04-19 TODO 管理版、Claude 側更新）  
**本便の目的**: MB-002/003/004/005/006/007/018/019 の着手前提を、コード・GCP・docs・git working tree から 1 便で確定する。**事実確認のみ**。修正案・実装・deploy・env 変更・scheduler 変更はしない。

## 背景

- `docs/handoff/master_backlog.md` を TODO 管理版へ整理し、Phase 1 の release 到達条件を MB 番号で管理し始めた
- 次の実装依頼書を起票する前に、publish 解放 / prompt 改善 / X 自動投稿 / canary 整理の前提事実を固めたい
- 本便では **Codex に判断を求めない**。Claude / user が後続便を切れるよう、根拠付きの事実だけを集める

## 対象

- repo: `wordpressyoshilover`（`/home/fwns6/code/wordpressyoshilover`）
- 主な確認対象:
  - `src/rss_fetcher.py`
  - `src/server.py`
  - `src/wp_client.py`
  - `src/x_post_generator.py`
  - `src/x_api_client.py`
  - `tests/`（prompt / X 投稿 / publish 系の既存 test）
  - `docs/handoff/tickets/OPEN.md`
  - `docs/handoff/session_logs/2026-04-18_status_sync.md`
  - `docs/handoff/codex_responses/2026-04-18_21.md`
  - `docs/handoff/codex_responses/2026-04-18_22.md`
  - `docs/handoff/codex_responses/2026-04-18_31.md`
  - `docs/handoff/codex_responses/2026-04-18_32.md`
  - `docs/handoff/codex_responses/2026-04-18_35.md`
- GCP:
  - Cloud Run service `yoshilover-fetcher`（project `baseballsite` / region `asia-northeast1`）
  - Cloud Scheduler jobs
  - Cloud Logging（必要最小限）
- git:
  - `git diff -- docs/handoff/tickets/OPEN.md`
  - `git log` / `git blame` / `git grep` 相当

## やること（Phase 1 実装前提）

### 1. MB-002 / MB-003 / MB-004: publish 解放の実体確認

確認したいこと:

1. `manager` / `pregame` / `farm` を publish 解放する際、**flag を 1 にする以外の gate** が存在するか
2. 各 subtype について、現在の生成 path が既に通っているか
   - route / dispatcher
   - prompt builder
   - min_chars 判定
   - category 判定
   - eyecatch 取得 path
   - publish 可否判定 path
3. 過去 draft で当該 subtype に `thin_body` / `no_opinion` がどの程度出ているか

出してほしい形:

- subtype ごとの表:
  - subtype
  - 主 route 関数
  - prompt builder 関数
  - min_chars 関数 / 閾値
  - category / article_subtype の確定箇所
  - eyecatch 取得 / 必須化の箇所
  - publish を止めうる flag / 条件
  - 根拠ファイル + 行番号
- audit 実績:
  - 可能なら直近 7 日または直近 20 件以内で `manager` / `pregame` / `farm` 該当 draft を拾い、`thin_body` / `no_opinion` の件数だけ列挙
  - 該当 sample 0 件なら 0 件と明記

### 2. MB-005: chain of reasoning 投入位置の事実確認

確認したいこと:

1. `manager` / `pregame` / `farm` / `postgame` / `lineup` が、実際にどの strict prompt builder を通っているか
2. `_build_gemini_strict_prompt` / `_build_game_strict_prompt` / `_build_manager_strict_prompt` のうち、どこに既存の「事実」「背景」「次の注目」系 instruction が置かれているか
3. 既存 prompt 文字列を触ると影響を受けそうな test がどこか

出してほしい形:

- prompt builder 対応表:
  - subtype
  - builder 名
  - 呼び出し元
  - 既存 instruction 見出し順
  - 「事実→解釈→感想」を差し込めそうな位置の候補箇所
  - 根拠ファイル + 行番号
- test 影響面:
  - prompt 文字列 / 見出し / 生成構造に依存していそうな test file 一覧
  - 各 test の依存対象（builder / helper / route）
  - 既存 test 総数が `397+` から増減していれば実数

注意:

- **修正案は書かない**。どこに文字列があるか、どの test が当たりそうかの列挙に留める

### 3. MB-006 / MB-007: X 自動投稿 ON の全 gate 確認

確認したいこと:

1. `AUTO_TWEET_ENABLED=1` + `ENABLE_X_POST_FOR_POSTGAME=1` + `ENABLE_X_POST_FOR_LINEUP=1` で足りるか
2. `AUTO_TWEET_CATEGORIES` / `X_POST_AI_CATEGORIES` / publish 条件 / image 条件 / article status など、他に通過条件があるか
3. 炎上時の即時停止に使える kill switch が既に存在するか

出してほしい形:

- X 投稿までの gate 一覧:
  - flag / allowlist 名
  - 既定値
  - 判定関数
  - どの時点で効くか（draft 作成前 / publish 前 / X 送信前）
  - postgame / lineup への影響
  - 根拠ファイル + 行番号
- kill switch 一覧:
  - 既存手段だけ列挙
  - env 変更 / scheduler pause / Cloud Run revision rollback 等、**現に存在する停止手段**とコマンドだけを書く
  - 「最善策」や推奨は不要

### 4. MB-019: T-021 第2弾 canary promote 判断材料の整理

確認したいこと:

1. `ARTICLE_INJECT_TEAM_STATS` canary の観察期間・観察指標として、既に docs に何が書かれているか
2. 現在の canary revision / tag / prod revision の状態
3. promote する場合 / kill する場合の既存 command がどれか

出してほしい形:

- 事実一覧:
  - `OPEN.md` / `codex_responses/2026-04-18_32.md` / `2026-04-18_35.md` の該当箇所抜粋
  - 現行 revision:
    - `00143-luj`
    - `00141-cen`
    - tag 名
    - traffic
- command 一覧:
  - promote に使う command
  - rollback / kill に使う command
  - command の出典（response doc か、現行 `gcloud` describe / help か）

## やること（低優先だが同便で回収）

### 5. MB-018 周辺: canary 残置の現状整理

確認したいこと:

1. prod revision `00143-luj` に `audit-notify-canary` tag が残っている現状で、Cloud Run 上どう見えているか
2. `00141-cen`（`canary-team-stats`）が docs 上どう扱われているか
3. `00117-kes`（`stage1`）の用途と、削除基準が docs に存在するか

出してほしい形:

- `gcloud run services describe` と既存 response doc の整合表
- 「残置によって観測できる具体事象」があれば列挙
- docs 上に削除基準が無ければ「明文化なし」と書く

### 6. 現行 flag / scheduler 状態の来歴確認

確認したいこと:

1. `ENABLE_ENHANCED_PROMPTS=1` は、いつ 1 になったと追跡できるか
2. その flag が今どの関数に効いているか
3. `yoshilover-fetcher-job` が `PAUSED` なのは、どこに理由が残っているか

出してほしい形:

- `git log -S ENABLE_ENHANCED_PROMPTS`、既存 response、session log からの時系列
- 現在効いている code path 一覧
- `yoshilover-fetcher-job` について:
  - job 種別
  - schedule / state
  - 既出 docs / response にある理由
  - 理由が見つからなければ「理由の記録なし」

### 7. `OPEN.md` working tree diff の現状

確認したいこと:

1. 第35便完了後の現 working tree に `docs/handoff/tickets/OPEN.md` 差分があるか
2. 差分がある場合、T-024 関連が含まれるか

出してほしい形:

- `git diff -- docs/handoff/tickets/OPEN.md` の結果
- empty なら empty と明記
- non-empty なら hunk ごとの要点だけ列挙
- T-024 関連行がある / ない の事実だけ書く

## 出力ルール

出力先: `docs/handoff/codex_responses/2026-04-19_36.md`

必須ルール:

- **事実のみ**
- 修正案 / 優先順位提案 / 「〜すべき」 / 次便提案は禁止
- 各 Q ごとに以下 3 つを分ける:
  - 結論
  - 根拠
  - 未確定点
- 根拠には必ず以下のいずれかを付ける:
  - file path + line number
  - command
  - Cloud Run / Scheduler / log の生出力抜粋
- 表で出せるものは表にする

## 成功条件

1. Q1〜Q7 の全項目について、回答または「未確認」の理由がある
2. `manager` / `pregame` / `farm` の publish gate が表で見える
3. `postgame` / `lineup` の X 投稿 gate が表で見える
4. `chain of reasoning` を入れる候補 prompt 箇所が builder 別に列挙されている
5. `00143-luj` / `00141-cen` / `00117-kes` の現状が 1 か所で確認できる
6. `OPEN.md` diff の有無が確定している
7. コード修正 / deploy / env 変更 / scheduler 変更が 0 件

## commit / push

- 本便が着手時点で未コミットなら、対象は次の 1 ファイルだけ:
  - `docs/handoff/codex_requests/2026-04-19_36.md`
- request doc の commit message:
  - `docs(handoff): add codex request 36 (Phase 1 gate fact-finding)`
- response doc の commit message:
  - `docs(handoff): add codex response 36 (Phase 1 gate fact-finding)`
- push:
  - `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_yoshilover" git push origin master`

## NG

- `.codex` / `data/` / `docs/handoff/master_backlog.md` の staging（`git add -A` / `git add .` 禁止）
- コード修正
- test 修正
- deploy
- env / Secret / scheduler / traffic 変更
- `OPEN.md` / `RESOLVED.md` の編集
- 調査結果からの判断代行
- `--no-verify` / `--amend`
