# 便 80 — audit-notify-6x PAUSED 経緯特定 (read-only)

**起票**: Claude Code, 2026-04-20 22:35 JST
**種別**: read-only 調査便
**実行導線**: Claude が `codex exec --full-auto` で直接起動(user relay なし)
**対象**: Cloud Audit Logs (project=baseballsite)
**write 権限**: なし(gcloud は read のみ、filesystem は response md 書き込みのみ)

## 背景

- audit_notify_completed の最終発火: 2026-04-20T06:00:12Z (JST 15:00:12)
- `gcloud scheduler jobs describe audit-notify-6x` → PAUSED、userUpdateTime 2026-04-20T07:46:17Z (JST 16:46:17)
- fact-check-morning-report は ENABLED、fact_check_email_skipped が 24h 毎時正常
- T-017 / T-024 は RESOLVED 済み、mail path コードに local 差分なし

## やること

1. Cloud Audit Logs から PauseJob 呼び出し取得:

```
filter:
  protoPayload.serviceName="cloudscheduler.googleapis.com"
  AND protoPayload.resourceName:"projects/baseballsite/locations/asia-northeast1/jobs/audit-notify-6x"
  AND (protoPayload.methodName:"PauseJob" OR protoPayload.methodName:"UpdateJob" OR protoPayload.methodName:"ResumeJob" OR protoPayload.methodName:"CreateJob")
freshness: 14d
format: timestamp, protoPayload.methodName, protoPayload.authenticationInfo.principalEmail,
        protoPayload.requestMetadata.callerIp
```

2. time-ordered で Create → Enable → Pause のライフサイクルを並べる

3. principalEmail を判定:
   - human user (fwns6760@gmail.com 等) → 意図的 user 操作
   - service account → どの automation / deploy が pause したか

## やらないこと

- `gcloud scheduler jobs resume/pause` 実行禁止
- audit-notify-6x の設定変更禁止
- 他 scheduler / mail path コード / commit / push / deploy 禁止

## 成功条件

- 誰が (principalEmail) / いつ (JST) / どの API で PAUSED 化したかを特定
- 意図 (意図的 / 事故 / 不明) を 1 行で判定

## 出力

`docs/handoff/codex_responses/2026-04-20_audit_notify_pause.md` に以下:

```
- pause 実行者: <principalEmail>
- pause 時刻: <JST ISO8601>
- 呼び出し API: <methodName>
- 呼び出し元 IP: <callerIp>
- 直前 14d の同 job 操作履歴: (time-ordered list)
- 判定: 意図的 / 事故 / 不明
- 次に Claude が判断すべきこと: <一行>
```

## 注意

- 本便は read-only 調査便(修正便ではない)
- 仕様判断が要る場合は結論を出さず、その旨を出力に明記して Claude に戻す
