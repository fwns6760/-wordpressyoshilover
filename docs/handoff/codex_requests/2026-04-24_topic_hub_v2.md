# 2026-04-24_topic_hub_v2 — topic-hub を巨人オレンジ hero strip に格上げ (CSS-only deploy)

## 目的

- `yoshi-topic-hub` の見た目を巨人オレンジ hero strip に格上げ(black panel + orange 5px + white cards)
- 巨人カラー 3 色 (orange #F5811F / black #1A1A1A / white) のみ使用
- DOM 変更なし、CSS のみ
- 他 block (breaking-strip / article-bundles / sns-reactions / front-card / sidebar-rail) への干渉なし

## 対象ファイル / 対象リソース

- `src/custom.css` (既に Claude Code 側で編集済み、未 commit)
- WP `https://yoshilover.com/wp-admin` SWELL → カスタマイザー → 追加 CSS
- SWELL / WP cache / CDN cache

## やること

- `git diff src/custom.css` で変更内容を確認(topic-hub ブロックと @media 600px / 390px 周辺のみ変わっていることを確認)
- `git add src/custom.css`
- `git commit -m "063-v7.1(css-only): topic-hub を巨人オレンジ hero strip に格上げ (orange/black/white 3 色のみ)"`
- `git push origin master`
- WP admin → SWELL → カスタマイザー → 追加 CSS を開く
- 現行の `src/custom.css` **全文** で上書き保存(SWELL 追加 CSS は `src/custom.css` の全文反映前提)
- WP / SWELL / CDN cache clear 実施
- live 確認 curl:
  - `curl -sS https://yoshilover.com/ -o /tmp/yoshi_top_after.html`
  - `curl -sS https://yoshilover.com/62965 -o /tmp/yoshi_post_after.html`
- 以下を確認:
  - トップ `yoshi-topic-hub` の DOM 構造に変化なし(class 名維持)
  - inline `<style>` 内に新ルール `.yoshi-topic-hub { background: var(--black); }` / `.yoshi-topic-hub__heading::after { content: 'THIS WEEK'; }` が出ていること
  - 他 block (`yoshi-breaking-strip` / `yoshi-article-bundles` / `yoshi-sns-reactions`) の CSS ルール差分が **topic-hub 以外にない**こと(diff で topic-hub 領域 + @media 内の topic-hub 関連のみが変わっていることを再確認)
- mobile emulation (Chrome devtools 390×844) で:
  - 見出し「今週の話題 THIS WEEK」が 1 行 or 2 行で読める
  - item が 1col で並ぶ
  - card title が 13px で潰れていない
  - hover translate は OFF (tap で bg だけ変わる)
- desktop (Chrome 1280 幅) で:
  - black panel + orange 5px top が見える
  - 4 items が 1 行(240px min)で並ぶ
  - hover で card が浮く (translateY -2px + orange shadow)

## NG (触らないもの)

- `src/custom.css` の topic-hub 以外の区間(breaking-strip / article-bundles / sns-reactions / front-card / sidebar-rail / l-header / gnav 等)
- `src/yoshilover-063-frontend.php` (DOM 側は今回変更なし)
- 親テーマ `swell/*` 直編集 (子テーマ `swell_child` 経由が原則)
- validator / route / mail chain / ledger / automation / X API / 記事生成 path
- published post 本体の編集
- scheduler / secret / env var

## commit / push コマンド

```bash
git add src/custom.css
git commit -m "063-v7.1(css-only): topic-hub を巨人オレンジ hero strip に格上げ (orange/black/white 3 色のみ)"
git push origin master
```

## response doc

`docs/handoff/codex_responses/2026-04-24_topic_hub_v2.md` に以下を記載:

- git commit hash
- SWELL 追加 CSS 反映日時
- cache clear 実施の有無
- curl 後の inline `<style>` 内での新ルール検出結果
- desktop / mobile 確認結果
- 他 block への回帰なし確認

## rollback 手順(必要なら)

- `git revert <commit_hash>` で custom.css を戻す
- SWELL 追加 CSS に revert 後の `src/custom.css` 全文を再貼り付け
- cache clear
