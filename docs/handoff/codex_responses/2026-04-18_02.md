# Codex Responses — 2026-04-18（第2便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_02.md`

- 実施内容: `T-007 実装 -> 9件再判定 -> T-002 A/B/C分類`
- 本対応では `deploy / env変更 / Cloud Run反映 / T-001修正 / T-008実行` は未実施

---

## 1. T-007 実装の diff サマリ

修正ファイル:

- [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py)
- [tests/test_acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/tests/test_acceptance_fact_check.py)

実装内容:

1. `_source_reference_facts()` から raw text ベースの `score / venue` 抽出を停止
   - `score` は空固定
   - `venue` は空固定
   - `opponent / time` のみ source origin の補助値として残す
   - field ごとの `field_evidence_urls` を持たせた

2. `_fetch_npb_schedule_snapshot()` を HTML 構造ベースへ変更
   - `dateMMDD` 行を起点に当日ブロックを切り出し
   - `<div class="team1">`, `<div class="team2">`, `<div class="place">`, `<div class="time">`, `<div class="score1">`, `<div class="score2">`
     を構造で読む方式にした
   - Giants 戦の対象 row を選び、`opponent / venue / time / score` を structured に返す

3. `_fetch_game_reference()` を structured fallback 化
   - Yahoo `game_id` が引けなくても NPB row 由来の `score / venue / time / opponent` を返せる
   - `evidence_by_field` を追加して field ごとの根拠 URL を持たせた

4. `_check_game_facts()` の evidence URL を supply origin 別に分岐
   - `reference` 由来なら `reference.evidence_by_field[field]`
   - `source_facts` 由来なら `source_facts.field_evidence_urls[field]`

5. regression test 追加
   - UUID 断片 `4725-97a1` から `25-97` を score 抽出しない
   - NPB 月間日程の target row から `神宮 / 18:00 / 8-2` を取れる
   - evidence URL が `reference` と `source_facts` で正しく切り替わる

---

## 2. 修正後の再判定結果（9件）

詳細レポート:

- [docs/fix_logs/2026-04-18_t007_post_fix_recheck.md](/home/fwns6/code/wordpressyoshilover/docs/fix_logs/2026-04-18_t007_post_fix_recheck.md)

### 結果サマリ

| post_id | before | after | 判定 |
|---|---:|---:|---|
| 62518 | 🔴 | ✅ | 疑陽性解消 |
| 62527 | 🔴 | 🔴 | 真の red 継続 |
| 62540 | ✅ | ✅ | 変化なし |
| 62044 | 🔴 | ✅ | 疑陽性解消 |
| 61981 | 🔴 | 🔴 | venue は解消、opponent red 継続 |
| 61886 | 🔴 | ✅ | 疑陽性解消 |
| 61802 | 🔴 | ✅ | 疑陽性解消 |
| 61770 | 🔴 | 🟡 | venue 解消、source_reference_missing のみ残存 |
| 61598 | 🔴 | 🟡 | venue 解消、source_reference_missing のみ残存 |

### 注記

- `62518` は `score=25-97` / `venue=PayPay` の両方が完全解消し green
- `62527` は parser 起因ではなく、title の `DeNA -> ヤクルト` が真の mismatch のまま残存
- `61981` は venue の疑陽性が消えた一方、`opponent=阪神 -> 楽天` が source 起点で red 継続

---

## 3. T-002 の3分類（A/B/C）

対象: `62044 / 61981 / 61886 / 61802 / 61770 / 61598`

### A. 修正後 green（疑陽性確定）

- `62044`
- `61886`
- `61802`

判断:

- いずれも parser バグ由来の venue red のみが消え、green 化
- T-002 の該当行には `green確認済` を付けてよい

### B. 修正後も red（真の事実誤記候補）

- `61981`

判断:

- venue 偽陽性は解消
- `opponent=阪神 -> 楽天` が source origin (`https://www.nikkansports.com/baseball/news/202604140001354.html`) を根拠に red 継続
- 真の修正対象候補として残すべき

### C. yellow（個別確認必要）

- `61770`
- `61598`

判断:

- parser 起因の venue red は解消
- ただし `source_reference_missing` が残るため、公開記事としては手動確認が必要

---

## 4. 追加メモ

- `62540` は before/after とも green
  - 以前確認した `source_facts.score=19-4` の潜在誤抽出は、今回の修正で経路自体が消えた
- `62527` と `61981` は、T-007 修正後も残るため parser 由来ではない
- T-008（62527 の title 自動修正）は、今回の結果を踏まえると実行候補として妥当

---

## テスト

- `python3 -m unittest tests.test_acceptance_fact_check`
- `python3 -m unittest discover -s tests`
- 結果: `366 passed`
