# Codex Responses — 2026-04-18（第6便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_06.md`

## 実行結果

- **summary 欄の `相手=DeNA` は `ヤクルト` に修正済み**
- **本文中の `4日DeNA戦〜` は 5 箇所とも未変更を確認**
- **ただし fact_check は red 継続**

依頼書の異常時条件どおり、green にならなかったため **ここで停止** しています。

---

## 実施内容

対象: `post_id=62527`

### 1. 事前確認

live post 取得時点で:

- title: `巨人ヤクルト戦 大城卓三は何を見せたか`（前便で修正済み）
- summary の `相手=DeNA`: **1 箇所**
- `4日DeNA戦`: **5 箇所**

確認ログ:

```text
TITLE 巨人ヤクルト戦 大城卓三は何を見せたか
STATUS draft
SUMMARY_NEEDLE_COUNT 1
HISTORY_COUNT 5
```

### 2. 反映した修正

summary ブロックの opponent 行だけを 1 箇所置換:

```diff
--- before
+++ after
@@
-<div style="font-size:0.95em;color:#111;font-weight:700;line-height:1.5;">DeNA</div>
+<div style="font-size:0.95em;color:#111;font-weight:700;line-height:1.5;">ヤクルト</div>
```

### 3. 変更していない箇所

本文中の `4日DeNA戦と並んで今季最多8得点` はすべて保持しました。

残存箇所:

```text
L4: <p>巨人が首位ヤクルトに快勝し、今季最長の3連勝を収めた。4日DeNA戦と並んで今季最多8得点を挙げた。</p>
L28: <div style="font-size:0.95em;color:#111;font-weight:700;line-height:1.5;">4日DeNA戦と並んで今季最多8得点を挙げた</div>
L33: <li>4日DeNA戦と並んで今季最多8得点を挙げた。</li>
L41: <p>4日DeNA戦と並んで今季最多8得点を挙げた。</p>
L46: <p>4日DeNA戦と並んで今季最多8得点を挙げた。</p>
```

件数確認:

```text
HISTORY_COUNT_AFTER 5
```

summary 側の修正確認:

```text
L23: <div style="font-size:0.84em;color:#666;font-weight:700;">相手</div>
L24: <div style="font-size:0.95em;color:#111;font-weight:700;line-height:1.5;">ヤクルト</div>
```

---

## fact_check 再判定

更新後に:

```bash
python3 -m src.acceptance_fact_check --post-id 62527 --json
```

を実行した結果、`result=red` が継続しました。

```json
{
  "post_id": 62527,
  "title": "巨人ヤクルト戦 大城卓三は何を見せたか",
  "status": "draft",
  "primary_category": "試合速報",
  "article_subtype": "postgame",
  "result": "red",
  "findings": [
    {
      "severity": "red",
      "field": "opponent",
      "current": "DeNA",
      "expected": "ヤクルト",
      "evidence_url": "https://www.nikkansports.com/baseball/news/202604170001523.html",
      "cause": "generation_hallucination",
      "proposal": "本文内の `DeNA` を `ヤクルト` に置換する"
    }
  ]
}
```

---

## 判断

今回の live article では、

- summary の `相手=DeNA` は正しく修正済み
- それでも fact_check は、本文中の **歴史参照** `4日DeNA戦〜` を opponent として拾って red 判定している

したがって、この red は **現在の fact_check ロジック側の判定限界** であり、記事本文の残件とは言い切れません。

## 停止理由

依頼書の成功条件は:

- `result=green`
- `findings=[]`

ですが、そこに到達していないため、依頼書どおり **ここで停止** しました。

---

## 次の提案

1. `62527` の記事自体は、依頼どおりの summary 修正は完了
2. 残る red は `4日DeNA戦〜` を opponent と誤認する fact_check 側のルールに見える
3. 次便ではどちらかの判断が必要:
   - `62527` は **記事修正完了 / fact_check 側 false positive** として T-008 をクローズする
   - もしくは fact_check に「歴史参照の `DeNA戦` は opponent とみなさない」補正を入れてから再判定する

現時点では、**記事側の追加修正は行っていません**。
