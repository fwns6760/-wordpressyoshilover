# Codex Responses — 2026-04-18（第8便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_08.md`

## 実行結果

- **T-013 deploy: 完了**
- **deploy 対象 runtime commit: `d6e19eb`**
- **deploy / smoke test / 手動 trigger / run summary 確認: すべて成功**

---

## deploy 内容

### deploy 元

- request では `master HEAD (d6e19eb)` 指定
- 実際の repo HEAD は `dbe4760` でしたが、差分は handoff docs のみで runtime code 変更なし
- そのため、依頼の意図どおり **`git archive d6e19eb`** を一時ディレクトリへ展開して source deploy しました

### deploy コマンド

```bash
tmp=$(mktemp -d)
git archive d6e19eb | tar -x -C "$tmp"
gcloud run deploy yoshilover-fetcher \
  --source "$tmp" \
  --region asia-northeast1 \
  --project baseballsite
```

### deploy 結果

- new revision: `yoshilover-fetcher-00132-lgv`
- traffic 100%: `yoshilover-fetcher-00132-lgv`
- image digest:
  `asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:f922ab5b43de664e84d1ca25d5acc8bd905a1aa12c1ba5f24b020c315ff72738`

---

## smoke test

実行:

```bash
bash scripts/cloud_run_smoke_test.sh yoshilover-fetcher-00132-lgv
```

結果:

```text
Cloud Run smoke test
service=yoshilover-fetcher
region=asia-northeast1
project=baseballsite
latest_ready_revision=yoshilover-fetcher-00132-lgv
traffic_100_revision=yoshilover-fetcher-00132-lgv
image=asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:f922ab5b43de664e84d1ca25d5acc8bd905a1aa12c1ba5f24b020c315ff72738
RUN_DRAFT_ONLY=1
AUTO_TWEET_ENABLED=0
PUBLISH_REQUIRE_IMAGE=1
OK: Cloud Run revision is serving, RUN_DRAFT_ONLY=1, AUTO_TWEET_ENABLED=0, PUBLISH_REQUIRE_IMAGE=1, and recent logs exist.
```

### env 維持確認

- `RUN_DRAFT_ONLY=1`
- `AUTO_TWEET_ENABLED=0`
- `PUBLISH_REQUIRE_IMAGE=1`
- `ENABLE_PUBLISH_FOR_* = 0` 維持（service describe で確認）

---

## Scheduler 手動 trigger

実行 job:

- `giants-weekend-pre`

実行:

```bash
gcloud scheduler jobs run giants-weekend-pre \
  --project baseballsite \
  --location asia-northeast1
```

確認:

```json
{
  "lastAttemptTime": "2026-04-18T05:42:45.332942Z",
  "status": {}
}
```

---

## `/run` 実行確認

Cloud Logging query:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND resource.labels.revision_name="yoshilover-fetcher-00132-lgv" AND textPayload:"rss_fetcher_run_summary"' \
  --project baseballsite --limit=5 --format=json
```

最新の run summary:

```json
{
  "event": "rss_fetcher_run_summary",
  "dry_run": false,
  "draft_only": true,
  "has_game": true,
  "opponent": "ヤクルト",
  "venue": "神宮",
  "entry_limit": 10,
  "total_entries": 182,
  "drafts_created": 0,
  "skip_duplicate": 34,
  "skip_filter": 89,
  "error_count": 0,
  "x_post_count": 0,
  "x_post_daily_limit": 10,
  "x_ai_generation_count": 10,
  "x_ai_generation_limit": 10
}
```

手動 trigger 直後の run も確認済み:

```json
{
  "event": "rss_fetcher_run_summary",
  "dry_run": false,
  "draft_only": true,
  "has_game": true,
  "opponent": "ヤクルト",
  "venue": "神宮",
  "entry_limit": 10,
  "total_entries": 182,
  "drafts_created": 2,
  "skip_duplicate": 32,
  "skip_filter": 89,
  "error_count": 0,
  "x_post_count": 0,
  "x_post_daily_limit": 10,
  "x_ai_generation_count": 10,
  "x_ai_generation_limit": 10
}
```

---

## ERROR 確認

query:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND resource.labels.revision_name="yoshilover-fetcher-00132-lgv" AND severity>=ERROR' \
  --project baseballsite --limit=20 --format=json
```

結果:

```json
[]
```

---

## 成功判定

- new revision が `00132-*` 以降 → **達成**
- traffic 100% が new revision → **達成**
- env 維持 → **達成**
- `/run` 実行で `draft_only=true`, `error_count=0` → **達成**
- `severity>=ERROR` on new revision: `0 件` → **達成**

---

## チケット提案

- **T-013 → RESOLVED 提案**

---

## 補足

- rollback は不要でした
- deploy / smoke / trigger のみで、env / secret / scheduler schedule / publish gate は変更していません
