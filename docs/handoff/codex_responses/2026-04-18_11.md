# Codex Responses — 2026-04-18（第11便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_11.md`

- 実施内容: `T-015 Cloud Run deploy`
- 本対応では `env変更 / Secret Manager変更 / Scheduler設定変更 / WP記事更新` は未実施

---

## 1. deploy 実施内容

依頼どおり、第8便と同じく **`git archive` でクリーンな tmpdir を作って source deploy** した。

実行コマンド:

```bash
tmp=$(mktemp -d)
git archive a08d875 | tar -x -C "$tmp"
gcloud run deploy yoshilover-fetcher \
  --source "$tmp" \
  --region asia-northeast1 \
  --project baseballsite
```

補足:

- ローカル `HEAD` には docs-only commit が乗っていたため、**runtime に反映すべき commit `a08d875` を明示して archive** した
- これにより、依頼どおり **T-010 (a) 実装だけ** を Cloud Run に反映している

---

## 2. deploy 結果

- new revision: `yoshilover-fetcher-00133-gvf`
- image digest:
  `asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:0915cc1bfbef588ba62c638035cfecd128ddb4c971492418e1e09632480af71d`
- service URL:
  `https://yoshilover-fetcher-487178857517.asia-northeast1.run.app`

Cloud Run deploy 出力:

```text
Service [yoshilover-fetcher] revision [yoshilover-fetcher-00133-gvf] has been deployed and is serving 100 percent of traffic.
```

---

## 3. smoke test 結果

実行:

```bash
bash scripts/cloud_run_smoke_test.sh yoshilover-fetcher-00133-gvf
```

結果:

```text
Cloud Run smoke test
service=yoshilover-fetcher
region=asia-northeast1
project=baseballsite
latest_ready_revision=yoshilover-fetcher-00133-gvf
traffic_100_revision=yoshilover-fetcher-00133-gvf
image=asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:0915cc1bfbef588ba62c638035cfecd128ddb4c971492418e1e09632480af71d
RUN_DRAFT_ONLY=1
AUTO_TWEET_ENABLED=0
PUBLISH_REQUIRE_IMAGE=1
```

判定:

- latest ready revision = new revision → **OK**
- traffic 100% = new revision → **OK**
- `RUN_DRAFT_ONLY=1` → **OK**
- `AUTO_TWEET_ENABLED=0` → **OK**
- `PUBLISH_REQUIRE_IMAGE=1` → **OK**

---

## 4. Scheduler 手動 trigger

実行 job:

```bash
gcloud scheduler jobs run giants-weekend-pre \
  --location asia-northeast1 \
  --project baseballsite
```

結果:

- 実行成功
- Cloud Run request log で `Google-Cloud-Scheduler` の `POST /run` `status=200` を確認

request log 抜粋:

```json
{
  "requestMethod": "POST",
  "requestUrl": "https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/run",
  "status": 200,
  "userAgent": "Google-Cloud-Scheduler"
}
```

---

## 5. `/run` 実行確認

新 revision の `rss_fetcher_run_summary`:

```json
{
  "event": "rss_fetcher_run_summary",
  "dry_run": false,
  "draft_only": true,
  "has_game": true,
  "opponent": "ヤクルト",
  "venue": "神宮",
  "entry_limit": 10,
  "total_entries": 182,
  "drafts_created": 0,
  "skip_duplicate": 34,
  "skip_filter": 89,
  "error_count": 0,
  "x_post_count": 0,
  "x_post_daily_limit": 10,
  "x_ai_generation_count": 10,
  "x_ai_generation_limit": 10
}
```

`run_started` も new revision で確認:

```json
{
  "event": "run_started",
  "run_draft_only": true,
  "auto_tweet_enabled": false,
  "publish_require_image": true,
  "revision": "yoshilover-fetcher-00133-gvf"
}
```

判定:

- `draft_only=true` → **OK**
- `error_count=0` → **OK**

---

## 6. ERROR 確認

実行:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND resource.labels.revision_name="yoshilover-fetcher-00133-gvf" AND severity>=ERROR' \
  --project baseballsite \
  --limit=20 \
  --format=json
```

結果:

```json
[]
```

判定:

- `severity>=ERROR` on new revision = **0件**

---

## 7. 成功判定

依頼書の成功条件:

1. new revision が `00133-*` 以降で ready  
   → **達成** (`00133-gvf`)
2. traffic 100% が new revision  
   → **達成**
3. env が期待値維持  
   → **達成**
4. `/run` 実行で `draft_only=true`, `error_count=0`  
   → **達成**
5. `severity>=ERROR` on new revision = 0  
   → **達成**

---

## 8. 結論

- **T-015 deploy は成功**
- Cloud Run `yoshilover-fetcher` は `00133-gvf` で稼働中
- 次回以降の Scheduler 自然発火から、**T-010 (a) の source coverage 拡張が反映された fact_check / runtime** が使われる

### closing 提案

- **T-015 → RESOLVED 提案**

