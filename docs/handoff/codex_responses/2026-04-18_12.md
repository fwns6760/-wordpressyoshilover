# Codex Responses — 2026-04-18（第12便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_12.md`

- 実施内容: `T-014 subject: needs_manual_review 調査`
- 本対応では `コード修正 / deploy / env変更 / WP記事更新` は未実施

---

## 1. 現在の再現状況

2026-04-18 時点で `python3 -m src.acceptance_fact_check --post-id <id> --json` を再実行した結果:

| post_id | 現在結果 | field | cause | 補足 |
|---|---|---|---|---|
| 62003 | green | - | - | **現在は非再現**。`source_urls=[]` |
| 61779 | yellow | `subject` | `needs_manual_review` | 再現 |

### 62003 rerun

```json
{
  "post_id": 62003,
  "title": "阿部監督「本当に自己犠牲ができる素晴らしい打者」 ベンチの狙いはどこか",
  "status": "publish",
  "primary_category": "首脳陣",
  "article_subtype": "manager",
  "modified": "2026-04-14T22:32:44",
  "result": "green",
  "findings": [],
  "source_urls": []
}
```

### 61779 rerun

```json
{
  "post_id": 61779,
  "title": "6試合連続で3得点以下…貧打にあえぐ巨人打線　大矢明彦氏「我慢の時期かもしれない」",
  "status": "publish",
  "primary_category": "首脳陣",
  "article_subtype": "manager",
  "modified": "2026-04-13T09:14:56",
  "result": "yellow",
  "findings": [
    {
      "severity": "yellow",
      "field": "subject",
      "current": "阿部監督",
      "expected": "source title/text を確認",
      "evidence_url": "https://baseballking.jp/ns/692794/",
      "message": "記事の主語 `阿部監督` を source で直接確認できない",
      "cause": "needs_manual_review"
    }
  ],
  "source_urls": [
    "https://baseballking.jp/ns/692794/"
  ]
}
```

---

## 2. 記事別調査

### p=62003

取得した `post_facts / source_facts`:

```json
{
  "post_id": 62003,
  "article_subtype": "manager",
  "subject": "阿部監督",
  "source_links": [
    {
      "name": "スポニチ野球記者X",
      "url": ""
    }
  ],
  "source_subject_head": "",
  "snapshots": []
}
```

読み取り:

- `acceptance_fact_check._extract_subject()` は `manager` でも `_extract_subject_label()` を使って `subject=阿部監督` を抽出している
  - 参照: [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py:266)
- ただし `extract_source_links()` から得られる source は `name=スポニチ野球記者X` だけで、`url` が空
- そのため `source_facts.snapshots=[]`、`source_text=""`
- `_check_subject_facts()` は `subject and source_text and subject not in source_text` の時だけ yellow を出す
  - 参照: [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py:735)

結論:

- **現在のコード・現在のデータでは yellow は再現しない**
- 分類は **(Z) source coverage 不足**
- 推論: T-014 起票時の `62003 yellow` は、別タイミングの監査結果か、source 抽出状態が一時的に異なっていた可能性が高い

### p=61779

取得した `post_facts / source_facts`:

```json
{
  "post_id": 61779,
  "article_subtype": "manager",
  "subject": "阿部監督",
  "source_links": [
    {
      "name": "https://baseballking.jp/ns/692794/",
      "url": "https://baseballking.jp/ns/692794/"
    }
  ],
  "source_subject_head": "6試合連続で3得点以下…貧打にあえぐ巨人打線 大矢明彦氏「我慢の時期かもしれない」 – BASEBALL KING",
  "snapshots": [
    {
      "url": "https://baseballking.jp/ns/692794/",
      "title": "6試合連続で3得点以下…貧打にあえぐ巨人打線 大矢明彦氏「我慢の時期かもしれない」 – BASEBALL KING"
    }
  ]
}
```

読み取り:

- source title / source text 側の主体は **大矢明彦氏**
- 一方、記事本文には途中で `阿部監督はこの停滞を打破すべく...` が出てくる
- `_extract_subject()` は `_extract_subject_label()` に委譲している
  - 参照: [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py:266)
- `_extract_subject_label()` は `category == "首脳陣"` のとき `staff_role_pattern` を優先し、本文中の `阿部監督` を候補として返し得る
  - 参照: [src/rss_fetcher.py](/home/fwns6/code/wordpressyoshilover/src/rss_fetcher.py:1491)
- その結果 `post_facts.subject="阿部監督"` となり、`_check_subject_facts()` で `source_text` に含まれないため yellow
  - 参照: [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py:735)
- `expected=""` のため `_infer_cause()` はどの専用分岐にも入らず、最後の `needs_manual_review` に落ちる
  - 参照: [src/acceptance_fact_check.py](/home/fwns6/code/wordpressyoshilover/src/acceptance_fact_check.py:570)

結論:

- **再現あり**
- 分類は **(Y) 抽出ミス**
- mismatch の実体は「肩書差」ではなく **主体の取り違え**

---

## 3. 共通性の判定

- **共通原因ではない**
- `62003`: source URL が空で source coverage が足りないため、現在は subject check 自体が発火しない
- `61779`: source は正常だが、subject 抽出が本文中の別主体へ引っ張られている

したがって T-014 は 1 本の systemic bug ではなく、

1. `62003` = data coverage / non-repro
2. `61779` = extraction mismatch

の混在とみなすのが妥当。

---

## 4. 修正案比較

### (m1) subject の正規化

- `阿部監督` vs `阿部慎之助` のような肩書差には効く
- ただし `61779` は **大矢明彦氏 vs 阿部監督** なので解決しない
- 今回の主問題には不十分

### (m2) source_text 側の matching をゆるめる

- 部分一致や肩書吸収で false positive は減る
- ただし `61779` のような **別人物誤抽出** も見逃しやすくなる
- 品質面の後退リスクがある

### (m3) subject チェック対象 subtype を見直す

- `manager` を外せば `61779` は消える
- ただし `61754 / 61897 / 61903 / 61779` のような現行 yellow をまとめて隠すだけになる
- 根治にならない

### (m4) 2件だけなら yellow のまま放置

- 現時点では **62003 は非再現**、実質残件は `61779` のみ
- m1〜m3 のどれも `61779` をきれいに直せない
- 運用上は最も安全

---

## 5. 推奨方針

**推奨: (m4) いったん放置**

理由:

- 再現しているのは `61779` の 1 件だけ
- `62003` は current run で green のため、同じ修正でまとめて直す対象ではない
- m1〜m3 は今回の主因に当たっておらず、誤検知よりも見逃しリスクを増やしやすい

---

## 6. もし次便で実装するなら

実装するなら、m1〜m4 ではなく **`_extract_subject_label()` の狭いルール修正** を別便で切るのがよい。

最小スコープ案:

- `category="首脳陣"` かつ title 側に `○○氏「...」` / `○○監督「...」` がある場合は、本文後半の別主体より **title 側の quoted speaker を優先**
- または `manager` 記事で source title に明示された人名がある場合、subject 候補に source title を優先

このスコープなら `61779` だけを狙って直しやすく、既存の game fact 系ロジックには触れない。

---

## 7. 結論

- `62003`: **current run では green、T-014 は非再現**
- `61779`: **yellow 再現、原因は subject 抽出ミス**
- T-014 は「2件同根の bug」ではなく、**1件非再現 + 1件抽出ミス**
- 今日の判断としては **WONTFIX 寄りで保留** が妥当
