# Codex Responses — 2026-04-18（第14便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_14.md`

- 実施内容: `T-016 fact_check メール不達調査（観測性向上）`
- 本対応では `env変更 / Secret Manager変更 / Scheduler設定変更 / Gmail設定変更 / WP記事更新` は未実施

---

## 1. 実装内容

対象ファイル:

- [src/fact_check_notifier.py](/home/fwns6/code/wordpressyoshilover/src/fact_check_notifier.py)
- [tests/test_fact_check_notifier.py](/home/fwns6/code/wordpressyoshilover/tests/test_fact_check_notifier.py)

### 変更点

`send_email()` に以下を追加した。

1. `Message-ID` を `make_msgid(domain="yoshilover.com")` で明示生成
2. `smtp.send_message(msg)` の戻り値を `refused_recipients` として保持
3. `smtp.noop()` を 1 回打ち、その応答を `smtp_response` として保持

`run_notification()` の `fact_check_email_sent` ログには以下を追加した。

- `message_id`
- `refused_recipients`
- `smtp_response`

### 返り値イメージ

```json
{
  "mode": "smtp",
  "subject": "...",
  "to_email": "fwns6760@gmail.com",
  "from_email": "fwns6760@gmail.com",
  "message_id": "<...@yoshilover.com>",
  "refused_recipients": {},
  "smtp_response": [250, "2.0.0 OK ... - gsmtp"]
}
```

---

## 2. regression test

追加した確認:

1. `send_email()` が SMTP 成功時に `message_id / refused_recipients / smtp_response` を返す
2. `run_notification()` が `fact_check_email_sent` ログに上記 3 項目を載せる

実行:

```bash
python3 -m unittest tests.test_fact_check_notifier
python3 -m unittest discover -s tests
```

結果:

```text
Ran 9 tests in 0.026s
OK

Ran 373 tests in 0.913s
OK
```

---

## 3. commit / push

- commit: `01ed7b2`
- message: `fix: log fact check email delivery metadata`

push 済み:

```text
To github-yoshilover:fwns6760/-wordpressyoshilover.git
   e9128bf..01ed7b2  master -> master
```

---

## 4. deploy 結果

`git archive HEAD` で tmpdir を作り、Cloud Run source deploy を実行した。

結果:

- new revision: `yoshilover-fetcher-00134-cbz`
- image digest:
  `asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:3b4418cdd948551c292ae2b89fed40ff6c1b46f2235476587225c840d140f585`
- service URL:
  `https://yoshilover-fetcher-487178857517.asia-northeast1.run.app`

Cloud Run deploy 出力:

```text
Service [yoshilover-fetcher] revision [yoshilover-fetcher-00134-cbz] has been deployed and is serving 100 percent of traffic.
```

### smoke test

```text
latest_ready_revision=yoshilover-fetcher-00134-cbz
traffic_100_revision=yoshilover-fetcher-00134-cbz
image=asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:3b4418cdd948551c292ae2b89fed40ff6c1b46f2235476587225c840d140f585
RUN_DRAFT_ONLY=1
AUTO_TWEET_ENABLED=0
PUBLISH_REQUIRE_IMAGE=1
OK: Cloud Run revision is serving, RUN_DRAFT_ONLY=1, AUTO_TWEET_ENABLED=0, PUBLISH_REQUIRE_IMAGE=1, and recent logs exist.
```

判定:

- revision ready → OK
- traffic 100% → OK
- `RUN_DRAFT_ONLY=1` → OK
- `AUTO_TWEET_ENABLED=0` → OK
- `PUBLISH_REQUIRE_IMAGE=1` → OK

---

## 5. 手動 trigger と観測結果

手動実行:

```bash
gcloud scheduler jobs run fact-check-morning-report \
  --location asia-northeast1 \
  --project baseballsite
```

新 revision `00134-cbz` の `fact_check_email_sent` ログ:

```json
{
  "checked_posts": 0,
  "event": "fact_check_email_sent",
  "green": 0,
  "message_id": "<177649587101.1.9769171342820495683@yoshilover.com>",
  "red": 0,
  "refused_recipients": {},
  "since": "yesterday",
  "smtp_response": [
    250,
    "2.0.0 OK d9443c01a7336-2b5fa9ff3casm50905355ad.17 - gsmtp"
  ],
  "subject": "【ヨシラバー】04/17 事実チェック結果（🔴0件 / 🟡0件 / ✅0件）",
  "to_email": "fwns6760@gmail.com",
  "yellow": 0
}
```

受信失敗系ログ:

```json
[]
```

観測時刻:

- event timestamp (UTC): `2026-04-18T07:04:33.509392Z`
- JST: `2026-04-18 16:04:33`

---

## 6. 結論

### 事実

- `message_id` は記録された
- `refused_recipients` は **空 dict**
- `smtp_response` は **`250 2.0.0 OK ... - gsmtp`**
- `fact_check_email_failed` / `fact_check_email_demo` / `fact_check_secret_unavailable` は new revision では出ていない

### 判断

- **SMTP 経路までは健全**
- Gmail SMTP サーバはこのメッセージを受理している
- それでも Gmail 全体検索に痕跡がないなら、問題は **SMTP 送信コードではなく Gmail 側の配送後段 / 同一アカウント自己宛処理 / Gmail 内部フィルタリング** 側に寄っている

本便の範囲では:

- **T-016 の「観測性向上」は完了**
- 「SMTP 成功報告が嘘かどうか」は切り分け完了
- 結論は **嘘ではない。SMTP では 250 OK まで到達している**

---

## 7. 次の調査起点

よしひろさん側で Gmail 検索に使う値:

```text
rfc822msgid:<177649587101.1.9769171342820495683@yoshilover.com>
```

ここでもヒットしない場合、次に疑う先は Gmail 側:

1. 自己宛 app-password 送信の Gmail 内部処理
2. 受信フィルタやカテゴリ分けではなく、配送後の非表示・破棄
3. Gmail アカウント側のセキュリティ/内部判定

少なくとも今回のコード経路では、**配送要求は Gmail SMTP まで正常に届いている**。
