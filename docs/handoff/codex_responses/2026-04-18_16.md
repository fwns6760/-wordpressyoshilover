# Codex Responses — 2026-04-18（第16便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_16.md`

- 実施内容: 1時間おき fact-check メール化 + skip-on-empty + 24h 運用サマリ追加
- 本便では `env変更 / Secret Manager変更 / Phase C 解放` は未実施

---

## 1. 実装内容

対象ファイル:

- [src/fact_check_notifier.py](/home/fwns6/code/wordpressyoshilover/src/fact_check_notifier.py)
- [src/server.py](/home/fwns6/code/wordpressyoshilover/src/server.py)
- [tests/test_fact_check_notifier.py](/home/fwns6/code/wordpressyoshilover/tests/test_fact_check_notifier.py)

主な変更:

1. `_should_send_email(reports, posts_in_last_hour)` を追加
   - 直近1時間窓に post あり → `hourly_window_has_posts`
   - 直近1時間窓ゼロでも red あり → `red_present`
   - どちらもなし → `no_change_no_red`
2. 件名を hourly 形式へ変更
   - `ヨシラバー MM/DD HH:00 🔴N 🟡M ✅K`
3. `run_notification()` を send / skip 分岐へ刷新
   - `fact_check_email_sent` に `reason` / `posts_in_last_hour_count` を追加
   - `fact_check_email_skipped` を追加
   - 第14便の `message_id` / `refused_recipients` / `smtp_response` は維持
4. 直近24h運用サマリ helper を追加
   - Cloud Logging の `rss_fetcher_run_summary` / `rss_fetcher_flow_summary` を集計
   - `drafts_created` / `created_subtype_counts` / `skip_duplicate` / `skip_filter` / `error_count` / `x_post_count` を本文へ差し込み
   - publish 件数がログ未提供なら `0件（Phase C 未解放）`
   - 集計失敗時は `集計取得失敗` を本文へ出し、送信自体は継続
5. `/fact_check_notify` レスポンスに `reason` / `posts_in_last_hour_count` / `delivery_mode` を追加

実装 commit:

- `ab349de feat: make fact check emails hourly and concise`

---

## 2. regression test

追加・更新した確認:

1. `_should_send_email` の 3 ケース
2. 件名が `MM/DD HH:00 🔴N 🟡M ✅K` 形式になること
3. `rss_fetcher_run_summary` / `rss_fetcher_flow_summary` 集計
4. 集計取得失敗時でも送信が止まらないこと
5. skip 分岐で `fact_check_email_skipped` が出ること

実行:

```bash
python3 -m unittest tests.test_fact_check_notifier
python3 -m unittest tests.test_server
python3 -m unittest discover -s tests
```

結果:

```text
Ran 15 tests in 0.016s
OK

Ran 15 tests in 0.088s
OK

Ran 379 tests in 0.826s
OK
```

---

## 3. Cloud Run deploy / smoke test

source deploy 対象 commit:

- `ab349de`

deploy 結果:

- revision: `yoshilover-fetcher-00135-rpc`
- service URL: `https://yoshilover-fetcher-487178857517.asia-northeast1.run.app`
- image digest:
  `asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:af1313ff0fb4f705e10115e2aa13eba7355a25041bc84f2f21b9c286dcfa6a6a`
- traffic: 100%

smoke test:

```bash
bash scripts/cloud_run_smoke_test.sh yoshilover-fetcher-00135-rpc
```

確認結果:

```text
latest_ready_revision=yoshilover-fetcher-00135-rpc
traffic_100_revision=yoshilover-fetcher-00135-rpc
RUN_DRAFT_ONLY=1
AUTO_TWEET_ENABLED=0
PUBLISH_REQUIRE_IMAGE=1
OK: Cloud Run revision is serving, RUN_DRAFT_ONLY=1, AUTO_TWEET_ENABLED=0, PUBLISH_REQUIRE_IMAGE=1, and recent logs exist.
```

判定:

- revision ready → OK
- traffic 100% → OK
- `RUN_DRAFT_ONLY=1` → OK
- `AUTO_TWEET_ENABLED=0` → OK
- `PUBLISH_REQUIRE_IMAGE=1` → OK

---

## 4. Scheduler 再調整

実行:

```bash
gcloud scheduler jobs update http fact-check-morning-report \
  --location asia-northeast1 \
  --project baseballsite \
  --schedule '0 * * * *' \
  --time-zone 'Asia/Tokyo'
```

describe:

```bash
gcloud scheduler jobs describe fact-check-morning-report \
  --location asia-northeast1 \
  --project baseballsite \
  --format='value(schedule,timeZone,state,lastAttemptTime,scheduleTime)'
```

結果:

```text
0 * * * *	Asia/Tokyo	ENABLED	2026-04-18T07:30:09.130753Z	2026-04-18T08:00:00Z
```

判定:

- `schedule=0 * * * *`
- `timeZone=Asia/Tokyo`
- `state=ENABLED`

---

## 5. 手動 trigger 観測

実行:

```bash
gcloud scheduler jobs run fact-check-morning-report \
  --location asia-northeast1 \
  --project baseballsite
```

ログ確認:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND timestamp>="2026-04-18T07:59:47Z" AND (jsonPayload.event="fact_check_email_sent" OR jsonPayload.event="fact_check_email_skipped" OR jsonPayload.event="fact_check_notify_started")' \
  --project baseballsite \
  --limit 10 \
  --order=asc \
  --format=json
```

観測結果:

```json
[
  {
    "timestamp": "2026-04-18T08:00:08.373383Z",
    "event": "fact_check_notify_started",
    "reason": "no_change_no_red",
    "posts_in_last_hour_count": 0,
    "checked_posts": 0,
    "red": 0,
    "yellow": 0,
    "green": 0
  },
  {
    "timestamp": "2026-04-18T08:00:08.373405Z",
    "event": "fact_check_email_skipped",
    "reason": "no_change_no_red",
    "posts_in_last_hour_count": 0,
    "subject": "ヨシラバー 04/18 17:00 🔴0 🟡0 ✅0",
    "checked_posts": 0,
    "red": 0,
    "yellow": 0,
    "green": 0
  },
  {
    "timestamp": "2026-04-18T08:01:58.346600Z",
    "event": "fact_check_notify_started",
    "reason": "no_change_no_red",
    "posts_in_last_hour_count": 0,
    "checked_posts": 0,
    "red": 0,
    "yellow": 0,
    "green": 0
  },
  {
    "timestamp": "2026-04-18T08:01:58.346638Z",
    "event": "fact_check_email_skipped",
    "reason": "no_change_no_red",
    "posts_in_last_hour_count": 0,
    "subject": "ヨシラバー 04/18 17:00 🔴0 🟡0 ✅0",
    "checked_posts": 0,
    "red": 0,
    "yellow": 0,
    "green": 0
  }
]
```

補足:

- `08:00:08Z` は 17:00 JST の自然発火
- `08:01:58Z` は手動 trigger 分
- 今回は `checked_posts=0` かつ `red=0` のため、期待どおり `skip` 分岐に入った

判定:

- 観測された branch は **(C) no_change_no_red → skip**
- `fact_check_email_skipped` ログは正常
- 件名フォーマットは `ヨシラバー MM/DD HH:00 🔴N 🟡M ✅K` へ更新済み
- 今回は skip のため `message_id` / `refused_recipients` / `smtp_response` は発生しない

---

## 6. 結論

- 第16便の実装は Cloud Run `00135-rpc` へ反映済み
- Scheduler は最終的に `0 * * * *` へ再調整済み
- 手動 trigger では `no_change_no_red` 条件に一致し、期待どおり `fact_check_email_skipped` を確認
- skip branch のため今回の実測ではメール本文 preview は非該当
- 第14便の SMTP 観測性はコード上維持されている

---

## 7. commit / push

- 実装 commit: `ab349de`
- 本 response doc 追加後に commit / push 実施
