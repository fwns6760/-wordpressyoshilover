# Codex Responses — 2026-04-18（第17便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_17.md`

- 実施内容: `src/yoshilover-exclude-cat.php` に REST_REQUEST ガードを 2 箇所追加
- 本便では `WP (Xserver) への deploy / env変更 / Cloud Run / Cloud Scheduler / Secret Manager` は未実施

---

## 1. 修正内容

対象ファイル:

- [src/yoshilover-exclude-cat.php](/home/fwns6/code/wordpressyoshilover/src/yoshilover-exclude-cat.php)

追加した箇所:

1. `pre_get_posts` フック先頭
2. `posts_where` フック先頭

変更要点:

```diff
 add_action( 'pre_get_posts', function( WP_Query $query ) {
+    // REST リクエスト中はこのプラグインを効かせない（collection query の status フィルタ保護）
+    if ( defined( 'REST_REQUEST' ) && REST_REQUEST ) {
+        return;
+    }
     if ( is_admin() ) {
         return;
     }
 ...
 add_filter( 'posts_where', function( $where, $query ) {
     global $wpdb;
+    if ( defined( 'REST_REQUEST' ) && REST_REQUEST ) {
+        return $where;
+    }
     if ( is_admin() ) {
         return $where;
     }
```

意図:

- REST collection query (`/wp/v2/posts?status=draft`) 中はこのプラグインを効かせない
- front-end 用の `post_status=publish` 強制や category 除外が REST に漏れないようにする
- 既存ロジックは変更せず、REST のみ早期 return

---

## 2. syntax check

実行:

```bash
php -l src/yoshilover-exclude-cat.php
```

結果:

```text
No syntax errors detected in src/yoshilover-exclude-cat.php
```

判定:

- syntax error なし

---

## 3. diff 確認

実行:

```bash
git diff -- src/yoshilover-exclude-cat.php
```

結果要点:

- `pre_get_posts` 先頭に REST ガード追加
- `posts_where` 先頭に REST ガード追加
- それ以外の既存ロジック変更なし

---

## 4. Python test suite

実行:

```bash
python3 -m unittest discover -s tests
```

結果:

```text
Ran 379 tests in 0.928s
OK
```

判定:

- 既存 test suite は維持
- 今回の PHP 変更による repo 全体への副作用は見えていない

---

## 5. commit / push

実装 commit:

- `f09cffc`
- message: `fix(exclude-cat): skip REST requests so ?status=draft works`

push:

- response doc 追加後に `origin/master` へ push 実施

---

## 6. 第18便への引き継ぎ

第18便で必要なこと:

1. 修正済み `src/yoshilover-exclude-cat.php` を WP 側へ差し替え
2. 実機確認
   - `curl .../wp-json/wp/v2/posts?status=draft&context=edit`
   - draft が返ること
3. T-001 の RESOLVED 判定

必要な接続手段:

- Xserver SSH 接続情報、または
- WP admin で plugin 差し替えできる権限

本便では deploy していないため、T-001 は **コード修正完了 / 本番未適用** の状態。
