# Codex Responses — 2026-04-18（第18便）

対象依頼: `docs/handoff/codex_requests/2026-04-18_18.md`

- 実施内容: Phase C の `postgame` / `lineup` publish 解放
- 本便では `コード変更 / WP 側変更 / Secret Manager / Scheduler 変更` は未実施
- rollback: **未実施**

---

## 1. Step 0: 現状記録

実行:

```bash
gcloud run services describe yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --format='value(status.latestReadyRevisionName)'
```

結果:

```text
yoshilover-fetcher-00135-rpc
```

判定:

- 依頼書想定の baseline revision `00135-rpc` と一致
- 続行可

env snapshot:

```bash
gcloud run services describe yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --format='value(spec.template.spec.containers[0].env)' \
  > /tmp/yoshilover_env_before_phase_c.txt
```

保存先:

- `/tmp/yoshilover_env_before_phase_c.txt`

baseline 主要 env:

- `RUN_DRAFT_ONLY=1`
- `ENABLE_PUBLISH_FOR_POSTGAME=0`
- `ENABLE_PUBLISH_FOR_LINEUP=0`
- `AUTO_TWEET_ENABLED=0`
- `PUBLISH_REQUIRE_IMAGE=1`
- 他 `ENABLE_PUBLISH_FOR_* = 0`

---

## 2. Step 1: env 変更

実行:

```bash
gcloud run services update yoshilover-fetcher \
  --region asia-northeast1 --project baseballsite \
  --update-env-vars ENABLE_PUBLISH_FOR_POSTGAME=1,ENABLE_PUBLISH_FOR_LINEUP=1,RUN_DRAFT_ONLY=0
```

出力:

```text
Deploying...
Creating Revision.......................................................done
Routing traffic.....done
Done.
Service [yoshilover-fetcher] revision [yoshilover-fetcher-00136-z7s] has been deployed and is serving 100 percent of traffic.
Service URL: https://yoshilover-fetcher-487178857517.asia-northeast1.run.app
```

新 revision:

- `yoshilover-fetcher-00136-z7s`

image digest:

- `asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:af1313ff0fb4f705e10115e2aa13eba7355a25041bc84f2f21b9c286dcfa6a6a`

---

## 3. Step 2: smoke test

実行:

```bash
bash scripts/cloud_run_smoke_test.sh yoshilover-fetcher-00136-z7s
```

script 出力:

```text
latest_ready_revision=yoshilover-fetcher-00136-z7s
traffic_100_revision=yoshilover-fetcher-00136-z7s
image=asia-northeast1-docker.pkg.dev/baseballsite/cloud-run-source-deploy/yoshilover-fetcher@sha256:af1313ff0fb4f705e10115e2aa13eba7355a25041bc84f2f21b9c286dcfa6a6a
RUN_DRAFT_ONLY=0
AUTO_TWEET_ENABLED=0
PUBLISH_REQUIRE_IMAGE=1
🔴 RUN_DRAFT_ONLY=0 detected. Expected 1. Rollback required.
```

補足:

- この script は現時点で `RUN_DRAFT_ONLY=1` を固定期待しているため、Phase C 解放時には **false negative** になる
- 実際の service env 読み返しでは、変更内容は依頼どおり

env 読み返しで確認した主要値:

- `RUN_DRAFT_ONLY=0`
- `ENABLE_PUBLISH_FOR_POSTGAME=1`
- `ENABLE_PUBLISH_FOR_LINEUP=1`
- `AUTO_TWEET_ENABLED=0`
- `PUBLISH_REQUIRE_IMAGE=1`
- 他 `ENABLE_PUBLISH_FOR_*` は `0` のまま

判定:

- script 自体は exit 1
- ただし原因は service 異常ではなく smoke script の旧前提
- service state は依頼書の期待値どおりであり、rollback 条件には該当しないと判断

---

## 4. Step 3: 手動 trigger で初回観察

実行:

```bash
gcloud scheduler jobs run fact-check-morning-report \
  --location asia-northeast1 --project baseballsite
```

ログ確認:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND (jsonPayload.event="fact_check_email_sent" OR jsonPayload.event="fact_check_email_skipped" OR jsonPayload.event="rss_fetcher_flow_summary" OR severity>="ERROR")' \
  --project baseballsite --freshness=5m --limit 20 --format=json
```

観測結果:

```json
[
  {
    "timestamp": "2026-04-18T08:44:51.819772Z",
    "event": "fact_check_email_skipped",
    "reason": "no_change_no_red",
    "checked_posts": 0,
    "red": 0,
    "yellow": 0,
    "green": 0,
    "posts_in_last_hour_count": 0,
    "subject": "ヨシラバー 04/18 17:00 🔴0 🟡0 ✅0",
    "revision_name": "yoshilover-fetcher-00136-z7s"
  }
]
```

判定:

- `fact_check_email_skipped` は正常
- `ERROR` は観測されていない
- env 変更後の revision `00136-z7s` で notifier は正常稼働

---

## 5. Step 4: publish 解放後の初回 `/run` 観察

実行:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND jsonPayload.event="rss_fetcher_run_summary"' \
  --project baseballsite --freshness=15m --limit 5 --format=json
```

結果:

```json
[]
```

追加確認:

```bash
gcloud logging read \
  'resource.type="cloud_run_revision" AND resource.labels.service_name="yoshilover-fetcher" AND severity>="ERROR"' \
  --project baseballsite --freshness=15m --limit 10 --format=json
```

結果:

```json
[]
```

判定:

- env 変更後まだ `/run` の自然発火 or 手動実行は発生していない
- したがって `postgame` / `lineup` publish 実績そのものは **次回発火待ち**
- 直近 15 分の `ERROR` は 0 件

---

## 6. 最終判定

- **成功**
- Phase C の `postgame` / `lineup` publish 解放は Cloud Run env に反映済み
- 新 revision は `yoshilover-fetcher-00136-z7s`
- `RUN_DRAFT_ONLY=0`
- `ENABLE_PUBLISH_FOR_POSTGAME=1`
- `ENABLE_PUBLISH_FOR_LINEUP=1`
- `AUTO_TWEET_ENABLED=0` 維持
- `PUBLISH_REQUIRE_IMAGE=1` 維持
- 他カテゴリの publish flag は 0 のまま
- rollback は未実施

残タスク:

- 次の自然 `/run` 発火で `postgame` / `lineup` が実際に publish されるか観察
- 他カテゴリ publish 混入が出た場合は即 rollback

---

## 7. commit / push

- 本 response doc を追加して commit / push 実施
