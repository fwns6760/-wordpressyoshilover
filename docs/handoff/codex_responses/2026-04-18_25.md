# Codex response — 2026-04-18 第25便

## 概要

- 本便は **調査のみ**。コード修正 / deploy / env / scheduler 変更なし
- 7d 窓の `rss_fetcher_flow_summary` を確認したが、`jsonPayload.event` では 0 件だったため、**`textPayload` の JSON 文字列を正規化して集計**した
- 結論を先に書くと、平日 18-19 時 JST の空振りは **部分的に systemic** だが、主に **`pregame` subtype が試合開始後に 0% 通過**していることが本体
- 一方で `postgame` / `lineup` / `farm` は sample では **通過率 100%**
- `pregame_started` は件数上は決定的だが、**時刻的には過剰発火の証拠はなく、むしろ試合開始後の stale pregame を正しく止めている** 可能性が高い
- 推奨は 1 つに絞ると **(E) WONTFIX 寄り**。少なくとも次便で `pregame_started` を緩める根拠は足りない

## Step 0: flow summary 7d 取得

### 取得結果

- `jsonPayload.event="rss_fetcher_flow_summary"`: **0 件**
- `textPayload:"rss_fetcher_flow_summary"`: **76 件**

補足:

- `src/rss_fetcher.py:9440` では `logger.info(json.dumps(...))` で出しているが、Cloud Logging 上では今回 **`textPayload`** として保存されていた
- そのため、`textPayload` 末尾の JSON を抽出して正規化した

正規化後:

- `normalized_count = 76`
- 有効範囲:
  - earliest: `2026-04-16T07:50:38.162490Z`
  - latest: `2026-04-18T10:30:21.980384Z`

注意:

- freshness は 7d だが、実際に取れた flow summary は **2026-04-16〜2026-04-18** が中心
- よって sample は 7d フルではなく、**実効 2 平日 + 1 週末** 程度

## Step 1: 平日 18-19 時 JST のみ抽出

抽出条件:

- JST
- weekday = 月火水木金
- hour = `18` or `19`

抽出結果:

- `weekday_18_19_rows = 8`

対象行:

| JST | prepared_subtype_counts | created_subtype_counts | 主な skip_reasons |
|---|---|---|---|
| 2026-04-16 18:00 | `{"lineup": 1}` | `{"lineup": 1}` | `not_giants_related=64`, `history_duplicate=22`, `sns_polluted=20`, `social_too_weak=14` |
| 2026-04-16 18:30 | `{"farm": 1}` | `{"farm": 1}` | `not_giants_related=63`, `sns_polluted=23`, `history_duplicate=18`, `social_too_weak=14` |
| 2026-04-16 19:00 | `{}` | `{}` | `not_giants_related=56`, `sns_polluted=26`, `social_too_weak=19`, `history_duplicate=19`, `live_update_disabled=4` |
| 2026-04-16 19:30 | `{}` | `{}` | `not_giants_related=59`, `sns_polluted=26`, `social_too_weak=23`, `history_duplicate=12`, `live_update_disabled=4` |
| 2026-04-17 18:00 | `{"pregame": 3}` | `{}` | `not_giants_related=63`, `history_duplicate=33`, `pregame_started=3`, `sns_polluted=20` |
| 2026-04-17 18:30 | `{"postgame": 1, "pregame": 3}` | `{"postgame": 1}` | `not_giants_related=60`, `history_duplicate=29`, `pregame_started=3`, `sns_polluted=21` |
| 2026-04-17 19:00 | `{"postgame": 1, "pregame": 3}` | `{"postgame": 1}` | `not_giants_related=56`, `sns_polluted=26`, `history_duplicate=23`, `pregame_started=3`, `live_update_disabled=1` |
| 2026-04-17 19:30 | `{"pregame": 2}` | `{}` | `not_giants_related=53`, `social_too_weak=27`, `sns_polluted=26`, `history_duplicate=19`, `pregame_started=2` |

所見:

- 2026-04-16 は 18:00 / 18:30 に `lineup` と `farm` が素直に通っている
- 問題が濃いのは **2026-04-17 の `pregame`**
- 2026-04-16 19:00 / 19:30 は prepared 自体が 0 で、候補段階から消えている

## Step 2: skip_reason 分布集計

平日 18-19 時 JST の **raw 合計**:

| rank | skip_reason | count |
|---|---|---:|
| 1 | `not_giants_related` | 474 |
| 2 | `sns_polluted` | 188 |
| 3 | `history_duplicate` | 175 |
| 4 | `social_too_weak` | 139 |
| 5 | `live_update_disabled` | 16 |
| 6 | `pregame_started` | 11 |
| 7 | `comment_required` | 6 |

解釈:

- raw 件数の上位は **source noise / upstream hygiene** 寄り
- ただし、実際に prepared まで来た candidate を殺しているのは raw 順位と必ずしも一致しない

## Step 3: subtype × prepared / created / skip のクロス

### prepared_subtype_counts

| subtype | prepared |
|---|---:|
| `pregame` | 11 |
| `postgame` | 2 |
| `lineup` | 1 |
| `farm` | 1 |

### created_subtype_counts

| subtype | created |
|---|---:|
| `postgame` | 2 |
| `lineup` | 1 |
| `farm` | 1 |
| `pregame` | 0 |

### prepared → created 通過率

| subtype | prepared | created | pass_rate |
|---|---:|---:|---:|
| `pregame` | 11 | 0 | `0.0%` |
| `postgame` | 2 | 2 | `100.0%` |
| `lineup` | 1 | 1 | `100.0%` |
| `farm` | 1 | 1 | `100.0%` |

### category view

| category | prepared | created | pass_rate |
|---|---:|---:|---:|
| `試合速報` | 14 | 3 | `21.4%` |
| `ドラフト・育成` | 1 | 1 | `100.0%` |

重要:

- `試合速報` の通過率が低く見える主因は、**`pregame 11件が全落ち`**
- sample 上、`postgame` / `lineup` / `farm` には systemic failure は見えない

## Step 4: `pregame_started` の妥当性評価

### 発火日時

`pregame_started > 0` が出たのは次の 4 行:

| JST | pregame_started | prepared_subtype_counts |
|---|---:|---|
| 2026-04-17 18:00 | 3 | `{"pregame": 3}` |
| 2026-04-17 18:30 | 3 | `{"postgame": 1, "pregame": 3}` |
| 2026-04-17 19:00 | 3 | `{"postgame": 1, "pregame": 3}` |
| 2026-04-17 19:30 | 2 | `{"pregame": 2}` |

### 実試合開始時刻のベストエフォート突合

同日ログを確認すると、2026-04-17 17:50 JST 前後から次の HIT が出ていた:

- `【一軍】巨人 vs ヤクルト 明治神宮野球場 18時試合開始...`
- `【一軍】巨人 vs ヤクルト 明治神宮野球場 18時試合開始予定...`

つまり:

- 少なくともこの sample では **試合開始は 18:00 JST**
- `pregame_started` の最初の発火は **18:00 JST**
- **18:00 より前に pregame_started が出ている証拠は見つからなかった**

### 評価

`pregame_started` は **過剰発火とは言いにくい**。

理由:

- earliest 発火が試合開始ちょうど
- しかも kill 対象は `pregame` subtype に限られている
- 18:30 / 19:00 / 19:30 にも pregame 候補が残っているのは、閾値の早すぎではなく **feed 側に stale pregame が残って流れてくる** ことを示している可能性が高い

逆に言うと:

- 平日 18-19 時の「記事が残らない」は、`pregame_started` のバグというより **phase C の policy がその時間帯の stale pregame を正しく捨てている結果**

## Step 5: 結論

### 1. 平日 18-20 時の空振りは systemic か

**部分的**

- `pregame` subtype については systemic
- `postgame` / `lineup` / `farm` は sample では systemic ではない

### 2. 最多 skip_reason TOP 3

1. `not_giants_related = 474`
2. `sns_polluted = 188`
3. `history_duplicate = 175`

補足:

- これらは raw volume 上位だが、prepared candidate の直接 kill とは別軸
- prepared candidate の通過率を壊している高レバレッジ要因は **`pregame_started` × `pregame`**

### 3. prepared → created 通過率

- `pregame`: `0 / 11 = 0.0%`
- `postgame`: `2 / 2 = 100.0%`
- `lineup`: `1 / 1 = 100.0%`
- `farm`: `1 / 1 = 100.0%`

### 4. `pregame_started` の妥当性評価

**妥当寄り**

- 試合開始前に早すぎる発火は観測できていない
- 試合開始後の stale pregame を止めている動きに見える

### 5. 次便候補

- (A) `pregame_started` 発動閾値の見直し
- (B) `social_too_weak` / `sns_polluted` 閾値の再評価
- (C) `live_update_disabled` を平日夕方だけ OFF
- (D) 18-20 時帯専用の relaxed policy profile
- (E) **WONTFIX**

### 6. 優先度推奨

**(E) WONTFIX** を推奨。

理由:

- 今回の sample では `pregame_started` が早すぎる証拠はなく、むしろ stale pregame を正しく止めている
- raw 上位の `not_giants_related` / `sns_polluted` / `history_duplicate` を緩めると、増えるのは evening coverage より noise の可能性が高い
- したがって、現状の平日 18-19 時の薄さは **policy bug ではなく by-design の結果** と見るのが妥当

補足:

- もし「量を増やしたい」を優先するなら、次の実装対象は `pregame_started` 緩和ではなく **(D) 時間帯限定の relaxed policy profile**。ただし別 acceptance を先に定義した方が安全

## 実施なし

- コード修正なし
- deploy なし
- scheduler 変更なし
- env / Secret 変更なし

## commit / push

- この response doc を追加後、commit / push 実施
