# Codex response — 2026-04-18 第27便

## 概要

- `src/rss_fetcher.py` に公開済み過去記事リンクの自動挿入を実装した
- `【関連記事】` は本文末尾の最終 CTA 手前に挿入され、候補 0 件時は出さない
- 関連記事候補は最大 2 件、30 日以内、優先順は「同一選手名 → 同 category + 同 subtype → 同 category」
- 本便では deploy はしていない

## 実装内容

- `src/rss_fetcher.py`
  - 公開済み記事検索ヘルパーを追加
  - 関連記事選定ヘルパーを追加
  - `build_news_block` 後段で関連記事を引き、`<div class="yoshilover-related-posts">` を CTA 手前に差し込むよう変更
  - 検索失敗時は warning を出して 0 件扱いにし、本文生成自体は継続する
- `tests/test_related_posts.py`
  - 選手名優先
  - 30 日窓
  - 自記事除外 / 重複除外
  - 空セクション抑止
  - CTA 手前への挿入
- `tests/conftest.py`
  - 既存 `build_news_block` 系 test が外部検索に触れないよう、関連記事検索を既定で stub 化
- `pytest.ini`
  - `enable_related_posts_lookup` marker を追加

## bundle 反映

同じ commit に次の残差分も含めた。

- `CLAUDE.md`
- `docs/handoff/codex_requests/2026-04-18_26.md`
- `docs/handoff/codex_requests/2026-04-18_27.md`
- `docs/handoff/tickets/OPEN.md`

## テスト

実行:

```bash
python3 -m pytest tests/
```

結果:

```text
389 passed, 3 warnings in 1.50s
```

## commit / push

実装 commit:

```text
9ae1169 feat(article): T-021 過去記事リンク自動挿入 + bundle CLAUDE.md/request 26-27
```

push 結果:

```text
To github-yoshilover:fwns6760/-wordpressyoshilover.git
   5c5c483..9ae1169  master -> master
```

## 残 untracked

本便完了時点:

```text
?? .codex
?? data/
```

## 次便への引き継ぎ

- deploy は未実施
- 実公開記事で `【関連記事】` 表示位置とリンク内容の目視確認は次便
