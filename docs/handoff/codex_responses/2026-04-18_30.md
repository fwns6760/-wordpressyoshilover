# Codex response — 2026-04-18 第30便

## 実装 commit

```text
b7f20d8 feat(article): T-021 第2弾 team batting stats の opt-in 注入（flag 既定 OFF）+ bundle request 30
```

push 結果:

```text
To github-yoshilover:fwns6760/-wordpressyoshilover.git
   9f49a0b..b7f20d8  master -> master
```

## 変更ファイル一覧

`git show --stat HEAD`:

```text
commit b7f20d8f1b3f0e650069b6fefb072902250d418f
Author: yoshihiro <yoshihiro@example.com>

    feat(article): T-021 第2弾 team batting stats の opt-in 注入（flag 既定 OFF）+ bundle request 30

 .env.example                                 |   4 +
 docs/handoff/codex_requests/2026-04-18_30.md | 150 +++++++++++++++++++++++++
 src/rss_fetcher.py                           | 149 +++++++++++++++++++++++--
 tests/test_team_stats_prompt.py              | 160 +++++++++++++++++++++++++++
 4 files changed, 454 insertions(+), 9 deletions(-)
```

## pytest

実行:

```bash
python3 -m pytest tests/ -x
```

結果:

```text
397 passed, 3 warnings in 7.65s
```

## grep

`grep -n "ARTICLE_INJECT_TEAM_STATS" src/` 相当:

```text
src/rss_fetcher.py:6042:        if os.environ.get("ARTICLE_INJECT_TEAM_STATS", "0") == "1" and category in ("試合速報", "首脳陣"):
```

補足:

```text
.env.example:35:ARTICLE_INJECT_TEAM_STATS=0
```

`grep -n "team_stats_block" src/rss_fetcher.py`:

```text
3001:    team_stats_block: str = "",
3022:    if team_stats_block:
3025:            f"{team_stats_block}\n"
3268:    team_stats_block: str = "",
3279:    if team_stats_block:
3282:            f"{team_stats_block}\n"
3425:    team_stats_block: str = "",
3457:            team_stats_block=team_stats_block,
3466:            team_stats_block=team_stats_block,
3505:    if team_stats_block:
3508:            f"{team_stats_block}\n"
6041:        team_stats_block = ""
6045:                team_stats_block = _format_team_batting_stats_block(team_stats)
6048:                team_stats_block = ""
6061:            team_stats_block=team_stats_block,
6063:        if team_stats_block:
6069:                        "line_count": team_stats_block.count("\n") + 1,
```

## 残 untracked

```text
?? .codex
?? data/
```
