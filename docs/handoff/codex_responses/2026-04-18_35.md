# Codex response — 2026-04-18 第35便

## 実装 file 一覧

- `src/audit_notify.py`
  - `/audit_notify` 本体を新設
  - `draft` + `publish` の直近更新記事を取得し、`title_body_mismatch / thin_body / no_opinion / no_eyecatch / pipeline_error` を評価
  - `<div class="yoshilover-related-posts">` を除外したコア本文抽出 helper を新規実装
  - 0件時 skip / 非0件時 `src/fact_check_notifier.py` の `send_email()` 再利用で SMTP 通知
- `src/server.py`
  - `GET /audit_notify` を追加
  - `X-Secret: $RUN_SECRET` 認証、`window_minutes`、`dry_run` を処理
- `tests/test_audit_notify.py`
  - related-posts 除外
  - 0件時 `mail_sent=false`
  - 非0件時 counts / subject / excerpt を検証
- `tests/test_server.py`
  - `_run_audit_notify()` の 200 / 500 を追加
- `.env.example`
  - `AUDIT_OPINION_USE_LLM=0` を追記
- `tests/test_cost_modes.py`
  - `test_todays_postgame_entry_is_not_skipped` の日付境界を JST 基準で安定化
- `docs/handoff/tickets/OPEN.md`
  - T-024 節を Remote Trigger 草案から Cloud Scheduler 実装前提へ全置換

## Canary deploy

### 1回目

```bash
gcloud run deploy yoshilover-fetcher \
  --source . \
  --region asia-northeast1 \
  --project baseballsite \
  --no-traffic \
  --tag audit-notify-canary
```

結果:

```text
revision: yoshilover-fetcher-00142-les
tag url: https://audit-notify-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app
```

補足:

- `describe` で `ARTICLE_INJECT_TEAM_STATS=1` を継承していたため、prod へは昇格させず作り直し
- 原因は過去 canary (`00141-cen`) の service template が latest config として残っていたため
- この時点で prod traffic は `00139-k7h = 100%` のまま

### 2回目（採用 revision）

```bash
gcloud run deploy yoshilover-fetcher \
  --source . \
  --region asia-northeast1 \
  --project baseballsite \
  --remove-env-vars ARTICLE_INJECT_TEAM_STATS \
  --no-traffic \
  --tag audit-notify-canary
```

結果:

```text
revision: yoshilover-fetcher-00143-luj
tag url: https://audit-notify-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app
```

`gcloud run services describe yoshilover-fetcher --region asia-northeast1 --project baseballsite` 抜粋:

- traffic:
  - `yoshilover-fetcher-00139-k7h = 100%`
  - `yoshilover-fetcher-00143-luj = 0%` with `tag=audit-notify-canary`
- `00143-luj` env には `ARTICLE_INJECT_TEAM_STATS` が存在しない
- `RUN_SECRET`, `GMAIL_APP_PASSWORD_SECRET_NAME=yoshilover-gmail-app-password` は維持

## Canary smoke

### 0件 branch

実行:

```bash
curl -sS -w '\nHTTP_CODE=%{http_code}\n' \
  -H 'X-Secret: <RUN_SECRET>' \
  'https://audit-notify-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app/audit_notify?window_minutes=1'
```

結果:

```json
{"window_minutes": 1, "counts": {"title_body_mismatch": 0, "thin_body": 0, "no_opinion": 0, "no_eyecatch": 0, "pipeline_error": 0}, "total": 0, "mail_sent": false, "findings": []}
HTTP_CODE=200
```

### 非0件 branch

実行:

```bash
curl -sS -w '\nHTTP_CODE=%{http_code}\n' \
  -H 'X-Secret: <RUN_SECRET>' \
  'https://audit-notify-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app/audit_notify?window_minutes=1440'
```

結果:

```text
HTTP_CODE=200
counts.title_body_mismatch=1
counts.thin_body=0
counts.no_opinion=6
counts.no_eyecatch=0
counts.pipeline_error=0
total=7
mail_sent=true
```

Cloud Run log (`jsonPayload.event="audit_notify_completed"`, revision=`yoshilover-fetcher-00143-luj`) 抜粋:

```json
{
  "event": "audit_notify_completed",
  "window_minutes": 1440,
  "counts": {
    "title_body_mismatch": 1,
    "thin_body": 0,
    "no_opinion": 6,
    "no_eyecatch": 0,
    "pipeline_error": 0
  },
  "total": 7,
  "mail_sent": true,
  "delivery_mode": "smtp",
  "message_id": "<177653194890.1.18300735097649226636@yoshilover.com>",
  "refused_recipients": {},
  "smtp_response": [250, "2.0.0 OK d9443c01a7336-2b5faa2df0asm55226325ad.26 - gsmtp"]
}
```

## Prod traffic 切替

実行:

```bash
gcloud run services update-traffic yoshilover-fetcher \
  --region asia-northeast1 \
  --project baseballsite \
  --to-revisions yoshilover-fetcher-00143-luj=100
```

結果:

```text
0%   yoshilover-fetcher-00117-kes  tag=stage1
0%   yoshilover-fetcher-00141-cen  tag=canary-team-stats
100% yoshilover-fetcher-00143-luj  tag=audit-notify-canary
```

`gcloud run services describe` 最終確認:

- `100% traffic revision = yoshilover-fetcher-00143-luj`
- 前 revision `00139-k7h` は消えていないが traffic 対象から外れた
- `ARTICLE_INJECT_TEAM_STATS` は未設定のまま
- `RUN_DRAFT_ONLY=0`
- `AUTO_TWEET_ENABLED=0`
- `PUBLISH_REQUIRE_IMAGE=1`

## Cloud Scheduler job

作成:

```bash
gcloud scheduler jobs create http audit-notify-6x \
  --location asia-northeast1 \
  --project baseballsite \
  --schedule '0 2,4,6,8,11,14 * * *' \
  --time-zone UTC \
  --uri 'https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/audit_notify?window_minutes=60' \
  --http-method GET \
  --headers 'X-Secret=<RUN_SECRET>'
```

`gcloud scheduler jobs describe audit-notify-6x --location asia-northeast1 --project baseballsite` 抜粋:

```text
name: projects/baseballsite/locations/asia-northeast1/jobs/audit-notify-6x
schedule: 0 2,4,6,8,11,14 * * *
timeZone: UTC
state: ENABLED
httpTarget.uri: https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/audit_notify?window_minutes=60
httpTarget.httpMethod: GET
httpTarget.headers.X-Secret: <configured>
```

手動実行:

```bash
gcloud scheduler jobs run audit-notify-6x \
  --location asia-northeast1 \
  --project baseballsite
```

Cloud Run request log 抜粋:

```text
timestamp: 2026-04-18T18:16:57.441157Z
revision: yoshilover-fetcher-00143-luj
requestMethod: GET
requestUrl: https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/audit_notify?window_minutes=60
status: 200
userAgent: Google-Cloud-Scheduler
latency: 5.952429086s
```

同時刻の `audit_notify_completed` log:

```json
{
  "event": "audit_notify_completed",
  "window_minutes": 60,
  "counts": {
    "title_body_mismatch": 0,
    "thin_body": 0,
    "no_opinion": 0,
    "no_eyecatch": 0,
    "pipeline_error": 0
  },
  "total": 0,
  "mail_sent": false
}
```

## テスト

```bash
python3 -m unittest discover -s tests
```

結果:

```text
Ran 402 tests in 3.841s
OK
```

```bash
/tmp/yoshillover-cov-venv/bin/python -m coverage run -m unittest discover -s tests
/tmp/yoshillover-cov-venv/bin/python -m coverage report
```

結果:

```text
Ran 402 tests in 2.365s
OK
TOTAL 8449 6332 25%
```

補足:

- `coverage` は system Python に未導入だったため、一時 virtualenv `/tmp/yoshillover-cov-venv` を作成して実行

## OPEN.md T-024 更新差分

要点:

- `Remote Trigger / OIDC` 前提を削除
- `Cloud Scheduler (GCP) 専用 job` に置換
- job 名 `audit-notify-6x`
- cron `0 2,4,6,8,11,14 * * *` UTC
- endpoint `/audit_notify`
- 軸スコープ `title_body_mismatch / thin_body / no_opinion / no_eyecatch / pipeline_error`
- `repetition` は本便スコープ外として明記
- 件名 format / JSON schema / env 再利用方針を確定

## 既知の残課題

- なし
