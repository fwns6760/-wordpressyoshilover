# 2026-04-19 Codex Response 36

## 対応概要

- MB-005 の追加修正として、strict prompt 冒頭の内部矛盾を解消
- `no_opinion` 監査で paraphrase を拾えるよう、stem 判定を追加
- deploy / gcloud / secret 変更は未実施

## 変更ファイル

- `src/rss_fetcher.py`
  - manager / game(lineup, postgame, pregame) / farm(farm_lineup, farm) の strict prompt 冒頭 6 箇所を同一文面へ置換
  - social strict prompt は変更していない
- `src/audit_notify.py`
  - `OPINION_MARKER_STEMS` を追加
  - `_no_opinion_finding()` を完全一致または stem 部分一致で green 判定するよう変更
- `tests/test_gemini_prompt.py`
  - strict prompt 冒頭の共通文言を確認する assertion を追加
- `tests/test_audit_notify.py`
  - stem 部分一致で `no_opinion` を回避する test
  - 事実 dump のみを `no_opinion` と判定する test

## 動作確認

- `python3 -m unittest tests.test_gemini_prompt tests.test_audit_notify`
- `python3 -m unittest discover -s tests`
- `python3 -c "from src.audit_notify import OPINION_MARKER_STEMS; print(OPINION_MARKER_STEMS)"`

## 結果

- `unittest discover` は `Ran 404 tests ... OK`
- `OPINION_MARKER_STEMS` は追加済み
- `rss_fetcher.py` の対象は strict prompt 冒頭 6 箇所に限定
