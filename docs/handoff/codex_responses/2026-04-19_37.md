# 2026-04-19 Codex Response 37

## 対応概要

- strict prompt の regression test を `tests/test_gemini_prompt.py` に追加
- 変更は test のみで、`src/rss_fetcher.py` / `src/audit_notify.py` は未変更

## 変更内容

- `tests/test_gemini_prompt.py`
  - strict prompt 共通 helper の assertion を拡張
  - 対象 subtype は `postgame` / `lineup` / `pregame` / `manager` / `farm` / `farm_lineup`
  - 各 subtype で以下を回帰確認
    - `事実 → 解釈 → 感想` を含む
    - opinion close の代表語を 1 つ以上含む
    - `感想は書いてよい` を含む
    - `結果予想` または `一般論` の禁止文言を含む
    - 先頭 300 文字以内に `感想を足さない` を含まない

## 動作確認

- `python3 -m unittest tests.test_gemini_prompt`
- `python3 -m unittest discover -s tests`

## 結果

- `tests.test_gemini_prompt` は通過
- `unittest discover` は `Ran 404 tests ... OK`
- strict prompt regression の監視点を test で固定化
