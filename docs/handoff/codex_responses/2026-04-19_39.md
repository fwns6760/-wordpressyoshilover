# 2026-04-19 Codex Response 39

## 対応概要

- pre-publish 本文品質 guard を追加し、thin / template leak を検知した記事は publish せず draft 維持に変更
- source fact block が薄い記事は記事生成前に skip する filter を追加
- `ARTICLE_INJECT_TEAM_STATS` の既定値を code / `.env.example` ともに `1` に変更

## 変更ファイル

- `src/rss_fetcher.py`
  - `ARTICLE_INJECT_TEAM_STATS` の default を `"1"` に変更
  - `yoshilover-related-posts` を除外した本文コア抽出 helper を追加
  - subtype 別の publish 品質 guard を追加
    - `postgame` / `lineup`: 280 文字未満で NG
    - `manager` / `pregame` / `farm` / その他: 350 文字未満で NG
    - leak marker 含有で NG
  - NG 時は `publish_blocked_by_quality_guard` を Cloud Logging に出力
  - source fact block 100 文字未満を `article_skipped_thin_source` として skip
- `.env.example`
  - `ARTICLE_INJECT_TEAM_STATS=1` に変更
- `tests/test_publish_guard.py`
  - core body helper / thin / leak / pass / thin source の unit test を追加
- `tests/test_team_stats_prompt.py`
  - team stats が env 未設定でも既定で fetch される test を追加

## 動作確認

- `python3 -m unittest tests.test_publish_guard -v`
- `python3 -m unittest tests.test_team_stats_prompt -v`
- `python3 -m unittest discover -s tests`

## 結果

- `tests.test_publish_guard`: `Ran 6 tests ... OK`
- `tests.test_team_stats_prompt`: `Ran 9 tests ... OK`
- `unittest discover`: `Ran 411 tests ... OK`
- deploy / gcloud / secret 変更は未実施
