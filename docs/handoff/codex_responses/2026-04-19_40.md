# 2026-04-19 Codex Response 40

## deploy 結果

- deploy 対象 commit: `221e7f1`
- no-traffic deploy 成功
- 新 revision: `yoshilover-fetcher-00149-wal`
- 旧 prod revision: `yoshilover-fetcher-00144-h7g`
- build: `6cbc0ee0-7622-4af2-b03e-8ced91b99706` (`SUCCESS`)

## traffic

- no-traffic deploy 後:
  - `00144-h7g` = 100%
  - `00149-wal` = 0% (`smoke40` tag で個別到達)
- smoke 後に `gcloud run services update-traffic --to-latest` 実施
- 一時 tag `smoke40` は `--remove-tags smoke40` で削除
- 最終状態:
  - `00149-wal` = 100%
  - `00117-kes` (`stage1`) = 0%
  - `00141-cen` (`canary-team-stats`) = 0%

## smoke

### pre-cutover (no-traffic revision `smoke40`)

- `GET /health` → `200`, body=`OK`
- `GET /audit_notify?window_minutes=60&dry_run=1` → `200`
  - body 抜粋:
    - `window_minutes=60`
    - `mail_sent=false`
    - `counts.no_opinion=1`

### /run 確認

- revision tag URL への `POST /run` 直打ちは、audience 付き ID token を local user account から mint できず不可
  - `gcloud auth print-identity-token --audiences=...` は user account では不可
  - runtime service account impersonation も `roles/iam.serviceAccountTokenCreator` 不足で不可
- 代替として cutover 後に既存 authorized scheduler を手動実行:
  - `gcloud scheduler jobs run giants-weekend-pre --location asia-northeast1 --project baseballsite`
  - `2026-04-19T00:49:52Z` に Cloud Scheduler 側 AttemptStarted を確認
  - `yoshilover-fetcher-00149-wal` の Cloud Run log で `=== rss_fetcher 開始 ===` と取得ログを確認
  - 同 log 内で `article_team_stats_injected` も確認

## env 最終値

- `ARTICLE_INJECT_TEAM_STATS=1`
- service spec (`spec.template.spec.containers[0].env`) に明示設定あり

## rollback 参考

- `00144-h7g` は revision list に残存
- `gcloud run revisions list --service yoshilover-fetcher --project baseballsite --region asia-northeast1`
  で `00149-wal` と `00144-h7g` の両方を確認済み
