# 2026-04-19_45

## Phase A: 再現確認

- 対象: `faf3619` の smoke deploy 後
- 結果: intro echo leak は `manager social` で再現
- 再現内容: 本文冒頭が `読売ジャイアンツ専門ブログの編集者です。` で始まった
- 非再現:
  - `social postgame`
  - `social farm`
  - `manager news`
  - `game news`
  - `farm news`

## Phase B: 修正内容

### `src/rss_fetcher.py`

- `PROMPT_ROLE_ECHO_PREFIXES` を追加
  - `あなたは読売ジャイアンツ専門ブログの編集者です。`
  - `読売ジャイアンツ専門ブログの編集者です。`
- `_build_social_strict_prompt()` に明示ルールを追加
  - `上記の役割説明は本文に書かない。本文は必ず最初の見出し（【...】）から始める`
- `_strip_prompt_role_echo()` を追加
  - 本文先頭が role intro を echo した場合、最初の `【...】` 見出し位置まで除去
  - 見出しが無い場合も、先頭行が role intro ならその行だけ除去
- Gemini 生成後の strict / grounded 両 path で `_strip_prompt_role_echo()` を通すよう変更

### `tests/test_gemini_prompt.py`

- social strict prompt に「役割説明は本文に書かない」が入ることを追加確認
- regression test を追加
  - role intro が見出し前に漏れた場合は strip される
  - もともと見出し始まりの本文は変更されない

## 確認結果

- 修正後の `manager social` Gemini sample:
  - `INTRO_LEAK=False`
  - 本文冒頭: `【話題の要旨】 4月19日時点、スポーツ報知巨人班Xは、阿部監督がダルベック選手を熱血指導したことを伝えました。`
- `python3 -m py_compile src/rss_fetcher.py`: OK
- `python3 -m unittest discover -s tests`: `Ran 419 tests ... OK`

## 補足

- 本便では deploy は実施していない
- `manager / game / farm` の非 social strict builder では同種の intro echo は再現しなかったため、修正対象は social prompt 系に限定した
