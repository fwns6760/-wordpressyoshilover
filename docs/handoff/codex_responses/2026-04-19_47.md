# 2026-04-19_47

## 対応概要

- `thin_source_fact_block` の閾値を source type 別に分離
  - `social_news`: `50`
  - それ以外 (`news` など): `100`
- 実装は helper 方式
  - `_thin_source_fact_block_min_chars(source_type)`
  - `_is_thin_source_fact_block(source_type, source_fact_block_length)`
- `article_skipped_thin_source` の log `min_chars` には、実際に適用した threshold 値が入るまま維持

## 変更ファイル

- `src/rss_fetcher.py`
  - `THIN_SOURCE_FACT_BLOCK_MIN_CHARS_DEFAULT = 100`
  - `THIN_SOURCE_FACT_BLOCK_MIN_CHARS_SOCIAL_NEWS = 50`
  - `_thin_source_fact_block_min_chars()` を追加
  - `_is_thin_source_fact_block()` を追加
  - prepared 後の thin-source 判定を helper 経由に変更
- `tests/test_publish_guard.py`
  - social `50` は通過
  - social `49` は thin
  - default `99` は thin
  - 既存の rich / thin source metrics test を新定数名に追従

## 動作確認

- `python3 -m py_compile src/rss_fetcher.py`
- `python3 -m unittest tests.test_publish_guard -v`
- `python3 -m unittest discover -s tests`

## 結果

- `tests.test_publish_guard`: `Ran 9 tests ... OK`
- `unittest discover`: `Ran 422 tests ... OK`

## 補足

- 今回の requested threshold は `social_news=50` のため、`source_fact_block_length=50` は通るが、`45` と `39` は引き続き thin 判定で skip される
- したがって、`2026-04-19_46.md` で観測した `39 / 45 / 50` の 3 件に対する即時効果は、少なくとも `50` の 1 件を通せる範囲
- `45` も通したい場合は、threshold の再調整か source fact block の増強が別便で必要
