# 2026-04-19_49

## deploy 結果

- deploy 対象: `origin/master` HEAD `1d2e8bc`
- 含まれる主要 commit:
  - `faf3619` (`fix: route social game and farm stories to structured templates`)
  - `8c96980` (`fix: strip social prompt role intro echo`)
  - `1d2e8bc` (`fix: relax thin source threshold for social news`)
- Cloud Run service: `yoshilover-fetcher`
- project / region: `baseballsite` / `asia-northeast1`
- smoke revision: `yoshilover-fetcher-00157-wic`
- smoke tag URL: `social-thin-smoke`
- traffic: `00157-wic = 100%`
- 旧 live revision: `00153-ceg` は revision list に残存

## smoke / live 確認

- `GET /health`
  - smoke tag URL: `200`
  - live service URL: `200`
- `GET /audit_notify?window_minutes=60&dry_run=1`
  - direct curl は public URL だと `403`
  - 既存 Cloud Scheduler `audit-notify-6x` を手動実行したところ、live revision `00157-wic` で `HTTP 200` を確認

## article generation 確認

### template / intro leak

- social postgame
  - section header fingerprint: `【試合結果】 / 【ハイライト】 / 【選手成績】 / 【試合展開】`
  - intro leak: なし
- social farm
  - section header fingerprint: `【二軍結果・活躍の要旨】 / 【ファームのハイライト】 / 【二軍個別選手成績】 / 【一軍への示唆】`
  - intro leak: なし
- manager social
  - section header fingerprint: `【話題の要旨】 / 【発信内容の要約】 / 【文脈と背景】 / 【ファンの関心ポイント】`
  - intro leak: なし
  - 再サンプル冒頭: `【話題の要旨】 4月19日時点、スポーツ報知巨人班Xは、阿部監督がダルベック選手を熱血指導したことを伝えました。`

### live run 実測

- `giants-weekend-pre` を手動実行
- live revision `00157-wic` の `rss_fetcher_flow_summary`:
  - `prepared_subtype_counts = {manager:1, player:2, pregame:6}`
  - `created_subtype_counts = {manager:1}`
  - `thin_source_fact_block = 2`
- `00153-ceg` での同系 run は `created_subtype_counts = {}` / `thin_source_fact_block = 3` だったため、social thin-source 緩和の効果を確認

## draft / publish の観測

- generated draft / reused post:
  - `post_id=62667`
  - title: `阿部監督「いい感じに力が伝わってた」 ベンチの狙いはどこか`
- `publish_disabled_for_subtype` により draft 維持
- `x_post_ai_generated` まで実行されたが、`not_published` のまま

## audit-notify-6x

- Cloud Scheduler `audit-notify-6x` 手動実行:
  - Cloud Run requests log: `revision=00157-wic`, `status=200`
  - Cloud Scheduler execution log: `AttemptFinished`, `Original HTTP response code number = 200`
- `audit_notify_completed`:
  - `window_minutes=60`
  - `total=0`
  - `mail_sent=false`

## 補足

- `#4` の当初 prompt は `8c96980` 単独 HEAD 想定だったが、実運用では `#8` を先に反映したため、deploy 対象は `1d2e8bc` を含む current head に読み替えて実施
- `RUN_DRAFT_ONLY` は service env 上 `0` を維持
