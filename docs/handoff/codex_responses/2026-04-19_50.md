# 2026-04-19_50

## 対応概要

- `social_news` の thin-source 閾値を `50 -> 40` に引き下げ
- 通常 `news` の閾値 `100` は変更していない
- `article_skipped_thin_source` log の `min_chars` 出力は既存のまま維持

## 変更ファイル

- `src/rss_fetcher.py`
  - `THIN_SOURCE_FACT_BLOCK_MIN_CHARS_SOCIAL_NEWS = 40` に変更
- `tests/test_publish_guard.py`
  - social `40` は通過
  - social `39` は thin
  - social `45` は通過
  - default `99` は thin のまま

## 動作確認

- `python3 -m py_compile src/rss_fetcher.py`
- `python3 -m unittest tests.test_publish_guard -v`
- `python3 -m unittest discover -s tests`

## 結果

- `tests.test_publish_guard`: `Ran 10 tests ... OK`
- `unittest discover`: `Ran 423 tests ... OK`

## 補足

- これで `2026-04-19_46.md` / `2026-04-19_47.md` で観測した `source_fact_block_length=45` は通過対象になる
- `39` は引き続き thin 判定で skip される
