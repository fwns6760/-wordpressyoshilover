# 2026-04-19_51

## 対応内容
- `pregame_started` skip の `logger.debug` を、Cloud Logging で拾える `INFO` の構造化ログに変更
- 追加 event: `pregame_started_skip`
- payload に以下を含めるようにした
  - `title`
  - `post_url`
  - `first_pitch_time`（source の title + summary から抽出）
  - `now`
  - `game_state`
  - `game_ended`

## 変更ファイル
- `src/rss_fetcher.py`
- `tests/test_cost_modes.py`

## 実装メモ
- 判定ロジック `_should_skip_started_pregame_entry()` 自体は変更していない
- skip 発火時だけ `_log_pregame_started_skip(...)` を呼び、既存の `skip_reason_counts["pregame_started"]` 集計はそのまま維持
- `first_pitch_time` は Yahoo game status ではなく、source 側の `18:00` 表記を `_extract_game_time_token(...)` で拾う形にした

## テスト
- `python3 -m py_compile src/rss_fetcher.py`
- `python3 -m unittest tests.test_cost_modes -v`
- `python3 -m unittest discover -s tests`

## 結果
- `py_compile`: OK
- `tests.test_cost_modes`: OK
- `unittest discover`: `Ran 424 tests ... OK`

## 補足
- 追加ログは `pregame_started` skip が発生した記事だけに限定されるため、日次 volume は想定どおり小さい
- deploy は未実施
