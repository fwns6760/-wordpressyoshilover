# 2026-04-19_55

## 対応内容

- `not_giants_related` skip の observability を追加
- 追加内容
  - `INFO` 構造化 log: `not_giants_related_skip`
  - 1 run あたり先頭 5 件までに制限
  - `rss_fetcher_flow_summary.skip_reasons.not_giants_related_sample_titles` に先頭 3 件を追加

## 出力 payload

- `event`
- `title`
- `post_url`
- `source_name`
- `detected_keywords`
  - 現行実装では `GIANTS_KEYWORDS` に対する hit list
  - `not_giants_related` のため通常は空配列

## 変更ファイル

- `src/rss_fetcher.py`
- `tests/test_not_giants_observation.py`

## 実装メモ

- 判定ロジック `is_giants_related(...)` 自体は変更していない
- flow summary では count を維持したまま sample title だけ追加
- 追加 INFO は `not_giants_related` の先頭 5 件だけなので volume は限定的

## テスト

- `python3 -m py_compile src/rss_fetcher.py`
- `python3 -m unittest tests.test_not_giants_observation -v`
- `python3 -m unittest discover -s tests`

## 結果

- `py_compile`: OK
- `tests.test_not_giants_observation`: OK
- `unittest discover`: `Ran 431 tests ... OK`

## 補足

- deploy は未実施
