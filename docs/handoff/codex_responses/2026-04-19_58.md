# 2026-04-19_58

## deploy 結果

- deploy 対象: `origin/master` HEAD `75f82b7`
  - 含まれる code commit:
    - `046cab0` (`pregame_started` INFO log 昇格)
    - `0d060aa` (`giants_roster.json` + roster alias signal)
- Cloud Run service: `yoshilover-fetcher`
- project / region: `baseballsite` / `asia-northeast1`
- build: `6d9f9730-7f15-452f-a5c9-963765af7ecd` (`SUCCESS`)
- 新 revision: `yoshilover-fetcher-00163-fej`
- smoke tag URL: `https://roster-pregame-smoke---yoshilover-fetcher-n5hunzkyna-an.a.run.app`
- traffic: `00163-fej = 100%`
- 旧 live revision: `00161-sor` は revision list に残存

## smoke

- `GET /health`
  - smoke tag URL: `200`
- no-traffic revision の `/run`
  - 一時 Scheduler job `roster-pregame-smoke-20260419` を作成して tag URL へ `POST /run`
  - tag URL 側は `403`（Cloud Run request log 上でも `revision=00163-fej`, `status=403` を確認）
  - 既知の OIDC 制約として扱い、traffic 切替後に既存 authorized job `giants-weekend-pre` で live smoke を実施
- 一時 Scheduler job は確認後に削除済み

## traffic 切替後確認

- `gcloud run services update-traffic --to-latest` 実施済み
- current 100% traffic revision: `yoshilover-fetcher-00163-fej`
- `giants-weekend-pre` 手動実行
  - Cloud Scheduler execution: `AttemptFinished`, `HTTP 200`
  - live revision `00163-fej` の request log:
    - `POST https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/run`
    - `status=200`
    - `latency=85.084285649s`

## #9 pregame_started INFO log

- `00163-fej` で `pregame_started_skip` の INFO log を確認
- payload には `title / first_pitch_time / now / game_state / game_ended` を含む

### 例

```json
{
  "event": "pregame_started_skip",
  "title": "【一軍】巨人 vs ヤクルト 4/19(日)18時試合開始予定 明治神宮野球場",
  "post_url": "https://twitter.com/TokyoGiants/status/2045479878422856158",
  "first_pitch_time": "",
  "now": "2026-04-19T13:55:27.773425+09:00",
  "game_state": "見どころ",
  "game_ended": false
}
```

## #15 roster alias signal の runtime 観測

- 専用の `roster_hit` log は未追加のため、runtime では「GIANTS_KEYWORDS がないタイトルが article 化された」事実で確認
- `00163-fej` の live run で、以下のタイトルが article 化されていることを確認
  - `戸郷翔征が新フォームで初回３者凡退 最速１５０キロと好発進…ファーム・リーグ…`
    - `farm_body_template_applied`
    - `publish_disabled_for_subtype`
    - `[WP] 既存記事を再利用 post_id=62720`
  - `佐々木俊輔「必要とされる人間でありたい」 実戦で何を見せるか`
    - `social_body_template_applied`
    - `x_post_ai_generated`
- 上記 2 タイトルは `巨人 / ジャイアンツ / 東京ドーム / Giants / TokyoGiants` を含まず、roster alias signal を含むケース

## run summary

- `00163-fej` の `rss_fetcher_run_summary`
  - `drafts_created=7`
  - `error_count=0`
- `rss_fetcher_flow_summary`
  - `prepared_subtype_counts = {"farm":3,"farm_lineup":1,"manager":2,"player":2,"pregame":7}`
  - `created_subtype_counts = {"farm":3,"farm_lineup":1,"manager":2,"player":1}`
  - `skip_reasons.pregame_started = 7`
  - `skip_reasons.thin_source_fact_block = 1`

## audit-notify-6x

- Cloud Scheduler `audit-notify-6x` 手動実行
  - `00163-fej` で `audit_notify_completed`
  - `HTTP 200`
  - `total=0`
  - `mail_sent=false`

## 補足

- `roster-pregame-smoke` tag は live revision `00163-fej` に残っている
- tag URL の `/run` に対する OIDC 403 は継続課題だが、live service URL 経由の authorized scheduler では問題なく実行できた
