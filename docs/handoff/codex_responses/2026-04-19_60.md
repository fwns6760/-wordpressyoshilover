# 2026-04-19_60

## changed files

- `src/rss_fetcher.py`
  - `_game_strict_material_boundary_intro()` を追加
  - `_build_game_strict_prompt()` の `lineup` / `postgame` / `pregame` 3 分岐だけで、冒頭導入文を game 専用版へ差し替え
  - 変更内容
    - `source / 材料 にない事実・数字・比較・推測は書かない` を明示
    - `感想は締めの1文だけに限定し、source 事実ベースの短いファン視点` を明示
  - `OPINION_MARKERS` / `OPINION_MARKER_STEMS`、chain-of-reasoning 指示、各 subtype の構造は未変更
- `tests/test_gemini_prompt.py`
  - game prompt 2 ケース (`lineup` / `postgame`) で、冒頭に
    - `感想を足さない` が無いこと
    - `source / 材料 にない事実・数字・比較・推測は書かない` があること
    - `感想は締めの1文だけ` があること
    を確認する unittest を追加

## test result

- 実行コマンド
  - `python3 -m unittest discover -s tests -v`
- 結果
  - `Ran 436 tests in 1.027s`
  - `OK`

## remaining risk

- 変更対象は game strict prompt の 3 分岐だけで、manager / farm / social / player 系 strict prompt の冒頭文は触っていない
- code diff としては helper 追加 + 3 call site 置換が入っているが、rendered prompt の差分は game 系 3 分岐の冒頭導入文に限定される

## open question

- なし
