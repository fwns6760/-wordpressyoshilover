# 2026-04-19_62

## changed files
- `src/rss_fetcher.py`
  - `ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME` を strict postgame path に wire in
  - postgame parts prompt 呼び出し、Flash 応答 parse/validate、`render_postgame(...)`、`article_parts_applied` / `article_parts_fallback` log を追加
  - parts 成功時だけ renderer HTML を `build_news_block()` の本文へ差し込み、fallback 時は既存 `generate_article_with_gemini()` 経由へ戻す分岐を追加
- `tests/test_article_parts_wire_in.py`
  - new: 7 tests
  - flag OFF / applied / empty / parse fail / schema fail / render exception / non-postgame を mock ベースで固定

## diff summary
- commit `593bafa` (`Wire in postgame article parts renderer`)
- `src/rss_fetcher.py`: `590` lines changed (`436` insertions / `154` deletions)
- `tests/test_article_parts_wire_in.py`: new file, `226` lines

## 追加 test 件数 / 実行結果
- 新規 test: 7 件
- `python3 -m unittest tests.test_article_parts_wire_in -v`
  - `Ran 7 tests ... OK`
- `python3 -m unittest discover -s tests -v`
  - `Ran 449 tests in 0.954s ... OK`

## flag OFF 時の非破壊確認
- 方法
  - `tests/test_article_parts_wire_in.py::test_flag_off_keeps_legacy_route_and_skips_parts_helpers`
  - `build_news_block()` を postgame 入力で呼び、flag OFF 下で `generate_article_with_gemini()` が 1 回呼ばれること
  - `_request_gemini_strict_text()` / `_build_game_parts_prompt_postgame()` / `render_postgame()` が 1 度も呼ばれないこと
  - `blocks` に renderer 専用の `yoshilover-article-source` が入らず、legacy の `<h2>【試合結果】</h2>` path のままであること
- 結果
  - flag OFF では parts route に入らず、生成 HTML は既存 postgame path のまま
  - checked-out な 61 便コードとの別モジュール byte diff までは実施していないが、実際の route は完全に legacy 側へ固定できている

## fallback 4 条件の test 名
- empty: `test_flag_on_empty_response_falls_back_to_legacy_body`
- parse 失敗: `test_flag_on_json_parse_error_falls_back_and_logs_reason`
- schema 不一致: `test_flag_on_schema_mismatch_falls_back_when_body_core_is_not_list`
- render 例外: `test_flag_on_render_exception_falls_back_to_legacy_body`

## remaining risk
- deploy / Cloud Run 反映は未実施
- live の Gemini response で `source_attribution` がどの程度安定して埋まるかは未観察
- flag OFF の route 非破壊は test で確認済みだが、旧 commit 実体との別環境 byte 比較までは未実施

## open question
- なし

## commit id
- implementation: `593bafa`
