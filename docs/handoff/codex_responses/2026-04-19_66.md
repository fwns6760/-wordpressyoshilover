# 2026-04-19_66

1. changed jobs
`giants-postgame-catchup-am` を `0 7 * * *` / `Asia/Tokyo`、`giants-weekend-post-late` を `30 23 * * 0,6` / `Asia/Tokyo` で新規作成。既存 job の update / delete / pause / resume は未実施。

2. 実施内容
`gcloud scheduler jobs list` で 2 job 名が未存在であることを確認後、`giants-weekday-daytime` の `POST https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/run`、OIDC service account `seo-web-runtime@baseballsite.iam.gserviceaccount.com`、audience 同値、`attemptDeadline=180s`、retryConfig (`min=5s` / `max=3600s` / `doublings=5` / `duration=0s`) をそのまま使って create した。即時 `jobs run` はしていない。

3. 確認結果
`gcloud scheduler jobs list` で両 job とも `ENABLED` を確認。`gcloud scheduler jobs describe` で `httpTarget.uri` / `httpMethod=POST` / OIDC auth / retryConfig / `timeZone=Asia/Tokyo` は `giants-weekday-daytime` と一致し、差分は schedule のみだった。

4. remaining risk
本便では read-only 確認までで、実 fire は未観測。最初の実行は `giants-weekend-post-late` が次の土日 `23:30 JST`、`giants-postgame-catchup-am` が毎日 `07:00 JST` になる。

5. 次に Claude が判断すべきこと
次の実 fire 後に Cloud Scheduler / Cloud Run logs を見て postgame 回収が意図どおり増えたかだけ監査し、追加調整が不要ならこの件を close 扱いにしてよい。
