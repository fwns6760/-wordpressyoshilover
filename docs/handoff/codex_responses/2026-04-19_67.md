# Codex response — 2026-04-19 第67便

1. **flag OFF 状態の確認結果**
- Cloud Run `yoshilover-fetcher` の 100% traffic revision は `yoshilover-fetcher-00165-yaw`。env 一覧に `ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME` は存在せず、`src/rss_fetcher.py:692-693` の既定 `False` が効いている
- 同 revision には `STRICT_FACT_MODE=1` と `GEMINI_API_KEY` があるため、postgame parts path の未開通要因は実質この flag だけ
- live logs 観測窓は `2026-04-12 17:25:04 JST` から `2026-04-19 17:25:04 JST`。この 7 日間で `article_parts_applied=0`、`article_parts_fallback=0`
- `article_parts_fallback` の内訳も `empty=0 / parse_error=0 / schema_mismatch=0 / render_exception=0`
- 現行 revision `00165-yaw` に絞った直近 24 時間でも `article_parts_applied=0`、`article_parts_fallback=0`

2. **直近 postgame 生成の実績件数**
- 同じ 7 日窓で `rss_fetcher_flow_summary` は `118` run
- `created_subtype_counts.postgame` の合計は `57` 件、postgame を 1 件以上作成した run は `31` 本
- 直近 3 run（`2026-04-19 17:02 / 17:11 / 17:21 JST`）はすべて `prepared_subtype_counts.postgame=1` だが `created_subtype_counts.postgame=0`
- 最後に postgame 作成が出たのは `2026-04-19 09:16 JST` で、`created_subtype_counts={"farm":1,"general":2,"lineup":3,"manager":1,"player":1,"postgame":1}`

3. **preflight チェックリスト**
- `_maybe_render_postgame_article_parts()` の gate は `src/rss_fetcher.py:3919-4018`、通常本文への接続点は `src/rss_fetcher.py:7301-7315`、HTML renderer は `src/article_parts_renderer.py:74-97`
- 1. Gemini 2.5 Flash の dryrun で、返却が code fence なし JSON object かつ `title / fact_lead / body_core / game_context / fan_view / source_attribution` を満たすか確認する
- 2. `fan_view` は prompt schema でも validator でも実質必須 (`src/rss_fetcher.py:3722`, `3888`, `3991-3999`) だが、型定義では optional (`src/article_parts_renderer.py:20-21`) のため、missing/empty が即 `schema_invalid` になる前提を共有する
- 3. live 監視の reason bucket は `empty_response -> empty`、`json_parse_error:* -> parse_error`、`schema_invalid:* -> schema_mismatch`、`render_error:* -> render_exception` に固定して見る
- 4. canary は postgame 限定の自然発火窓だけを見る。平日なら `giants-weekday-post` (`18:00-23:30 JST`)、週末なら `giants-weekend-post` / `giants-weekend-eve` / `giants-weekend-post-late` と `giants-postgame-catchup-am` (`07:00 JST`) を観測対象にする
- 5. 当日は `article_parts_applied` / `article_parts_fallback` と `audit-notify-6x` の `no_opinion / thin_body / title_body_mismatch` を同時監視する
- 6. rollback コマンドを先に確定しておく: `gcloud run services update yoshilover-fetcher --project baseballsite --region asia-northeast1 --update-env-vars ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME=0`
- 7. go 条件と stop 条件を先に固定する。go は canary 日に fallback 0 かつ audit 悪化なし、stop は `schema_invalid` / `render_error` の発火、または `thin_body` / `title_body_mismatch` / `no_opinion` の同日悪化

4. **次便(flag ON canary 実施便)骨子**
- `ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME=1` を 1 日だけ入れ、scheduler は触らず、自然発火した postgame run のみを観測する
- 観測終了点は当日夜の postgame 窓 + 翌朝 `giants-postgame-catchup-am` の 1 回まで。そこで `article_parts_applied` と audit-notify-6x を見て、継続か OFF 戻しを判断する
- rollback trigger は最初の `article_parts_fallback` 群、とくに `schema_invalid` / `render_error`。加えて同日中に `thin_body` / `title_body_mismatch` / `no_opinion` が増えたら即 OFF に戻す
- rollback は revision promote ではなく env を `0` に戻す 1 行で足りる

5. **次に Claude が判断すべきこと / user に上げる 1 件**
- Claude が user に上げる論点は 1 件だけでよい: `ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME=1` を「1 日限定・postgame 観測専用 canary」として実施するか
- user への聞き方は `go / stop` の 2 択で十分。推奨文面は「postgame 限定で 1 日だけ flag を ON にし、fallback 1 件または audit 悪化で即 OFF に戻す canary を実施するか」
