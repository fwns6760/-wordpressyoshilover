# Codex response — 2026-04-19 第69便

1. **採用案**
- **A-1**: `lineup` / `farm_lineup` を create 直前で先頭へ寄せる安定ソートを採用。`drafts_created=10` はそのまま、他 subtype の相対順も壊さずに late lineup を救えるため
- **B-2**: `lineup` だけ `close_marker` 必須を外した。68便で特定した fail は `2026-04-19 17:30:29 JST` の `@hochi_giants` lineup (`2045780007533072789`) で、local log には本文末尾は残っていないが、現行 lineup 正規化/安全フォールバックの終端は `...コメントで教えてください！` 系で終わりやすく、`POST_GEN_CLOSE_MARKERS` (`気になります` / `注目です` / `見たいところです` / `と思います`) が 1 つも入らない path が実在する。欠落 marker は特定の 1 個ではなく「4 候補すべて未出現」と判断した

2. **changed files**
- `src/rss_fetcher.py:5888-5902` `post_gen_validate` に `article_subtype` を渡せるようにし、`lineup` だけ `close_marker` 軸を免除
- `src/rss_fetcher.py:8583-8593` ` _prioritize_prepared_entries_for_creation()` を追加し、`lineup` / `farm_lineup` を create 対象の先頭へ寄せる helper を新設
- `src/rss_fetcher.py:10224-10224,10379-10379` create ループ直前で priority helper を適用し、validator 呼び出しへ `title_article_subtype` を渡すよう修正
- `tests/test_post_gen_validate.py:52-83` lineup 緩和 1 件、non-lineup 維持 1 件を追加
- `tests/test_lineup_create_priority.py:1-84` create 優先順 2 件を追加

3. **追加 test 件数 / 実行結果**
- 追加 test は **4 件**
- 先行確認: `python3 -m unittest tests.test_post_gen_validate tests.test_lineup_create_priority -v` → **7 tests OK**
- 全件確認: `python3 -m unittest discover -s tests -v` → **453 tests OK**

4. **remaining risk**
- 17:30 fail 本文そのものは local log に残っていないため、末尾は code path からの推定。実運用で `lineup` final section が空に近い異常本文まで通していないかは deploy 後の実ログ確認が必要
- A-1 は create 順のみ修正なので、`pregame_started_skip` 由来の `prepared=0` run には効かない

5. **次に Claude が判断すべきこと**
- **go 条件**: deploy 便でこの commit を反映し、次の late lineup 窓で `created_subtype_counts.lineup` または `farm_lineup` が limit 飽和 run でも 1 件以上残ること。加えて `article_skipped_post_gen_validate` の lineup `close_marker` 再発が 0 件であること
- **stop 条件**: deploy 後に lineup が 0 件のままなら、次便は create 優先ではなく `pregame_started_skip` 側を触る。逆に lineup 本文品質が崩れるなら B-2 を revert して B-1 へ切り替える
