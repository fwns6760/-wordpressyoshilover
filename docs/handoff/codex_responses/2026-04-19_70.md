# Codex response — 2026-04-19 第70便

1. 新 revision は `yoshilover-fetcher-00167-jan`。Cloud Build `ad123e41-78ba-4d56-95e8-e480752d600f` は `SUCCESS`、revision は `Ready`。preflight は `HEAD=938cc1c`、live 100% は開始時 `00165-yaw`、`ENABLE_ARTICLE_PARTS_RENDERER_POSTGAME` は Cloud Run env に存在せず未設定のままを確認。

2. smoke test は OK。tag URL `https://lineup-priority-smoke---yoshilover-fetcher-n5hunzkyna-an.a.run.app/health` は `200`。`/run` は paused の `yoshilover-fetcher-job` を一時的に tag URI へ向け、OIDC audience は live `/run` のままで manual run して `200`。`rss_fetcher_run_summary`: `drafts_created=10`, `error_count=0`。`rss_fetcher_flow_summary`: `created_subtype_counts={\"farm\":2,\"farm_lineup\":1,\"general\":2,\"lineup\":3,\"manager\":1,\"player\":1}`。`close_marker` skip は `postgame` 1 件のみで `lineup` / `farm_lineup` は 0 件。

3. 100% traffic 切替は `CLOUDSDK_CONFIG=/tmp/gcloud-config gcloud run services update-traffic yoshilover-fetcher --project baseballsite --region asia-northeast1 --to-revisions=yoshilover-fetcher-00167-jan=100` で実施済み。`gcloud run services describe` で `latestCreatedRevisionName=latestReadyRevisionName=yoshilover-fetcher-00167-jan`、traffic `00167-jan=100%`、旧 `00165-yaw` は 0% 残置を確認。

4. 切替後の即時観察は OK。manual live `/run` は `2026-04-19T09:12:22Z` に `200`、`rss_fetcher_flow_summary` は `created_subtype_counts={\"farm\":5,\"farm_lineup\":1,\"general\":2,\"manager\":1,\"player\":1}` で `lineup=0`, `farm_lineup=1`、`close_marker` skip は `postgame` 1 件 + `farm` 1 件、`lineup` / `farm_lineup` 再発は 0。次の自然発火 `giants-weekend-eve` (`2026-04-19T09:30:03Z` request `200`) でも `rss_fetcher_flow_summary` は `created_subtype_counts={\"farm\":5,\"farm_lineup\":1,\"general\":2,\"manager\":1,\"player\":1}`、`lineup=0`, `farm_lineup=1`、`close_marker` skip は `postgame` 2 件 + `farm` 1 件で `lineup` / `farm_lineup` 再発は 0。

5. rollback: `CLOUDSDK_CONFIG=/tmp/gcloud-config gcloud run services update-traffic yoshilover-fetcher --project baseballsite --region asia-northeast1 --to-revisions=yoshilover-fetcher-00165-yaw=100`
