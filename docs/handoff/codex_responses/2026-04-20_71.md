# Codex response — 2026-04-20 第71便

1. `giants-weekend-post-late` の 2026-04-19 23:30 JST 初 fire は成功。Cloud Scheduler は `23:30:06 JST AttemptStarted` → `23:31:26 JST AttemptFinished` / HTTP 200、Cloud Run `yoshilover-fetcher-00167-jan` も `23:30:00 JST` の `/run` が 200。`rss_fetcher_flow_summary` (`23:31:24 JST`) は `prepared_subtype_counts={"farm":2,"general":1,"manager":2,"player":1,"postgame":3,"pregame":7}` に対し `created_subtype_counts={"farm":2,"general":1,"manager":2,"player":1}` で、`created_subtype_counts.postgame` は出ず。同 run では `article_skipped_post_gen_validate(fail_axis=[close_marker])` が `postgame` で 2 件 (`Fedは何を見せたか` / `橋上秀樹の試合後コメントをどう見るか`)。比較用に `2026-04-18 23:00-24:00 JST` の `/run` request log を見ると 0 件で、今回 fire 自体は穴埋めできたが、23:00-24:00 帯の postgame 回収増はまだ確認できない。

2. `giants-postgame-catchup-am` の 2026-04-20 07:00 JST catchup fire も成功。Cloud Scheduler は `07:00:04 JST AttemptStarted` → `07:02:04 JST AttemptFinished` / HTTP 200、Cloud Run `/run` は `07:00:00 JST` に 200。`rss_fetcher_flow_summary` (`07:01:56 JST`) は `prepared_subtype_counts={"farm":2,"manager":2,"pregame":8}`、`created_subtype_counts={"farm":2,"manager":2}` で、postgame create は 0、他 subtype create は 4。`skip_reasons.stale_postgame=2` が出ており、前夜 23:30 run で取りこぼした postgame は翌朝 catchup でも回収できていない。

3. `yoshilover-fetcher-00167-jan` の live cutover 後 (`2026-04-19 18:12 JST` の最初の live `/run` 以降) から `2026-04-20 11:00 JST` までを確認。live `/run` は 14 回すべて HTTP 200。`article_skipped_post_gen_validate(fail_axis=[close_marker])` は `postgame=14`, `farm=3`, `pregame=19`, `lineup=0`, `farm_lineup=0` で、**lineup / farm_lineup の close_marker 再発は 0 件継続**。同じ live 窓の `rss_fetcher_flow_summary` 合算では `created_subtype_counts.lineup=4`, `created_subtype_counts.farm_lineup=4` を確認した。`lineup` / `farm_lineup` は作成実績を維持している一方、今回追加した 23:30 / 07:00 の新 job では postgame 回収は増えていない。
