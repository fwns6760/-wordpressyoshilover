## 1. commit 実施

- hash: `563cb1d23d19874ecff7e18212d89ffb6e8e0359`
- message: `fix(rss_fetcher): tighten subtype boundary so lineup prefix does not leak into live_update/postgame (reject 6本 reproduction)`
- 対象 file:
  - `src/rss_fetcher.py`
  - `tests/test_lineup_subtype_boundaries.py`
- `git log -1 --stat` でも上記 2 file のみを確認。75便 scope 外の dirty file は commit していない。

## 2. pre-deploy sanity

- `PYTHONPATH=. pytest -q tests/test_lineup_subtype_boundaries.py tests/test_title_rewrite.py tests/test_cost_modes.py`
  - `57 passed, 3 warnings, 34 subtests passed`
- `PYTHONPATH=. pytest -q tests`
  - `455 passed, 3 warnings, 155 subtests passed`
- warning は既知の vendor 由来 (`feedparser` / `requests` / `certifi`) のみ。

## 3. deploy 実施内容

- `gcloud run deploy` は最初 `~/.config/gcloud` が read-only で失敗したため、既存 config を `/tmp/gcloud-codex-76` に複製し `CLOUDSDK_CONFIG=/tmp/gcloud-codex-76` で再実行。
- no-traffic deploy:
  - service: `yoshilover-fetcher`
  - new revision: `yoshilover-fetcher-00156-865`
  - 0% tag: `codex-76-smoke`
- smoke:
  - ローカル DNS 不可で tagged `run.app` へ直接 `curl` 不可
  - `cloud-run-proxy` も component manager 無効で導入不可
  - 代替として Cloud Run revision 状態と起動ログを確認
    - revision status: `Ready=True`, `Active=True`
    - log: `Starting new instance` / `起動: port=8080` / `Default STARTUP TCP probe succeeded`
- traffic 移行:
  - `gcloud run services update-traffic yoshilover-fetcher --to-revisions=yoshilover-fetcher-00156-865=100`
  - rollback は未実施

## 4. post-deploy 観察

- `gcloud scheduler jobs run giants-weekday-daytime` を traffic 移行後に手動 fire。
- Cloud Scheduler:
  - `AttemptStarted`: `2026-04-20T04:03:23Z`
  - `AttemptFinished`: HTTP `200` (`URL_CRAWLED. Original HTTP response code number = 200`) at `2026-04-20T04:05:03Z`
- new revision run summary:
  - `=== 完了: 取得=186 / 投稿=5 / 重複スキップ=3 / フィルタスキップ=118 / エラー=0 ===`
- observed subtype / title samples:
  - `farm` → `巨人二軍 7-4 結果のポイント`
  - `pregame` → `石塚裕惺先発でどこを見たいか` / `巨人DeNA戦 戸郷翔征先発でどこを見たいか`
  - `manager` → `杉内俊哉コーチ「思い出いっぱい」 ベンチの狙いはどこか`
- reject / 異常:
  - `article_skipped_post_gen_validate` は 3件、すべて `article_subtype="pregame"` + `fail_axis=["close_marker"]`
  - `エラー=0`
  - 観測ログ内で `巨人スタメン` 漏れは 0件
  - この fire では `lineup / live_update / postgame` の実サンプルは出ていない

## 5. remaining risk + 次に Claude が判断すべきこと

- 今回の deploy 後 fire では `lineup / live_update / postgame` の実データが流れていないため、75便の主対象は test + deploy 後 run で未再現のまま。次の該当ソース流入時に `article_subtype` と `rewritten_title` を追加観察したい。
- no-traffic smoke は HTTP 到達ではなく `Ready/Active + startup probe/log` の代替確認。DNS / proxy 制約が消えたら tagged revision に対する `GET /health` か認証付き `/run` smoke を取り直す余地がある。
- `git stash pop` は `.codex already exists` で exit 1 になったが、tracked dirty file と報告先 file 群は復元済み。stash entry は残っているので、不要確認後に Claude 側で drop 判断をしてよい。
