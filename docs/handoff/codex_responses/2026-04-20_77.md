# 77便 本文品質監査

## 1. 対象5本の output

前提: Cloud Scheduler `AttemptStarted` は `2026-04-20T04:03:23Z` で、JST では `2026-04-20 13:03:23`。`next_action.md` の「04:03 JST」とは 9 時間ずれる。Cloud Logging 上、この便の 5 output は **publish ではなく全件 draft-only** (`publish_disabled_for_subtype`)。WP REST は DNS 不可だったため、category / title / subtype / 時刻は logging、tag は `取得不可`、本文冒頭は `preview_text` または template/log からの代用で監査した。5 output のうち **4 unique post ID** で、`62795` が同一 fire 内で 2 回再利用された。

- `62795` | `2026-04-20 13:03:33 JST` | `farm` | category=`ドラフト・育成` | tag=`取得不可` | 本文冒頭代用=`【二軍結果・活躍の要旨】 巨人二軍の試合は7-4という結果でした。投手は7回を投げ3失点…` | 6軸=`時系列:ok / mismatch:ok / 1核:ok / 薄さ:ok / AI tone:suspect / X付与:ok` | 評価=`publish 可` | 根拠=`numeric_count=6` で数字は足りる。`@TokyoGiants` own_source 埋め込みあり。AI tone は本文実文未取得でテンプレ末尾の残存を否定できないため suspect。
- `62795` | `2026-04-20 13:03:44 JST` | `farm` | category=`ドラフト・育成` | tag=`取得不可` | 本文冒頭代用=`【二軍結果・活躍の要旨】 巨人二軍の試合は7-4という結果でした。同点の6回に勝ち越し…` | 6軸=`時系列:ok / mismatch:ok / 1核:ok / 薄さ:ok / AI tone:suspect / X付与:ok` | 評価=`publish 可` | 根拠=`numeric_count=3` で最低限の具体性は維持。same `post_id=62795` 再利用で 1 本目を上書きしており、本文品質より運用上の重複上書きが気になる。
- `62984` | `2026-04-20 13:04:12 JST` | `manager` | category=`首脳陣` | tag=`取得不可` | 本文冒頭代用=`■ドラ2田和廉 好救援の裏側 / 杉内コーチ「思い出いっぱい」 / ※（質問内容不明）…` | 6軸=`時系列:suspect / mismatch:fail / 1核:fail / 薄さ:suspect / AI tone:fail / X付与:ok` | 評価=`reject` | 根拠=`title` は「ベンチの狙い」だが preview は田和廉の好救援と不明 placeholder が中心。`※（質問内容不明）` は AI tone fail。quote 文脈と選手評が分離し 1 記事 1 核が崩れている。
- `62978` | `2026-04-20 13:04:24 JST` | `manager` | category=`首脳陣` | tag=`取得不可` | 本文冒頭代用=`杉内コーチ「前回より全然よかった。ナイスピッチングと僕は思う」 / 今日の試合で井上温大投手が2敗目を喫しましたが…` | 6軸=`時系列:ok / mismatch:ok / 1核:ok / 薄さ:ok / AI tone:suspect / X付与:ok` | 評価=`publish 可` | 根拠=`quote -> 井上温大の登板内容 -> 今季成績` の流れは揃っている。具体数字も十分。AI tone は preview の末尾が説明書き調で少し単調。
- `63011` | `2026-04-20 13:04:39 JST` | `pregame` | category=`試合速報` | tag=`取得不可` | 本文冒頭代用=`■ジャイアンツ球場 先発投手陣の調整 / サンスポ巨人Xによると、ジャイアンツ球場では先発投手が…` | 6軸=`時系列:suspect / mismatch:suspect / 1核:suspect / 薄さ:fail / AI tone:fail / X付与:ok` | 評価=`reject` | 根拠=`game_body_template_applied numeric_count=0 name_count=1`。title は「何を見たいか」だが本文代用は一般化された調整話に寄り、具体事実が薄い。X は `@sanspo_giants` + `@hochi_baseball` の 2 本で適切。

## 2. fail reason 上位3つ

- `AI tone` × `2件` | 代表=`62984` | `※（質問内容不明）` と「ベンチの狙いはどこか」「何を見たいか」型の汎用化が残る。
- `title-body mismatch` × `1件` | 代表=`62984` | title の主語は杉内コメントだが、本文代用の重心は田和廉の好救援に寄っている。
- `thin_body` × `1件` | 代表=`63011` | `numeric_count=0`。練習・調整の総論が多く、固有事実が足りない。

## 3. subtype 別傾向

- `farm` | 2 output とも score 核 + own_source X は安定。品質は許容だが、同一 `post_id=62795` が同一 fire 内で 2 回再利用され、5 output / 4 unique ID になっている。
- `manager` | `62978` は quote と投手内容が揃って安定。`62984` は quote の文脈が薄いと player 寄り説明へ流れやすい。加えて publish log では `article_subtype=social`、body/X log では `manager` で揺れている。
- `pregame` | `63011` は具体事実不足で generic 化。さらに同じ fire で pregame 3 本が `close_marker` で WP 到達前に落ちており、生成と gate の両方が不安定。
- `他` | 今回の 5 output に `lineup / live_update / postgame / player` は入っていないため観察不能。

## 4. 修正優先順位

- `1. title assembly` | `62984` と `63011` の generic title / 本文重心ずれが直撃している。
- `2. subtype 境界` | `manager` 本文なのに publish log が `social`。`farm` は同一 ID 再利用 2 回で観測が濁る。
- `3. close_marker` | 同 fire で pregame 3 本が `article_skipped_post_gen_validate`。公開以前に落ちる量が多い。
- `4. 本文ブロック順` | `pregame` が総論先行で具体 fact が後ろに回り、薄さを助長している。
- `5. X 付与` | 今回の 5 output は全件 own_source / 主要媒体 X あり。優先度は低い。
- `6. postgame template` | 今回 sample 0。次の postgame 実データ観察便で判断でよい。

## 5. user が確認すべき記事

- `62984` | reject 代表。quote 文脈不足、title-body mismatch、AI tone fail が一度に見える。
- `63011` | reject 代表。pregame の薄さと generic 化が最もはっきり出ている。
- `62978` | publish 可の代表。manager 系で現状もっとも安定している。
- `62795` (`13:03:33 JST`) | farm の許容ライン確認用。数字と own_source が揃った安定側。
- `62795` (`13:03:44 JST`) | 同一 ID 再利用の代表。品質より「5 output / 4 unique ID」問題の確認対象。
