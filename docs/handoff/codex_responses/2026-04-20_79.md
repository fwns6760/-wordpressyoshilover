# Codex response — 2026-04-20_79

## Changed Files

- `src/wp_client.py`
  - `source_url` / custom meta ベースの draft reuse 判定、reuse reason log、meta backfill を追加
- `src/rss_fetcher.py`
  - same-fire guard helper を追加し、draft 作成時に `source_url` を必ず渡すよう変更
- `tests/test_wp_client.py`
  - `source_url` 一致 / 不一致 / title fallback / 新規 draft meta 保存の unittest を追加
- `tests/test_duplicate_prevention_golden.py`
  - same-fire distinct source split と same-source retry reuse の golden test を追加
- `docs/handoff/tickets/OPEN.md`
  - `T-025` を追記
- `docs/handoff/codex_responses/2026-04-20_79.md`
  - 本 response

## 実施内容

- `find_recent_post_by_title()` に `source_url` / `allow_title_only_reuse` を追加し、`source_url` 完全一致時だけ reuse、明示 opt-in のときだけ title-only fallback を許可する形に変更
- 新規 draft / post 作成時は `_yoshilover_source_url` meta を保存し、reuse 対象に meta が無い場合は `_reuse_existing_post()` で backfill するよう変更
- reuse log に `reuse_reason=source_url_match` / `reuse_reason=title_fallback` を付加
- `rss_fetcher` では same-fire の `rewritten_title -> source_url set` を保持し、別 source が同一 rewritten title に入った時だけ `same_fire_distinct_source_detected ...` を出しつつ、`wp.create_post(..., source_url=..., allow_title_only_reuse=False)` に統一

## Unittest

- 実行コマンド: `python3 -m unittest discover -s tests -v`
- 結果: `Ran 462 tests in 1.225s`
- 結果: `OK`
- 追加ケース:
  - `source_url` 一致時は既存 draft を reuse
  - `source_url` 不一致時は title 一致でも新規 draft を作成
  - `allow_title_only_reuse=True` でのみ title fallback を許可
  - same-fire で別 source / 同 title は post_id が分かれ、同一 source retry は reuse 維持

## Canary Deploy / Smoke

- deploy コマンド:
  - `CLOUDSDK_CONFIG=/tmp/gcloud-config-79 gcloud run deploy yoshilover-fetcher --source . --project baseballsite --region asia-northeast1 --no-traffic --tag=dup-fix-canary --quiet`
- build / revision:
  - build id: `35bbe7a8-2a2e-4b05-b4dc-d25a32f59ef6`
  - new revision: `yoshilover-fetcher-00172-lim`
  - tag URL: `https://dup-fix-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app`
- temporary Cloud Scheduler smoke:
  - temp job: `dup-fix-smoke-20260420-062840`
  - target: `POST https://dup-fix-canary---yoshilover-fetcher-n5hunzkyna-an.a.run.app/run`
  - OIDC audience: `https://yoshilover-fetcher-487178857517.asia-northeast1.run.app/run`
  - `AttemptStarted`: `2026-04-20T06:30:55.847426575Z`
  - `AttemptFinished`: `2026-04-20T06:32:55.884719738Z`
  - scheduler result: `HTTP 200` (`URL_CRAWLED. Original HTTP response code number = 200`)
  - Cloud Run request log: `2026-04-20T06:30:54.839228Z` / `POST` / `status=200` / `latency=111.722229113s` / `userAgent=Google-Cloud-Scheduler`
  - run summary: `drafts_created=6`, `skip_duplicate=3`, `skip_filter=118`, `error_count=0`
  - flow summary: `created_subtype_counts={"farm":3,"general":1,"manager":1,"pregame":1}`
- temp job cleanup:
  - `gcloud scheduler jobs delete dup-fix-smoke-20260420-062840 --project baseballsite --location asia-northeast1 --quiet`
- note:
  - canary revision spec に `ARTICLE_INJECT_TEAM_STATS=1` が既に含まれており、smoke log でも `article_team_stats_injected` を観測した。この便では env を変更していないため、現行 service template 由来の状態として記録だけ残す
  - prod promote は未実施。`dup-fix-canary` は 0% のまま残置

## Commit / Push

- local commit:
  - `c7c1b31 fix(wp_client): require source_url match for draft reuse (same-fire guard, T-025)`
- `git push origin HEAD`
  - 1回目: `ssh: Could not resolve hostname github.com: Temporary failure in name resolution`
  - 2回目: 同エラーで失敗
  - この環境から GitHub へは未反映
