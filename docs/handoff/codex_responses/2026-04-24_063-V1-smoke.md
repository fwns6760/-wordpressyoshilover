# 2026-04-24_063-V1-smoke

## summary

- 063-V1 front impl を live deploy 済み
- hub 配置は `(a)` で確定: SWELL `front_top` に `[yoshilover_topic_hub]`
- plugin / 追加 CSS / topic hub / 検証用 meta / WP 実機 smoke まで完了
- 判定: `E-1〜E-5 all pass`

## deploy

- plugin live 反映: `src/yoshilover-063-frontend.php`
- SWELL 追加 CSS 反映: `src/custom.css` の `063 — topic hub / 記事下 SNS 反応 block` セクション
- topic hub items を live option に投入
- `front_top` に shortcode widget を前置
- cache clear 実施

## smoke results

- `E-1` pass
  - `yoshi-topic-hub` が `w-frontTop` 内に出ており、`p-homeContent` より前
- `E-2` pass
  - `62965` は `yoshi-sns-reactions` 表示
  - `62962` は meta 無しで非表示
- `E-3` pass
  - `62965` の SNS block は fallback link で表示
  - X oEmbed 不成立でも空 block にならない
- `E-4` pass
  - `replytocom` URL と一時 page-flag の両方で `<meta name="robots">` は 1 本
  - content は `max-image-preview:large, noindex, follow`
- `E-5` pass
  - 記事下順序は `related-posts -> yoshi-sns-reactions -> shareBtns -> comments`

## notes

- robots / noindex は `wp_robots` 経由で 1 本統合を実測確認
- 検証用 page flag は確認後に削除済み
- スクリーンショットは未取得。HTML 実測で十分
- 063-V1 は accept / close 候補へ昇格可

