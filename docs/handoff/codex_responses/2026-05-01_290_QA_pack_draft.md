# 290-QA weak title rescue deploy judgment Pack draft

Date: 2026-05-01 JST  
Lane: Codex B (doc-only)  
Scope: deploy judgment pack draft only, no src/tests/env/gcloud/WP mutation

## 1. 既存状態

- `c14e269` は `master` に含まれている(`git branch --contains c14e269` = `master`)。
- 290-QA の repo 状態は **PUSHED / GH Actions green / pytest baseline 維持**として扱われている。
  - `doc/active/290-QA-weak-title-rescue-backfill.md`: 完了条件に `GH Actions Tests workflow = success` を明記。
  - `docs/ops/OPS_BOARD.yaml`: `commit c14e269 PUSHED / GH Actions run success / pytest 1865 baseline 維持` を 290 deploy evidence として保持。
  - `docs/handoff/session_logs/2026-05-01_ops_reset.md`: `c14e269 PUSHED / GH green` を 290 deploy の現状態として記録。
- 2026-05-01 の 298-Phase3 deploy preflight / hold-carry verify では、`c14e269` が `4be818d..ffeba45` commit range に含まれることを確認済み。
  - ただし `ENABLE_WEAK_TITLE_RESCUE` は未設定で、298 の deploy 対象は `publish-notice` だったため、290-QA の rescue は **live-inert** のまま。
  - 根拠: `docs/handoff/codex_responses/2026-05-01_codex_b_storm_permanent_fix.md` と `docs/ops/INCIDENT_LIBRARY.md` の hold-carry evidence。
- `docs/ops/OPS_BOARD.yaml` の 290 entry `next_action` はまだ「Acceptance Pack 13 項目作成」と書いてあるが、現行正本は `docs/ops/POLICY.md` §18 / `docs/ops/ACCEPTANCE_PACK_TEMPLATE.md` の **18 項目**。本 doc はその差し替え draft。
- 現時点の 290-QA は **code ready / live OFF / deploy judgment HOLD** と整理するのが妥当。

## 2. read-only 解析(`c14e269`)

### 2-1. commit 範囲

`git show --stat c14e269`:

- `src/weak_title_rescue.py` 新規
- `src/rss_fetcher.py`
- `src/title_validator.py`
- `tests/test_weak_title_rescue.py` 新規
- `doc/active/290-QA-weak-title-rescue-backfill.md`

合計 `5 files changed, 952 insertions`。実装の主眼は **regex / metadata only** の narrow rescue で、Gemini call site 追加や scheduler/env mutation は含まない。

### 2-2. env flag / integration

- `src/rss_fetcher.py`
  - `WEAK_TITLE_RESCUE_ENV_FLAG = "ENABLE_WEAK_TITLE_RESCUE"` を定義。
  - `_maybe_apply_weak_title_rescue(...)` は flag OFF 時に即 return するため、default OFF では live behavior 不変。
  - rescue 成功時のみ `weak_title_rescued` log を emit し、元の skip reason を保持したまま title を差し替える。
- `src/title_validator.py`
  - `no_strong_marker` 判定の前に `is_strong_with_name_and_event(...)` を参照。
  - **人名 + 具体イベント語の AND 条件**を満たす場合だけ narrow 例外で weak 判定を解除する。
- `src/weak_title_rescue.py`
  - `rescue_related_info_escape(...)`
  - `rescue_blacklist_phrase(...)`
  - `is_strong_with_name_and_event(...)`
  - safety block markers に `死亡 / 重傷 / 救急 / 意識不明 / ブルージェイズ / ロッキーズ / グッズ / NIKE / コジコジ` を含み、MLB/グッズ/重大事故系は rescue 対象外。

### 2-3. test coverage

`tests/test_weak_title_rescue.py` は以下を read-only で確認できる:

- A/B 群 7 候補の救済 path
  - 泉口友汰 `related_info_escape`
  - 山崎伊織・西舘勇陽 2 名併記
  - 阿部監督→竹丸 message
  - 平山功太 quote/event
  - 竹丸和幸 + 内海誕生日
  - 平山功太 `no_strong_marker` narrow 例外
- 救済しない path
  - グッズ
  - MLB
  - duplicate/hard-stop
  - missing-name invent 禁止
- fetcher integration
  - flag default OFF で no-op
  - flag ON で title rewrite + `weak_title_rescued` log
  - duplicate/review path 尊重

### 2-4. baseline evidence の扱い

- commit message 自体が「local pre-existing fails 不変」「deploy なし」「default OFF」を明記している。
- 290 deploy 向けの既存 evidence は **`pytest 1865 baseline 維持 + GH green`** として ops docs に残っている。
- さらに後続の 298-Phase3 preflight では、同 repo HEAD 上で `pytest -q = 2008 passed / 0 failed` が観測されており、少なくとも `c14e269` が後続 HEAD 上で新規 failure を増やした痕跡は見えていない。

## 3. deploy 判断 preconditions

| precondition | status | note |
|---|---|---|
| 298-Phase3 `ROLLED_BACK_AFTER_REGRESSION` 後 24h 安定 | pending | post-rollback observe が `2026-05-01 14:15 JST` のため、厳密な最短 gate は `2026-05-02 14:15 JST` 以降。user shorthand の `2026-05-02 14:00 JST` より 15 分遅い。 |
| 299-QA flaky close 判定 完了 | pending | `docs/handoff/codex_responses/2026-05-01_299_QA_flaky_analysis.md` は `OBSERVE` 維持推奨。close は `production_health_observe` と連動。 |
| `production_health_observe` `2026-05-01 17:00 JST` pass | pending | 289 silent skip / 282 flag OFF / 299 sub-observe / Team Shiny / mail budget の総合 read-only gate。 |
| Lane B Pack 完成 + Claude が user に提示可能 | this doc fulfills draft | doc 作成で Pack draft 自体は完成。実行には Claude review と user GO が残る。 |
| user GO | pending | category = `PROD_DEPLOY+FLAG_ENV`。 |

結論: **code は ready だが、deploy judgment は preconditions 未達のため HOLD**。

## 4. Acceptance Pack 18 項目 final draft

## Acceptance Pack: 290-QA-deploy

- **Decision**: `HOLD`
- **Requested user decision**: `yoshilover-fetcher` を rebuild/deploy し、`ENABLE_WEAK_TITLE_RESCUE=1` を apply して 290-QA rescue を live 化してよいか
- **Scope**: `c14e269` を含む `yoshilover-fetcher` service image rebuild 1 回 + service env `ENABLE_WEAK_TITLE_RESCUE=1` apply + deploy 後 read-only observe
- **Not in scope**: `publish-notice` / `guarded-publish` / Scheduler / Secret / Team Shiny From / SEO / X 自動投稿 / source 追加 / 282-COST / 293-COST / 298-Phase3 再設計 / 300-COST source-side fix
- **Why now**: A/B 群 7 候補(泉口 / 山崎+西舘 / 阿部 / 平山関連 3 / 竹丸+内海)の publish 機会回復が 290-QA の主目的で、しかも Gemini 不要の regex/metadata rescue なので cost-side blast radius が小さい。298 の incident 収束後に次の narrow deploy 候補として最も順序がよい
- **Preconditions**:
  - `298-Phase3` が `ROLLED_BACK_AFTER_REGRESSION` 後 24h 安定を通過すること(最短 `2026-05-02 14:15 JST`)
  - `299-QA` flaky が close または少なくとも `production_health_observe` で悪化なし確定であること
  - `production_health_observe` `2026-05-01 17:00 JST` が pass であること
  - 本 Pack を Claude が review し、user が `GO` を返せる状態で提示すること
- **Cost impact**: Gemini call `0` 増、token `0` 増、Cloud Build `1` 回、Cloud Run service deploy `1` 回。mail emit は rescue 成功分だけ **微増** の可能性あり
- **User-visible impact**: これまで `weak_subject_title:related_info_escape` / `weak_generated_title:blacklist_phrase` / `no_strong_marker` で落ちていた 7 類型が publish または review path に戻る。flag OFF の間は user-visible change `0`
- **Rollback**: 第1段は env rollback `gcloud run services update yoshilover-fetcher --region asia-northeast1 --project baseballsite --remove-env-vars=ENABLE_WEAK_TITLE_RESCUE`。第2段は image rollback `yoshilover-fetcher:4be818d` へ戻す。`publish-notice` は 290 deploy scope 外なので変更不要
- **Evidence**:
  - 事前 evidence: `c14e269` pushed / GH Actions success / pytest 1865 baseline 維持 / `git log 4be818d..ffeba45` に `c14e269` 含有 / `ENABLE_WEAK_TITLE_RESCUE` 未設定で live-inert 確認済
  - code evidence: `src/weak_title_rescue.py` 新規 helper、`src/title_validator.py` narrow 例外、`src/rss_fetcher.py` flag-gated integration、`tests/test_weak_title_rescue.py` positive/negative/integration coverage
  - deploy 後に要求する evidence: rescue candidate が publish/review path に流れる sample、Gemini call delta `0`、silent skip `0`、Team Shiny From 不変
- **Stop condition**: silent skip 増 / Gemini call delta `> 0` / rescue candidate がなお同じ weak-title path で fail / Team Shiny From 変化 / deploy 後 errors `> 0`
- **Expiry**: `2026-05-02 17:00 JST`。それまでに user judgment が無い場合は 298/299/production health を再確認して Pack refresh
- **Recommended decision**: `HOLD`
- **Recommended reason**: 290 の code/evidence 自体は十分だが、2026-05-01 の mail storm rollback 直後で 298 安定 24h と 299/17:00 観察が未了。現時点で GO にすると deploy 順序と incident boundary を崩す
- **Gemini call increase**: `NO`
- **Token increase**: `NO`
- **Candidate disappearance risk**: `NO`
- **Cache impact**: `NO`
- **Mail volume impact**: `YES`(微増。救済成功分が publish/review/mail path に戻るため)

User reply format: 「GO」/「HOLD」/「REJECT」のみ

## 5. rollback quick reference

### 5-1. 最短 rollback(env only)

```bash
gcloud run services update yoshilover-fetcher \
  --project baseballsite \
  --region asia-northeast1 \
  --remove-env-vars=ENABLE_WEAK_TITLE_RESCUE
```

### 5-2. image rollback(fetcher safe baseline)

```bash
gcloud run services update yoshilover-fetcher \
  --project baseballsite \
  --region asia-northeast1 \
  --image=asia-northeast1-docker.pkg.dev/baseballsite/yoshilover/yoshilover-fetcher:4be818d
```

### 5-3. safe-baseline note

- 298 work で safe baseline として参照されている旧 image family は `:4be818d`。
- 290 deploy は fetcher only scope。`publish-notice` を同時に動かす必要はない。

## 6. Claude next action

1. `production_health_observe` `2026-05-01 17:00 JST` 結果を追記して 299/298 precondition を更新。
2. `2026-05-02 14:15 JST` 以降、298 の 24h 安定が崩れていなければ本 Pack を user 提示用に昇格。
3. user `GO` 後のみ、deploy lane に `yoshilover-fetcher` rebuild + `ENABLE_WEAK_TITLE_RESCUE=1` apply を fire。
