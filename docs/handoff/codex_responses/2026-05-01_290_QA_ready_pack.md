# 290-QA ready pack

Date: 2026-05-01 JST  
Mode: Lane A round 15 / doc-only / read-only final consolidation  
Pack status: field-complete / user-reply-ready / precondition HOLD

## Decision Header

```yaml
ticket: 290-QA
recommendation: HOLD
decision_owner: user
execution_owner: Claude
risk_class: fetcher-title-rescue-visible-path
user_go_reason: PROD_DEPLOY
expires_at: 2026-05-02 14:15 JST first 24h stability gate
```

## 1. Conclusion

- **HOLD**
- `GO` 推奨は、下記 preconditions が **全て YES** になった後のみ。
- 現時点の主 blocker は `298-Phase3` の `ROLLED_BACK_AFTER_REGRESSION` 後 `24h` 安定がまだ閉じていない点。起点は `2026-05-01 14:15 JST` なので、最短 gate close は `2026-05-02 14:15 JST`。

## 2. Scope

- `c14e269` を含む repo HEAD から `yoshilover-fetcher` service image を rebuild
- `yoshilover-fetcher` service env に `ENABLE_WEAK_TITLE_RESCUE=1` を apply
- deploy 後 `1-2` trigger を read-only observe して rescue candidate / `289` / Team Shiny / Gemini delta を確認

## 3. Non-Scope

- `publish-notice`
- `guarded-publish`
- Scheduler
- SEO / noindex / canonical / `301`
- Team Shiny From change
- `290` 以外の ticket
- source 追加
- X / SNS / publish-notice routing redesign

## 4. Current Evidence

- impl commit は `c14e269`。`src/weak_title_rescue.py` 新規、`src/rss_fetcher.py` / `src/title_validator.py` integration、`tests/test_weak_title_rescue.py` 追加。default OFF なので live-inert。
- parent/supplement bundle は `65c09c1` + `d089340`。`0ae5505` pack consistency review v2 では `290-QA = 13/13 complete, UNKNOWN 0`。
- canonical ops state は `docs/ops/CURRENT_STATE.md` / `docs/ops/OPS_BOARD.yaml` の通り、`290-QA = FUTURE_USER_GO`、`298-Phase3 = HOLD_NEEDS_PACK`、`ENABLE_PUBLISH_NOTICE_OLD_CANDIDATE_ONCE = OFF_OR_ABSENT`。
- `2026-05-01 17:00 JST production_health_observe pass` は next-day summary と downstream ready packs に記録あり。ただし current canonical ops state はなお `298` を `HOLD_NEEDS_PACK` のまま維持。
- `299-QA` は `cache/process-history` 由来の flaky/transient 解析が完了済み。`b2e1a48` + `60242be` と `0ae5505` は product regression ではなく observe evidence bundle complete と整理。
- rollback baseline family は `yoshilover-fetcher:4be818d`。

## 5. User-Visible / Mail / Gemini / Silent Skip

- current production impact は `0`。`ENABLE_WEAK_TITLE_RESCUE` が未設定なので rescue path は live-inert。
- flag ON 後の期待効果は、A/B 群 `7` candidate family を weak-title skip から publish/review path へ戻すこと。
- rescue 失敗時は candidate disappearance を許さず、既存の visible route を維持する。internal-log-only outcome は禁止。
- mail impact は **YES (minor increase)**。既存 estimate は `~+7/day` 上限で、stable post-rollback band を前提に `30/h` と `100/d` の内側。
- Gemini call increase は **NO**。token increase は **NO**。cache impact は **NO**。Team Shiny From change は **NO**。

## 6. Implementation Order

1. clean build を作る  
   `POLICY` 境界に従い `git stash -u` または `/tmp` clean export を使う
2. HOLD 混入なし verify  
   `git log <prev_image>..<new_image>` で fetcher image delta に他の HOLD ticket code が入っていないことを確認
3. image rebuild + service update  
   `yoshilover-fetcher` の旧 baseline family `:4be818d` から新 SHA へ rebuild / deploy
4. env apply  
   `ENABLE_WEAK_TITLE_RESCUE=1`
5. `1-2` trigger 観察  
   rescue candidate ledger emit、`289` 不変、Team Shiny 不変、Gemini delta `0` を確認

## 7. Test Plan

- pre-GO gate
  - `298-Phase3` post-rollback `24h` stable
  - `2026-05-01 17:00 JST production_health_observe` pass
  - `pytest` baseline `+0` regression
- flag ON 後 verify
  - A/B 群 `7` candidate family の rescue emit 確認
  - `泉口`
  - `山崎 + 西舘`
  - `阿部 -> 竹丸`
  - `平山` 関連 `3`
  - `竹丸 + 内海`
- regression verify
  - title regression `0`
  - weak title rescue により strict validation 通過率は上がるが、既存 publish/review/hold path は壊さない
  - `289` visible path / Team Shiny / Gemini delta は不変

## 8. Preconditions

All must be `YES` before GO.

| precondition | judgment | note |
|---|---|---|
| `298-Phase3` ROLLED_BACK 後 `24h` 安定確認 | **NO** | 起点 `2026-05-01 14:15 JST`、最短 close は `2026-05-02 14:15 JST` |
| `2026-05-01 17:00 JST production_health_observe` pass | **YES** | next-day summary / downstream ready packs に pass 記録あり |
| `299-QA` flaky/transient 整理完了 | **YES** | `cache/process-history` 由来で bundle complete、product regression 扱いではない |
| clean build prep + HOLD 混入なし verify | **NO** | deploy 実行前の preflight step。まだ 290 専用には未実施 |
| rollback target image baseline 確認(`:4be818d`) | **YES** | parent/supplement で固定済み |
| `pytest` baseline `+0` regression | **YES** | `290` pack evidence + `299` N=2 evidence では `pytest -q = 2008 passed / 0 failed` |

## 9. Rollback Plan

- **Phase A: env remove**  
  `gcloud run services update yoshilover-fetcher --project baseballsite --region asia-northeast1 --remove-env-vars=ENABLE_WEAK_TITLE_RESCUE`  
  expected time: `~30 sec`
- **Phase B: image rollback**  
  `gcloud run services update yoshilover-fetcher --project baseballsite --region asia-northeast1 --image=asia-northeast1-docker.pkg.dev/baseballsite/yoshilover/yoshilover-fetcher:4be818d`  
  expected time: `~2-3 min`

## 10. Stop Conditions

- silent skip 増加(`POLICY §8` 違反)
- Gemini call delta `> 0`
- Team Shiny 変化
- rescue fail 継続
- title regression 検出
- `289` emit 減少
- publish 急減

## 11. 18-Field Resolution

### Pack completeness 13/13

| field | judgment |
|---|---|
| Conclusion | **YES** |
| Scope | **YES** |
| Non-Scope | **YES** |
| Current Evidence | **YES** |
| User-Visible Impact | **YES** |
| Mail Volume Impact | **YES** |
| Gemini / Cost Impact | **YES** |
| Silent Skip Impact | **YES** |
| Preconditions | **YES** |
| Tests | **YES** |
| Rollback | **YES** |
| Stop Conditions | **YES** |
| User Reply | **YES** |

### Additional 5 required answers

| item | final answer |
|---|---|
| Gemini call increase | **NO** |
| Token increase | **NO** |
| Candidate disappearance risk | **NO** |
| Cache impact | **NO** |
| Mail volume impact | **YES** |

### Net

- resolved: **18 / 18**
- unknown: **0**
- decision gate: **HOLD** because preconditions still include `NO`

## 12. User Reply

`GO` / `HOLD` / `REJECT`
