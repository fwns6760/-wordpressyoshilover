requests
python-dotenv
feedparser
tweepy
google-cloud-storage
pyyaml
google-generativeai
youtube-transcript-api
# 391: Tavily MCP (stdio 同梱) + Gemma 4 31B via Gemini API free tier.
# google-genai is the new Gemini SDK with native MCP client support.
# google-generativeai (older SDK) is kept for other lanes that depend on it.
google-genai
fastmcp
# 437: SVG → PNG dynamic eyecatch / X-post image generation
# cairosvg renders SVG to PNG via cairo system lib (libcairo2 in Dockerfile)
# jinja2 renders SVG templates with publisher data (autoescape XML special chars)
cairosvg
jinja2
# 437 Phase 2: Pillow-based X-post image generator (replaces cairosvg path for CJK)
# Pillow reads TrueType fonts (IPAGothic / Noto Sans CJK) directly to avoid the
# font fallback chain that produced tofu in Phase 1 (post 72079 / 72076).
# CJK fonts are installed via apt: fonts-ipafont-gothic (added in Phase 2D).
Pillow
